mask any shift astype sub shift ne cumsum sub mask groupby cumsum div
div attrs
where eq
select_dtypes mask lt shift cumsum astype bool loc columns
loc columns isin DataFrame columns
DataFrame DataFrame insert insert
groupby rolling mean dropna all reset_index drop index groupby add astype
groupby groupby
values agg apply
melt drop columns
iloc
groupby apply
clip shape shape any shape stack groupby reindex index mean std mean std mean std
DataFrame to_period
update set_index squeeze unstack update set_index squeeze unstack T
groupby max groupby where where astype where where astype loc duplicated drop columns
columns columns melt dropna sort_values reset_index drop
bool
items drop apply join groupby apply drop_duplicates last to_excel index
set_index set_index update reset_index
where iloc nunique
groupby to_csv
drop_duplicates
loc notna cumsum eq mask
apply shift first_valid_index dropna all fillna index index reset_index drop apply shift first_valid_index dropna all fillna index index columns shift shift first_valid_index apply
explode
set_index loc loc T squeeze dropna iteritems iteritems drop columns columns index drop columns stack iteritems
sort_values agg groupby rolling sort_values agg groupby rolling sort_index
groupby
apply rename columns
sort_values
DataFrame loc apply DataFrame loc
copy
replace astype loc ge all columns
droplevel set_index append set_axis unstack swaplevel columns columns join
items DataFrame T
apply
assign pivot_table index columns values count
applymap assign
DataFrame assign
replace
fillna apply set_axis
DataFrame DataFrame columns columns
replace
diff loc iloc iloc index index index explode where apply astype mask diff mask mask idxmax loc iloc iloc index index index explode where apply astype
apply
sub abs sum sum add sum sum
replace replace replace
pivot_table index columns values max fillna
iterrows iterrows compare
groupby agg columns DataFrame values index index columns columns append
astype join astype join
astype replace astype replace to_excel
reset_index merge iloc rename columns
iloc iloc min index iloc iloc min loc columns loc columns min
groupby sum unstack rename columns assign sum reset_index groupby sum unstack rename columns sum div assign reset_index
apply explode
append
index iloc index loc isin
replace astype shift div
apply apply
to_records index
index reset_index assign explode set_index
groupby shift cumsum groupby groupby sum
to_string replace
columns
sort_values drop_duplicates
astype
where
loc groupby mean
mask
assign empty set_index unstack sort_index
loc notnull loc notnull drop columns ffill dropna
ne ne loc
DataFrame index reset_index drop index loc index shift loc index columns index where fillna set_index drop index T values isnull values index loc T groupby index apply reset_index drop set_index drop index T isnull values values index loc loc T groupby index apply reset_index drop set_index drop index T columns columns values values index index iloc notnull any index index iloc values values index index iloc isnull all index index iloc values values index index iloc notnull any index index iloc T groupby index apply reset_index drop
DataFrame columns DataFrame values add_prefix
get all columns columns columns columns any columns transform any
set_index index
astype agg max pad iteritems max max pad
set_index T
merge fillna fillna drop
items loc
loc isna eq mask isna eq
shift
columns columns index
to_dict columns index to_dict to_dict index
groupby apply diff ge cumsum
apply dropna values
melt drop rename
columns columns columns bool columns
explode apply
reset_index drop
eq loc groupby idxmax sort_index bool
DataFrame round DataFrame round plot first plot merge abs
assign sum DataFrame explode sum sum sum astype values groupby sum sum gt groupby sum sum reset_index
DataFrame apply
DataFrame get get index where
astype replace
replace rename columns
sort_values iloc
columns columns columns columns
iterrows T join iloc DataFrame iterrows T join iloc DataFrame iterrows T join iloc mean std iterrows T join iloc mean std apply apply mean std apply explode set_index append unstack reset_index drop mean std set_index apply explode reset_index astype pivot_table index values columns droplevel reset_index mean std explode explode pivot_table columns index values mean std
merge apply mean std mean std DataFrame DataFrame merge
pop
filter mask eq mean fillna mean fillna
mask notnull notnull isnull assign groupby mask cumsum transform iloc where
mask columns loc mask ffill iloc
DataFrame shape index index columns columns
values
DataFrame columns reindex fillna cumsum
filter compare mask lt where mask where mask max all max fillna where mask max fillna
index iterrows iloc index isnull loc index where where isnull
DataFrame columns keys keys keys keys where notna apply
count fillna astype replace loc index count
merge
where
abs apply abs
pivot index columns values dropna all add_prefix
DataFrame DataFrame append hist DataFrame append to_csv index
pivot index columns values pivot
index groupby
loc append DataFrame columns columns index
columns copy
mean std mean std
max apply apply
ge loc le loc loc loc
apply count
append DataFrame DataFrame
notna groupby cumsum groupby shift cumprod ffill transform first ffill cumprod transform first
groupby transform mean fillna to_dict
std dropna values columns
DataFrame columns groupby size sort_values round iloc copy loc isin index
get product DataFrame explode merge product merge sort_values sort_values merge merge fillna sum merge groupby sum rename columns get product DataFrame explode get product groupby apply DataFrame explode merge product merge sort_values sort_values merge merge fillna sum merge groupby sum rename columns
index index DataFrame index columns apply index
astype
DataFrame groupby
values
loc loc values
keys
assign fillna fillna groupby last reset_index replace groupby last reset_index loc notna notna assign fillna drop groupby last reset_index assign fillna drop groupby last reset_index replace
DataFrame columns groupby sum
all at take first add DataFrame from_dict index reindex columns reindex index
DataFrame DataFrame DataFrame copy first columns T copy iloc values index replace loc apply
shift shift groupby shift diff
apply nlargest to_dict apply nlargest items
iloc T replace ffill replace columns stack reset_index rename columns iloc
filter index index duplicated
groupby to_csv index
index get get columns DataFrame get get get index index columns columns sub sub sub values sub sub sub groupby apply groupby apply
set_index apply stack dropna reset_index columns
columns filter where columns mask gt values idxmax lookup where mask any lookup index mask idxmax filter values mask values assign where mask any shape mask
transpose groupby values transpose set_index pivot columns values transpose set_index pivot columns values
append to_csv index
mode iloc groupby agg
applymap loc
all DataFrame items DataFrame items DataFrame items replace DataFrame items replace
values squeeze values values
ffill bfill
DataFrame DataFrame set_index loc
columns columns shift
columns sum values columns
DataFrame groupby mean reset_index
apply
append groupby mean groupby mean
rename
columns join columns
apply max apply max
apply replace apply index loc index loc index loc index loc index astype astype astype astype astype drop
any groupby columns groupby first to_dict get items values get max max keys
max
cumsum
DataFrame set_index mean
columns columns isin values
columns values values DataFrame
groupby rolling sum groupby shift groupby transform rolling sum shift
set_index apply sum index values
keys
value_counts fillna where eq
filter eq idxmax replace add lookup index
assign groupby merge assign groupby columns
isna all mean std index isin drop dropna index mean std count eq mean std where isnull all mean std
columns copy copy empty all reset_index drop loc first loc add index idxmax loc idxmax get append DataFrame iloc add drop all
columns filter columns to_dict columns drop columns
apply value_counts index
reset_index loc values values get index values values at loc sum index drop_duplicates set_index index index
apply DataFrame
T plot values
DataFrame columns first last first last columns replace
groupby max
apply
set_index select_dtypes eq max dot columns mask eq all
isin
DataFrame apply eval
DataFrame mask iloc isnull sum mask
groupby transform nunique fillna
div
values to_csv index mode
filter columns notna cumsum idxmax max
groupby columns mean add_prefix iloc groupby iloc columns mean add_prefix iloc
DataFrame columns empty
min max set_index reindex ffill reset_index lt shift cumsum groupby
DataFrame
set_axis
reset_index drop where applymap empty rename columns iloc drop index
isna loc loc set_index
apply value_counts fillna plot
columns columns
isin groupby agg sum sum pivot_table index columns values sum reset_index rename_axis columns replace
duplicated duplicated
replace groupby mean
fillna groupby transform first fillna groupby transform first
assign merge assign assign assign query drop columns to_string index DataFrame DataFrame columns sort_values sort_values sort_values sort_values assign sort_values assign sort_values to_string index to_string index
assign mul groupby transform sum div
mask groupby transform size eq loc mask
set_index reindex index reset_index drop loc index
apply
groupby groupby
columns columns apply sort_index
groupby to_csv index
apply DataFrame columns DataFrame iloc first
append rename rename join reset_index drop append rename append rename
groupby filter
merge
diff ne cumsum groupby transform size duplicated
merge groupby
max DataFrame columns
style
cummax merge groupby first
mask ne shift ne mask
sort_values reset_index drop cumsum sum idxmin
DataFrame diff abs index quantile index index mean values index index index eq squeeze
loc sum loc sum query sum query sum
DataFrame groupby transform sample
shift iloc iloc
reset_index melt index merge index loc loc loc shift values groupby index transform min groupby transform min index columns pivot_table index index columns values columns columns pivot_table index index columns values columns columns pivot_table index index columns values columns columns columns columns
set_index groupby resample ffill reset_index drop reset_index
cumsum mask combine_first groupby cumsum cumsum mask combine_first
sum sum
groupby
set_index to_dict copy rename columns where notnull merge drop drop rename columns merge
explode reset_index drop iloc join DataFrame
pop astype set_index unstack columns columns droplevel rename_axis columns reset_index
DataFrame shape shape
values
merge drop_duplicates merge merge
drop groupby tail index
apply
cumsum
pop sub set_index groupby keys
filter droplevel agg filter droplevel agg drop droplevel
index index
rename columns rename columns
assign diff diff diff gt cumsum
reset_index groupby first reset_index T loc
loc sum index index
all idxmax columns loc iloc
groupby transform sum groupby transform sum
drop_duplicates set_index drop_duplicates set_index replace fillna fillna
eq groupby transform any eq groupby transform any
DataFrame columns index round
groupby diff ge cumsum agg first sum sum last groupby diff ge cumsum agg first sum sum last
T eq min apply index value_counts
diff ne index index shift loc reset_index drop assign rename columns
apply
groupby agg to_dict index
groupby index apply rename reset_index to_csv index
gt shift lt mask shift fillna shift mask assign sum
join items DataFrame from_dict index reset_index
reset_index assign dropna astype
add columns append DataFrame
groupby transform max loc
rename columns values columns values columns iloc iloc replace last
loc loc loc values
loc loc
columns drop sub ge idxmax
loc apply
groupby apply iloc reset_index drop
append append set_index DataFrame merge reset_index
drop set_index index set_index stack set_index stack rename_axis index query reset_index
pivot_table index columns mean reindex columns index
transpose T
any
mul rename columns columns columns lookup index
loc diff index loc groupby diff ne cumsum
count count count append count
ne shift cumsum groupby transform size ge eq loc eq loc mul value_counts ge mean std groupby transform size ge mean std
notnull cummax isnull astype diff fillna update where eq loc cummin eq replace update update interpolate
notnull all at at loc idxmax idxmax ffill bfill
values astype shape loc
fillna ffill where eq
replace pivot columns index values to_dict replace pivot columns index values to_dict
dtypes dtypes pivot_table values index columns sum
eq bool eq groupby transform sum lt bool
groupby agg max max count count reset_index
apply iloc where iloc
apply get
all columns all update value_counts all to_csv
eq loc assign loc values
assign DataFrame columns assign DataFrame DataFrame copy DataFrame copy DataFrame
apply any loc apply any
apply
where mask notna shift mask mask replace all ffill fillna iloc columns where mask replace
groupby apply isin where
loc copy query
apply
set_index groupby apply values columns
groupby transform min
stack apply reset_index drop sort_values astype replace stack reset_index drop sort_values
groupby apply assign iloc sum reset_index drop iloc sum iloc groupby apply
merge stack groupby agg join filter columns merge filter stack groupby agg join merge apply join dropna filter columns merge filter apply join dropna
loc index values columns columns columns droplevel
loc loc
T melt
apply join
index iterrows append all sum
count isin
index index
DataFrame T columns
gt shift gt shift
nunique groupby agg nunique all
drop loc columns
drop groupby mean dropna all add_prefix
to_period add groupby last unstack ffill reset_index rename_axis columns
astype notna
DataFrame filter groupby transform nunique ne count groupby transform count filter groupby transform min
groupby where eq astype
groupby index to_period head loc lt index
where where
values
where where where where
apply replace
columns columns iloc columns
index isin cumsum loc reindex index isna cumsum where groupby
apply dropna apply
set_index reindex reset_index
add stack astype
set_index astype
astype where ne set_index stack reset_index
insert
to_xarray values transpose
count index iterrows loc iloc index drop loc index
DataFrame values index columns columns DataFrame set_index reindex rename_axis reset_index set_index loc reset_index iloc index DataFrame values index columns columns
stack reset_index drop groupby index sum unstack drop
DataFrame apply groupby set_index columns shape first head head head values values last tail tail values values
set_index columns fillna
apply fillna apply drop_duplicates reset_index drop fillna
apply abs mean std isna all
astype set_index radd index radd columns reset_index
columns
loc values
shift groupby cumsum
DataFrame DataFrame explode assign astype set_index groupby agg explode assign astype set_index groupby agg join
copy apply copy copy clip min copy stack clip min copy clip min
gt cumsum loc
eq dot columns eq dot columns iloc eq dot columns eq dot columns iloc
DataFrame
set_index join join sum
values
columns ne shift sum eq
reindex reindex index copy reindex index copy
index groupby apply loc max max
sum sum values values
apply
groupby transform mean groupby filter mean
isin mask where groupby bfill notnull groupby groupby shift cumsum agg sum last
eval to_json iloc iloc astype astype rename columns groupby apply to_json values
ne ne all bool
columns columns sort_values
groupby filter any any
apply append
groupby transform first rank astype
groupby agg nlargest mean reset_index
groupby corr eq reset_index drop rename reset_index
DataFrame groupby squeeze
index loc loc
DataFrame columns DataFrame columns append
apply
duplicated duplicated
mask loc
DataFrame index reindex
groupby ne shift cumsum agg first count groupby ne shift cumsum agg first count
drop columns columns iloc lt last iloc columns iloc lt bool columns iloc lt
values replace
to_string
isna
where ne shift where ne shift ne shift bool
where ge
duplicated eq duplicated
DataFrame columns DataFrame columns
iloc iloc iloc iloc
append index columns
mask DataFrame values index keys stack reset_index drop groupby sum astype bool
DataFrame index DataFrame columns columns columns columns loc
DataFrame
pivot columns quantile stack
columns loc unstack values index groupby apply unstack
first groupby agg max first reset_index
eq groupby transform all eq groupby transform nunique eq eq
dropna any
DataFrame size columns any bool
assign
eq eq loc shift
T dot
DataFrame where values values index index columns columns
columns loc loc dot columns isin dot columns
reset_index drop to_dict groupby reindex values
DataFrame append round append append append round
dropna select_dtypes columns all dropna select_dtypes columns all
diff fillna
astype assign fillna
sum clip
DataFrame index DataFrame
join apply dropna index dropna index dropna index add_suffix
cumsum shape sum diff sum cumsum shape
size DataFrame columns groupby count nunique iloc groupby apply dropna
DataFrame iloc values
set_index columns rename_axis set_index columns rename_axis columns columns columns where columns
diff dropna all bool
all any
values plot columns values
sum sum reset_index sum sum sum reset_index sum
copy DataFrame values size notnull mask mask mask mask mask mask mask size size DataFrame interpolate update
DataFrame index columns reset_index
copy columns groupby get groupby mean reset_index
insert insert
DataFrame DataFrame
reset_index melt index set_index groupby index resample ffill drop columns index reset_index pivot index columns resample first
min max add apply min max index min max add apply loc max max add apply
notna groupby transform any bool
loc index index loc index index loc index index
value_counts merge
get
set_index columns columns stack reset_index drop
apply sum mul sum
items
cumsum add shift fillna
to_json resample apply
iloc iloc iloc iloc
values ne values shape DataFrame columns columns shape ne values values shape DataFrame columns columns
columns columns columns columns
ne shift bool
DataFrame columns iloc iloc columns columns
DataFrame columns to_json columns join join index columns
set_index reset_index
columns dropna reset_index drop iterrows T
DataFrame apply apply all columns apply apply
append nlargest assign sum cumsum rename_axis reset_index append nlargest assign sum cumsum assign mul astype mul astype rename_axis reset_index
groupby index index max
iloc unstack reset_index loc
DataFrame columns DataFrame columns DataFrame columns merge merge merge merge
eq where groupby ne shift cumsum
values index to_dict
columns iloc columns columns
join
groupby apply join reset_index rename columns
iloc mean iloc fillna
apply columns values apply index
loc melt DataFrame loc values DataFrame loc values loc values
merge
fillna
index sum sum columns pivot columns index values DataFrame T fillna index columns columns columns fillna apply join fillna nlargest iloc nlargest index apply join fillna nlargest iloc nlargest index
DataFrame values pop apply stack reset_index drop rename join reset_index drop apply stack reset_index drop rename drop join reset_index drop DataFrame index T apply stack reset_index drop rename drop join reset_index drop mean std DataFrame values mean std
DataFrame columns append append
mask values any mask
lookup index lookup index
groupby transform size groupby transform max
set_index reindex reset_index
DataFrame values DataFrame
min min values mean std mean std min min values min mean std mean std mean std
loc shift
index
notna cumsum ffill select_dtypes columns fillna mean join groupby agg reset_index drop reset_index
replace groupby agg join join count assign count groupby agg join join count count
columns columns columns DataFrame columns columns columns columns columns DataFrame columns sum DataFrame columns size
apply DataFrame
set_index apply nlargest index index reset_index
index index
index append replace append replace append index
value_counts groupby filter
index groupby mean
DataFrame columns DataFrame T DataFrame to_csv
index index
merge dropna columns
DataFrame
rolling mean rolling sum loc mean sum rolling agg mean sum loc mean sum loc mean sum shift mean sum
iloc columns iloc cumsum le fillna
dropna values columns columns
dtypes DataFrame DataFrame apply mean std shape apply iloc iloc columns mean std all
columns
drop DataFrame
DataFrame drop
set_index join replace add_prefix join apply add_prefix
items items
groupby nunique index
items loc
iloc join iloc sum rename
DataFrame columns DataFrame columns DataFrame columns index
pct_change backfill
columns columns
all
any
sort_values drop_duplicates
mask duplicated loc mask groupby add astype
values agg dropna
index columns values
set_index max reset_index
isin isin any loc
loc index min itertuples index values size loc loc index min itertuples index values size
loc reset_index drop loc add_prefix reset_index drop add_prefix reset_index drop
columns DataFrame index columns columns
values values
values eq where values eq stack mean
replace count count count count columns
DataFrame columns DataFrame T columns append T append index
where ne shift ffill fillna size copy diff index index mean std where ne shift ffill fillna mean std
iterrows nlargest sum
xs xs xs xs
loc isnull ffill
index index index sort_index reset_index drop
groupby
mask eq loc mask index mask idxmax
DataFrame index T columns index
loc loc values where
values where update loc index update keys values append append append DataFrame where where where append loc where append loc append merge DataFrame index where where where
to_csv mode index any
dtypes apply apply dtypes
dropna std
groupby transform size
cumprod reindex index fillna ffill
abs abs DataFrame where eq eq
merge apply add_prefix notnull sum loc index values groupby index apply dropna mask
astype astype apply isnull
DataFrame mean groupby apply reset_index quantile values mean groupby apply reset_index
isin bool isin bool
interpolate interpolate
sum
DataFrame columns
groupby agg drop_duplicates index groupby agg drop_duplicates index groupby set_index unstack drop_duplicates index
sort_values drop_duplicates last
columns rename columns drop columns copy index index index index reset_index merge reset_index index drop columns index
empty shape values values astype
DataFrame filter filter iloc sum sum
drop replace sample
iloc index iloc iloc shape shape any iloc sort_values groupby transform
astype
lookup keys itertuples index append DataFrame keys join values reindex set_index reindex reset_index
filter columns loc
columns loc
dropna groupby isnull dot columns
apply
mask isin mask bool loc mask diff loc mask values loc mask diff loc mask diff
DataFrame index columns loc loc
stack value_counts index index index
loc loc loc loc dropna loc loc loc dropna
shift shift mask diff bfill mask mask apply shift mean mean
keys
T reset_index rename columns index
groupby shift cumsum filter size groupby shift cumsum transform size
astype reset_index index astype astype astype
pivot index
columns groupby apply to_dict columns iloc groupby iloc apply to_dict
groupby filter values
columns columns
index iloc index values iloc index values DataFrame columns
DataFrame merge merge drop
DataFrame DataFrame fillna merge where isna drop
fillna sum
replace fillna replace fillna
isin
columns values astype mean std astype mean std
groupby columns columns transform size index max
select_dtypes columns apply
join groupby size unstack add_prefix join groupby value_counts unstack add_prefix merge add_prefix
values
items
loc astype ffill
astype iat astype
groupby transform max where duplicated
set_index join groupby apply index dropna itertuples loc loc sum reset_index DataFrame columns set_index join set_index groupby apply index dropna drop set_index itertuples loc loc sum reset_index
shape shape shape shape shape rolling apply sum astype count iloc max sum items values sum DataFrame size columns
groupby replace sum columns columns replace sum
dropna set_index to_dict values dropna set_index to_dict iterrows at iterrows at at at
plot loc plot loc values index plot combine resample sum plot
join query
merge where
isin isin all bool isin all bool
values
astype loc loc
columns DataFrame columns T reset_index drop rename columns iloc drop index
groupby shift add_prefix shift add_prefix fillna astype
merge eval groupby apply rolling max astype bool
shift rename columns columns
columns iloc values values DataFrame iloc values values DataFrame
apply astype sum
copy
values append
DataFrame apply replace replace apply
columns
apply apply to_csv index
value_counts min groupby head
sample DataFrame columns empty notnull sum idxmax append columns drop columns columns loc notnull any loc
isin index
groupby sum
columns astype bool shift astype bool cumsum index get count count astype bool groupby nunique astype bool groupby count groupby sum
where mean
values
melt sort_values
rolling mean iloc
groupby apply join
stack iloc columns unstack swaplevel columns columns join
apply max
columns append columns index append index combine_first reindex index columns
diff eq bool
T index shape columns shape columns unstack
iloc eq values
where shift shift fillna astype
merge groupby apply sum reset_index
apply apply
DataFrame columns apply join
