merge DataFrame columns where eq
reset_index reset_index drop set_index append rename_axis sample index sample set_index
bool index
columns apply to_excel
groupby mean reset_index groupby agg reset_index values median apply
DataFrame columns DataFrame
DataFrame DataFrame DataFrame merge fillna drop columns merge fillna drop
groupby transform fillna mode groupby transform fillna mode
apply isin any apply apply isin any
loc astype loc astype
DataFrame apply
index DataFrame index DataFrame index applymap apply groupby apply values columns DataFrame append loc loc columns index columns columns index index
bool
mask loc mask loc mask loc mask loc mask mask
DataFrame DataFrame reset_index melt index sort_values index agg groupby apply to_dict
columns
apply join join apply join join
loc loc
append groupby sum loc groupby sum values append groupby agg sum count reindex loc groupby sum values
diff fillna update
values append append append append values append
index sort_index
dtypes
replace
groupby
apply apply
DataFrame columns assign assign index index DataFrame columns DataFrame
DataFrame values values groupby mean
eq eq groupby cumsum
drop apply
set_index T count mean min loc T reset_index count mean min max
DataFrame tz_localize tz_localize
lookup lookup apply lookup
where
merge astype astype
get values xs values values iloc values DataFrame xs columns columns
merge loc
iloc iloc iloc iloc iloc iloc iterrows
DataFrame mean median median
applymap
astype
replace apply join
DataFrame groupby agg index index replace unstack loc groupby count
fillna dtypes replace fillna dtypes replace
groupby shift eq eq groupby shift eq eq
mask mask loc mask
DataFrame apply apply DataFrame get apply loc get at DataFrame assign DataFrame take iloc index loc assign sort_values reset_index iloc
loc lt idxmax max where lt DataFrame index max where lt index
items items DataFrame columns set_index
mask product eq eq update loc mask update loc mask
groupby append sample max get replace sort_index sample groupby lt
replace mean replace mean add apply
eq eq groupby cumsum agg droplevel eq eq groupby first groupby first sub
describe size
to_excel to_excel
assign explode
loc
values
mask loc mask shift mask mask shift loc mask shift mask
DataFrame
iloc iloc shift fillna
where
groupby apply
apply
eq set_index groupby T agg idxmax T max set_index groupby T agg idxmin T min reset_index drop sort_values T drop_duplicates set_index add_suffix drop sort_values T drop_duplicates set_index add_suffix reset_index
lt groupby transform first loc groupby transform min
loc index apply groupby transform count
empty columns DataFrame columns columns loc loc
sort_values
get div append append append append append append append append append DataFrame
DataFrame groupby iloc append add drop groupby apply shape
groupby agg eq shift reset_index apply agg
set_index T
boxplot
columns columns columns columns
append
DataFrame DataFrame merge merge merge
append
append columns
max values bool
drop_duplicates last
apply join join join
replace
index values add DataFrame sort_values reset_index drop insert set_index join
iloc idxmax first index idxmax
index astype astype get index query mean apply
sort_values loc sort_values mask mask sort_values
loc groupby transform size where duplicated
notna sum loc
loc idxmax
DataFrame apply join groupby apply groupby agg min max
apply explode values values items
values groupby loc values T T DataFrame index columns T T DataFrame index columns loc
style apply columns
DataFrame merge
min min max DataFrame columns size sort_values get last
index append iloc max min index iloc max min index to_csv index index to_csv
count product product product product product product count product product product product product product count product product product product append DataFrame
query
DataFrame where mean fillna DataFrame where mean fillna
apply
groupby agg dropna where astype bool
sort_values
get DataFrame head first get DataFrame columns set_index DataFrame columns set_index DataFrame columns set_index combine index index index add head items plot loc
replace groupby first replace
axes count sum get all columns axes groupby agg plot plot plot
combine_first set_index set_index reset_index
index loc loc groupby apply reset_index merge
items append append append
loc pivot pivot_table index columns values pivot loc query
sort_values drop_duplicates last
filter columns replace
DataFrame DataFrame where merge
shift diff diff fillna loc index isin index
groupby sum
groupby ne shift cumsum transform size
merge apply replace
pivot index columns values count columns reset_index
index index loc
iloc cumsum astype cumsum all all astype astype groupby agg first first first first first first last
get merge drop_duplicates set_index interpolate resample interpolate reset_index merge merge
apply any
at
DataFrame columns get apply apply
sort_values sort_values
any stack
value_counts index eq
columns join
merge assign fillna fillna drop merge assign filter fillna values sum drop
all empty append
DataFrame values values values values values
notnull
get first values groupby agg first sum columns sum columns copy merge apply merge columns
DataFrame all duplicated duplicated loc copy rename columns columns columns columns copy drop first assign drop_duplicates first copy append first index append sort_index append append
merge loc drop
DataFrame replace apply
index
index shift iloc
product product values append product product values product values append product product values product values append product product values product values append product product values DataFrame product values append product product values DataFrame
product
DataFrame columns apply
values T any diff DataFrame columns
iterrows columns
columns iloc isin mask notnull mask mask shape mask T T T mask T
duplicated duplicated merge columns columns loc duplicated duplicated merge
sort_values sort_index
items
to_string iterrows
T T set_axis T T T set_axis drop_duplicates
columns melt columns columns melt apply values sort_values empty values dropna values shift where values mask eq shift bfill loc mask columns reindex columns
columns dropna shape
groupby shift groupby shift
to_csv
select_dtypes columns
loc idxmax append DataFrame loc loc idxmax append DataFrame
set_index replace set_index replace fillna reindex_like eq any where
set_index reindex min reset_index rename columns index groupby ffill
index
shift loc apply loc shift
add_prefix
value_counts unstack add_prefix sum sort_values reset_index
copy apply
iloc isna all
columns values fillna columns values fillna
DataFrame columns columns columns columns
join index join index
groupby diff
groupby shift fillna ne astype
join merge
notna notna
append append
isin isin isin isin isin isin loc loc isin isin loc isin isin
where where
index items index index items
stack duplicated unstack DataFrame where columns columns index index style apply columns style apply stack duplicated bool stack duplicated unstack
columns loc columns loc loc loc columns
append to_csv index
index values drop
DataFrame set_index sort_index columns groupby agg columns sum reset_index columns pivot_table pivot index columns values pivot_table index pivot_table columns append append pivot_table loc append append min
groupby apply sum reset_index count assign groupby sum astype reset_index count
columns copy round values diff diff copy sort_values diff groupby diff first diff diff diff isnull where cumsum fillna ffill duplicated sum first reset_index set_index groupby first drop diff all diff copy index reset_index set_index index sort_index get copy index
DataFrame columns columns where astype bool where astype bool columns columns
itertuples append
DataFrame DataFrame
iterrows append append transform iloc
DataFrame DataFrame index itertuples join
sort_values pivot_table index columns values sum cumsum fillna groupby apply sum size
all get
compare gt sum groupby sum reset_index
replace values
merge explode apply merge reset_index drop merge
bool bool
abs
groupby T boxplot
applymap any bool
join keys
columns columns columns columns
eq eq shift fillna
DataFrame
replace replace dropna all
ffill isin all DataFrame join DataFrame join mean std ffill isin all mean std
apply all apply apply any apply any iloc first
values astype columns truncate mean std values mean std
ne shift values loc
eq eq reset_index merge assign merge assign groupby index max rename eq eq reset_index merge all DataFrame merge reset_index assign merge reset_index assign min sort_values groupby index head set_index index filter rename columns eq eq reset_index merge reset_index sort_values groupby index tail set_index index filter rename columns
loc values columns
unstack stack reorder_levels sort_index
DataFrame loc loc loc fillna items mask loc mask fillna
drop sort_values drop_duplicates last drop sort_values drop_duplicates last to_csv index
groupby agg to_dict
all all columns at loc loc loc loc loc columns loc loc columns columns loc loc
set_index set_index mul rename reset_index
ndim apply min groupby transform min DataFrame where index index columns columns
items replace
to_csv index mode
ffill
groupby agg min max groupby agg min max
DataFrame DataFrame DataFrame
DataFrame columns drop columns
where gt le
join
min abs abs idxmin append append
replace astype astype replace replace replace eval astype replace eval astype
mask mask
stack
append DataFrame
ne shift any cumsum sum join astype sum join groupby agg droplevel reset_index
DataFrame append DataFrame values DataFrame DataFrame append DataFrame first DataFrame append DataFrame DataFrame append
count count
count
mask columns notnull dropna drop index
duplicated set_axis columns duplicated replace assign sort_values astype groupby transform first drop
size astype index DataFrame columns index groupby agg sum sum sum groupby agg sum sum sum
groupby transform sum
lookup melt size
DataFrame index columns loc loc loc loc loc
assign merge assign query groupby mean
copy groupby iloc iterrows loc loc drop
get DataFrame
merge
DataFrame combine_first mask isna isna mask
DataFrame
isin index iloc
groupby
shift fillna astype add_suffix shift add_suffix fillna astype
DataFrame columns apply
DataFrame columns columns
append
eval
iloc append drop index
rename columns merge eq
groupby sum loc sum
DataFrame
DataFrame value_counts index index apply
DataFrame combine_first
DataFrame index columns
product get values combine values product merge sub at append append DataFrame values product values append DataFrame
pivot index columns values fillna DataFrame index index columns index
T rename index
values loc iloc apply
merge
append groupby
drop_duplicates first first last
DataFrame DataFrame DataFrame append append reset_index drop loc loc loc DataFrame index DataFrame index DataFrame index append append index reset_index drop
DataFrame columns
index isin isin index
fillna fillna mask mask
sort_values reset_index groupby index cumsum index values where duplicated last reset_index drop where duplicated last reset_index drop
groupby index transform cumsum
to_csv
query value_counts reindex fillna
columns merge assign drop columns columns set_index merge set_index apply reset_index
cumsum gt groupby
append DataFrame merge drop_duplicates
DataFrame set_index DataFrame set_index loc loc loc
apply notnull
set_index set_index filter stack reset_index drop
merge drop DataFrame columns DataFrame columns merge drop sort_values
items sort_values items groupby append sum count DataFrame columns items sort_values DataFrame columns items groupby sum count DataFrame columns append
drop_duplicates set_index
any values
sample where DataFrame set_index size assign dropna where index isin dropna drop columns assign shift index dropna shift size head to_string
DataFrame iloc iloc replace iloc iloc apply
columns columns
max copy insert
columns columns columns columns columns join columns sum count sum items columns get columns
groupby plot nunique groupby plot
merge loc isna drop
loc isin groupby sum reset_index
astype astype groupby transform size sub clip groupby apply value_counts head index value_counts head values astype groupby max reset_index
apply
keys columns values values columns values keys values append any append
DataFrame
apply duplicated apply isin shift isin
shape size shape
set_index groupby stack unstack columns columns join
join groupby agg join
rename columns pop items
groupby groupby count
stack reset_index var pivot var rename_axis index columns
loc loc loc loc
iloc iloc groupby transform
columns columns astype T
apply replace astype
merge astype where
get size groupby size transform size filter last size filter groupby apply rename apply rename reset_index
DataFrame columns drop_duplicates first drop_duplicates last empty drop_duplicates
DataFrame columns loc loc cumsum
DataFrame copy DataFrame
DataFrame DataFrame eq eval eq eq eval eq
eval values first DataFrame columns first first replace replace values replace replace values
set_index reindex min max fillna index reset_index
mask mask mask values
where eq dropna groupby isna cumsum
to_excel iloc iloc to_excel
loc isin loc eq
groupby apply join duplicated index isin
values isin values isin values loc sum apply
sort_values drop_duplicates
ge DataFrame set_index drop
columns last columns columns values rename columns columns columns values columns
DataFrame index columns DataFrame index columns reindex values set_axis keys reset_index DataFrame DataFrame DataFrame set_index set_index reindex set_axis columns reindex set_axis reset_index
groupby transform diff sum astype bool groupby transform max min groupby transform max min
all
pivot lookup drop_duplicates lookup set_index pivot join lookup join
pop explode sum add_prefix
assign where shift replace dropna all ffill groupby agg join reset_index
pivot_table index columns values
values DataFrame columns add first get size size size join join size size cumsum sort_values sort_values
groupby median drop columns groupby median
loc columns values loc columns values where mean pivot product columns merge pivot index columns values reset_index assign pivot index columns values reset_index assign assign loc T loc columns astype values loc columns astype values where mean pivot product columns merge pivot index columns values reset_index assign pivot index columns values reset_index assign assign apply
isin
reset_index index mask index loc mask groupby ffill
columns loc columns apply columns loc loc apply
merge
astype
to_csv
set_index reset_index
DataFrame
DataFrame from_dict groupby apply corr values columns
sample groupby iloc
mask eq isna loc mask
ge le any
apply
isin isin values values values
copy drop drop drop drop drop
groupby apply iloc DataFrame groupby shape append reset_index iloc DataFrame groupby shape append reset_index iloc
last columns index loc columns last columns index iloc
index
pipe pipe pipe pipe
DataFrame filter
groupby transform nunique
apply apply
any all loc loc diff astype astype loc diff loc diff
get DataFrame rename columns astype query dropna assign append
groupby transform count groupby apply assign shift reset_index drop groupby transform first
hist value_counts
loc groupby count transform nunique loc groupby count transform nunique
get index DataFrame columns values index values stack index index groupby apply ge astype le astype groupby sum cumsum shift assign values sum apply sum apply sum
get first first iloc values values first mask mask isna values replace first first index ffill fillna first filter first add first index mask values set_index first append columns first columns columns astype values mask stack columns mask stack rename_axis reset_index add add pop pop index sort_values reset_index drop
columns
reset_index rename columns index loc fillna bfill DataFrame copy shift fillna merge fillna bfill
melt set_index pivot fillna
pop first DataFrame columns
set_index join groupby agg reset_index
groupby sum
mul shape replace plot shape columns
columns DataFrame columns DataFrame transform columns
DataFrame append to_excel index
plot
groupby diff lt apply rolling mean shift where cummax
sort_values groupby shift mask notnull loc mask mask apply where where fillna DataFrame size size sort_values groupby shift T sum round mask notnull loc mask mask apply sort_values groupby shift mask notnull loc mask mask apply where where fillna mean std mean std
apply apply
groupby product apply join reset_index product
DataFrame columns DataFrame columns merge
aggregate append DataFrame columns items columns columns columns
assign merge assign drop query groupby first reset_index
columns columns
first apply T iloc index
groupby agg items groupby apply items
apply
info info info
std var sum var sum var var var std rolling std dropna std mean std mean std
items apply
apply replace apply replace
loc assign replace apply explode loc isin
groupby agg
groupby shift eq cumsum agg last join reset_index drop
DataFrame replace
to_dict columns columns columns columns columns columns
bool drop drop groupby apply
columns set_index set_index
where
first ne shift groupby sum max ne shift groupby sum
iloc
columns apply join values astype replace replace
mean
loc append loc
isin
loc loc loc loc DataFrame
groupby size reset_index
DataFrame DataFrame columns
eq cumsum ne groupby cumsum
iloc count values count
index index
explode
loc loc loc set_index iloc replace
get get query pct_change empty get all DataFrame get add at
replace values replace replace drop_duplicates dropna set_index drop_duplicates dropna set_index
add add
loc isin
groupby agg
plot loc loc
groupby agg sum reset_index
columns sum columns sum index values size size columns size size columns DataFrame columns size sum max sum columns max columns
DataFrame drop index diff diff any
apply replace astype
DataFrame values index index
set_index
groupby mean unstack groupby mean unstack
eq
mode groupby transform mode mode
values values
stack
index
assign groupby agg sum idxmax loc
loc loc dropna DataFrame drop index DataFrame
rank astype
astype
cumsum
groupby filter count values shape groupby filter count values shape
sample sample sample
DataFrame columns get columns replace columns
drop_duplicates groupby ffill astype
loc values
index columns loc index index diff mean append
loc index
items join
keys mode join items
set_index index columns append values
sum eq sum sum isin sum ge
where where
max
DataFrame values append
join shift cumsum index index columns columns apply columns DataFrame iloc DataFrame iloc
DataFrame values columns DataFrame columns applymap
reindex columns
get all xs values all xs all DataFrame index columns xs index columns columns append fillna append columns index all empty values sort_index index values sort_index columns xs values shape shape values
update DataFrame index index iloc columns
values filter isin values
DataFrame columns astype
apply apply
assign
groupby value_counts groupby count unstack fillna
merge
groupby apply shift shift shift
DataFrame columns DataFrame columns isin rename columns
first append append first append append DataFrame DataFrame
iloc index index
columns columns values sub values
columns loc
mask eq mask bool
count apply count sum
groupby sum reset_index
DataFrame DataFrame columns apply get apply
sort_values head
DataFrame astype rename columns keys columns
DataFrame apply DataFrame apply values
drop_duplicates set_index groupby apply isin
groupby apply mode
groupby transform max ne duplicated
mode any append last append to_csv index
columns columns
columns round
sort_values assign shift shift head
count groupby count count astype set_index count unstack count reset_index columns
merge loc
DataFrame columns notnull drop
groupby apply bool shift fillna cumsum agg first last join
where isin
DataFrame columns index to_excel index
index loc index index size empty index index sort_values iloc index append index index iterrows plot
append
groupby transform size DataFrame size groupby transform size mean std duplicated mean std duplicated groupby transform mean std
assign groupby transform count
columns values where append DataFrame columns columns iloc iloc
DataFrame apply apply to_json
apply
append append append style apply
loc values loc values loc values
reindex index min index max set_index groupby apply drop reset_index groupby
copy groupby agg mode dropna copy groupby agg isnull all mode dropna
values index reset_index set_index index fillna reset_index groupby index apply reset_index drop index
groupby to_excel
DataFrame set_index T
items merge
groupby diff fillna
empty merge append
loc groupby transform any sort_values assign groupby ffill dropna reset_index drop
sort_values groupby pct_change where diff
columns set_index groupby mean
DataFrame
append
get first pop DataFrame columns
replace groupby eq cumsum first
merge
append dtypes bool
isnull isnull astype drop append to_csv index merge drop to_csv index
eq shift ne shift ne shift index index isin index index loc drop
loc max columns
DataFrame sort_values reset_index drop
loc loc
append append append style apply
groupby transform count ne copy astype sort_values astype sort_values count groupby transform count count ne mean std mean std
head iterrows iloc sub replace iloc to_excel index
value_counts nunique iloc nunique sort_values reset_index drop melt sort_values reindex index mul values astype melt sort_values reindex index mul values assign set_axis sort_values values values
to_dict groupby groupby to_dict groupby apply to_dict to_dict
all values assign merge explode drop columns groupby join agg add
groupby index mask count loc mask count count loc count update update count sum
apply isin apply product apply isin apply product
plot
groupby product apply loc cummax mask sum product
groupby agg sum sum
values values
merge drop rename columns
groupby transform size groupby filter value_counts
replace all bool astype
hist hist sum size size size hist hist hist T hist hist sum hist columns hist hist sum sum hist hist hist
columns
apply
assign
merge merge fillna
apply explode dropna DataFrame index index stack unstack
DataFrame
apply
clip where
append append append sum DataFrame
index index loc loc
last last last last last last last last fillna index join drop columns last last
get
index
shift shift fillna
set_index reindex reset_index
DataFrame columns columns reindex
DataFrame diff shape columns columns index index index
dropna any
DataFrame columns append columns
DataFrame add_prefix groupby apply reset_index
equals equals append equals equals append
loc stack sum loc stack eq any sum count all sum
DataFrame DataFrame
apply
T columns reset_index drop
DataFrame columns columns T plot columns columns T plot T
DataFrame columns apply replace
astype astype loc pop pop
DataFrame DataFrame DataFrame dropna
to_parquet sort_values diff min cumsum diff groupby min reset_index merge astype
DataFrame index values to_dict
iloc astype
groupby groupby items append update items items
shift eq cumsum groupby agg first last loc eq last sub first values mean std groupby groupby diff loc isin mean std
merge assign explode assign astype
T mode T T
append
DataFrame columns sum sum sum sum sum mean sum sum sum sum sum sum sum groupby apply
all replace all all DataFrame append DataFrame pivot DataFrame to_csv
DataFrame DataFrame items
groupby agg join to_string
loc dtypes loc dtypes fillna
info index info shape info info info
to_excel index
reindex index loc
loc
groupby count plot
fillna apply
style apply to_excel index
loc rename columns merge set_index
DataFrame groupby merge drop append
nunique groupby size sum assign merge rename reset_index query drop columns groupby size
DataFrame columns DataFrame index append
columns index iterrows columns
append
join assign apply join assign ne
loc
sum melt groupby sum
loc product isin
rename columns filter filter filter columns columns columns filter rename columns columns rename columns append
DataFrame assign count product product dropna drop_duplicates count empty loc count idxmax apply
groupby cummax ge ge loc groupby diff fillna mask bool
DataFrame index columns dtypes values
append first append DataFrame columns compare
columns values columns
DataFrame columns groupby index to_csv
groupby index round items index groupby groupby head
set_index resample ffill
empty values replace groupby sum set_index update reset_index loc fillna groupby sum set_index update reset_index
merge where drop
iloc iloc
groupby idxmax loc
iloc iloc
get columns DataFrame columns empty columns append append
sub where index groupby transform first
DataFrame columns DataFrame columns
values
join keys index get fillna plot items loc index plot fillna
pivot index columns values
groupby transform min
append sum values sum values DataFrame from_records columns
iloc loc iloc DataFrame columns
rename columns rename le apply le apply all columns drop apply le
to_csv
mod iloc astype mean mod diff mod
isin value_counts
min
merge
join pipe sort_values columns to_csv index
DataFrame columns cumsum shift fillna shape shape values
explode value_counts plot sum plot
columns columns mask first duplicated aggregate assign apply groupby agg add join rename values first empty fillna mask columns duplicated duplicated loc index duplicated groupby apply duplicated duplicated where
set_index iloc
empty DataFrame DataFrame index index columns columns where style apply
groupby apply min index
get attrs DataFrame columns replace replace append apply dropna
DataFrame sum sum mean std mean std DataFrame sum sum mean std mean std
groupby filter mean
DataFrame replace
groupby cumsum agg first sum first
loc apply
DataFrame plot
apply apply
reindex columns
unstack items unstack unstack sort_values set_index
DataFrame DataFrame merge
groupby values sum set_index values div reset_index groupby values sum groupby values sum div reset_index
groupby apply nlargest
apply
loc eq iloc agg
sort_values columns reset_index drop sort_values columns reset_index drop equals
pivot_table index columns values sum columns iloc columns astype columns
DataFrame pivot_table values columns index count to_dict
loc values
isin loc
join keys replace get DataFrame
DataFrame from_records itertuples columns DataFrame from_records itertuples columns DataFrame from_records itertuples columns iloc DataFrame values values values take all assign groupby apply reset_index drop max DataFrame values values values values loc
columns DataFrame columns columns append append append loc columns DataFrame columns columns loc
merge astype groupby astype index iterrows values
where where
where notnull astype
DataFrame columns columns columns values columns empty columns columns columns values all
iloc iloc
columns values DataFrame DataFrame DataFrame DataFrame index columns
isnull all shift groupby cumsum
melt
groupby apply min max reset_index
isin isin reset_index melt index merge drop columns set_index index unstack transform assign xs sum
any diff diff diff diff cumsum shape
index index index iterrows index index iloc iloc iloc iloc loc
stack rename_axis reset_index
items items append iteritems append
merge groupby apply
groupby all sum groupby any sum any all all groupby all sum
rename index index
all merge all all all DataFrame info
get get pad get items div join get get DataFrame columns append to_csv index
sub index sub
DataFrame columns loc values iterrows
loc loc
apply join join
DataFrame values columns columns groupby
median astype quantile astype
groupby transform fillna mean
set_index stack reset_index rename columns melt
DataFrame DataFrame join corr
pivot_table index columns size rename columns groupby apply join sort_values drop columns pivot_table index columns size rename columns groupby apply join
replace min min where min shift isna max max where max shift isna dropna all min combine_first max
DataFrame
merge drop
columns columns T groupby columns sum
drop melt prod merge groupby prod apply iloc iloc prod
apply
merge DataFrame columns loc
isnull loc loc apply
add rolling apply plot plot
isin
merge
append append values at DataFrame columns iloc reindex index drop
groupby apply reset_index sort_values
stack isin value_counts set_index reset_index melt isin value_counts
DataFrame
merge
append to_csv index
DataFrame DataFrame merge
drop index
DataFrame loc columns
DataFrame first columns first DataFrame columns
DataFrame columns columns plot
reindex columns
attrs
explode value_counts mean std fillna mean std mean std
values
DataFrame sample groupby nunique
loc ge loc
assign sort_values columns
cumsum shift loc size loc iloc loc iloc get groupby apply loc
index iterrows
shift
DataFrame T values groupby cumsum values eq astype groupby cumsum eq DataFrame T sort_values reset_index drop index all get last index iloc loc get all last loc
DataFrame median astype values median astype
stack max astype bool index
T values shift T values shift shift shift
gt min first gt min where gt min values T copy min replace min DataFrame index columns index columns copy at div min T plot
items
groupby diff fillna
cov values cov index nunique cov index nunique cov shape values values cov groupby apply loc dot values loc mean std values values mean std
assign apply assign
pivot index columns values reindex
assign values assign apply assign mean std mean std mean std mean std mean std mean std
product join product columns values columns values columns filter product items DataFrame abs abs abs abs abs abs index columns
DataFrame index columns
apply
groupby mean
where index index get
DataFrame add_prefix add_prefix DataFrame merge merge
DataFrame DataFrame DataFrame reset_index drop reset_index drop
values values
apply
index assign merge assign drop columns reset_index
size
sort_values
rename columns columns columns merge set_index
index min loc index DataFrame
loc value_counts reindex reindex values merge
axes axes
filter loc get get index set_index assign add values values loc values loc values loc values get mask update mask update values where mask mask astype update update set_index index reset_index set_index index
sub set_index
bool
select_dtypes columns clip
get assign get all columns all values loc copy dropna loc all values loc copy dropna loc iterrows sum iloc apply at plot round plot values keys loc size plot
mask mask at mask astype sum groupby sum
index values values
merge rename columns merge set_index merge drop
lt le eq gt ge ne
astype astype plot value_counts plot
DataFrame apply
columns rename columns all columns merge columns columns columns columns to_csv index
eval replace bool
shift cumsum rename groupby agg join reset_index drop
loc isin
take DataFrame assign index combine get merge
loc
groupby agg idxmax loc groupby agg idxmax max
groupby apply dropna expanding mean apply dropna diff reset_index drop reset_index drop dropna expanding mean diff DataFrame groupby apply reset_index drop
pivot index pivot_table values index columns sum reset_index rename columns columns sum values sum
update astype to_excel join index update
index apply
apply
stack groupby transform size groupby agg join stack value_counts index apply join
join query query where
plot where plot
ne groupby transform all bool groupby transform nunique
DataFrame DataFrame
merge fillna merge fillna
DataFrame apply apply apply sort_values DataFrame sort_values
itertuples append count
columns merge
dropna columns columns columns
get apply keys explode explode apply
shape columns get values get
DataFrame explode pivot columns values
T to_csv first to_csv
where isna
melt apply loc isna loc fillna
reset_index to_excel index to_excel index
copy
all iloc iloc append append abs append append append DataFrame columns columns columns DataFrame columns to_csv add to_csv T
DataFrame DataFrame DataFrame
join
DataFrame columns values values
corrwith pop abs gt bool items
shift
sort_values groupby diff
get
DataFrame apply
groupby transform nunique eq loc
astype astype astype astype
groupby transform count count count sum sum sum
sample set_index columns columns T groupby agg xs join filter notnull xs T
set_index
DataFrame columns fillna
pivot_table index columns values rename_axis reset_index
shift shift
apply apply
groupby agg sum sort_values
values
to_excel index
DataFrame index index
apply
set_index loc value_counts index
add DataFrame columns copy
sort_values where shift shift astype sort_values where shift shift groupby cumsum replace ffill groupby agg min max
astype groupby transform sum groupby transform sum loc gt eq
groupby sum
merge
apply
get apply shape
DataFrame
DataFrame columns columns
pivot_table values index columns fillna reset_index
sum eq eq sum eq eq apply apply sum eq eq
groupby pivot_table index columns first columns columns reset_index
append
groupby axes keys axes plot
isin isin where where isin isin where where columns isin where
melt drop sort_values drop_duplicates merge assign drop sort_values assign notnull drop
align rank rank rank align align align count count align rank rank rank align align align
value_counts loc isin index
DataFrame get rename columns T T T T
DataFrame DataFrame columns applymap any iloc columns DataFrame DataFrame applymap any columns iloc columns
DataFrame explode value_counts reset_index rename index shape shape agg count DataFrame columns columns values count add values at insert insert insert rename all columns columns
DataFrame columns apply join append append append apply
values index
index loc loc groupby DataFrame
transform get
loc drop_duplicates
all where where
where where
isin isin
DataFrame DataFrame values groupby items DataFrame DataFrame items groupby append
DataFrame columns DataFrame columns loc
set_index index set_index unstack set_index unstack reset_index columns index values rename columns columns
drop_duplicates drop columns merge
where pivot_table index columns values first reset_index
DataFrame columns set_index iterrows loc loc sort_index
pivot_table index columns values sum fillna astype
to_records index
ndim
apply apply size loc apply
last
DataFrame columns pivot index columns values
pivot_table index columns sum size swaplevel sort_index
DataFrame diff loc
DataFrame to_csv
select_dtypes apply max min select_dtypes max min append
astype iloc iloc
set_index pop index pop index get pop index pop
groupby apply sum reset_index merge groupby apply astype cumsum reset_index drop loc merge
axes axes axes axes
groupby count reset_index sort_values
get rename index columns append dropna to_excel columns index
DataFrame to_json
groupby agg mean
astype
copy copy copy copy pop pop loc isin values insert
get groupby min groupby transform min sub add all stack dropna groupby nunique where gt
mask prod loc mask loc prod mul mask values mask mul prod loc mul mean std apply cumprod values mean std append product append prod items DataFrame from_dict index columns reset_index mean std
values columns values columns
loc mean std mean std values mean std values mean std
where astype bool columns shape shape iloc columns
set_index set_index fillna
DataFrame replace round index index index index
DataFrame info to_csv index replace info DataFrame info to_csv index replace info
columns columns columns fillna iloc columns shape fillna
replace columns fillna ffill columns fillna ffill columns columns
empty sample all sample loc loc
DataFrame items loc
nunique shape bool duplicated bool iloc all bool
loc index iloc columns
ge le notnull ge le notnull
clip shape clip astype shape shape
items items DataFrame items index columns
merge
count pop pop count DataFrame count DataFrame groupby index agg last max min append iloc append iloc append iloc DataFrame
melt groupby size reset_index
append DataFrame index
DataFrame index transpose get append append append append DataFrame columns
append
mask eq
keys DataFrame from_dict
count
pivot_table columns columns reset_index
shift
get values columns index
DataFrame columns DataFrame columns columns
columns
DataFrame columns sort_values drop_duplicates sort_index
loc
sample sample index set_index set_index resample resample mean resample mean join drop all values join dropna all
product DataFrame columns product product product DataFrame columns DataFrame columns product product product DataFrame columns product product product DataFrame columns DataFrame columns append mean product DataFrame columns product product product DataFrame columns DataFrame columns product append mean
DataFrame columns groupby index
set_index loc values
DataFrame plot
where fillna
DataFrame pop values index index stack reset_index drop join rename DataFrame values DataFrame
values values shape
loc duplicated isna T T T T T T T T T columns
set_index to_dict groupby join set_index to_dict groupby
DataFrame get index index values
shape max max DataFrame shape max DataFrame
compare append head to_excel index
shape DataFrame DataFrame T columns
explode explode dropna dropna
sort_values sort_values loc groupby agg count sum join sort_values
append DataFrame index
where
keys reset_index set_index groupby resample mean dropna
set_index fillna
loc values
isna
DataFrame index columns sort_index sort_index T copy
isnull isin join add_suffix apply
index
where where where
groupby apply max min
merge filter T groupby
explode index explode DataFrame values explode index join drop explode
shift fillna iterrows at
T apply columns groupby apply drop columns groupby apply columns
groupby
groupby transform max eq duplicated
loc index index
groupby agg first last join
size astype merge
join sum clip join
all min eq groupby min bool
values values values last values values values where index fillna groupby agg join rename_axis replace assign
astype
resample asfreq reindex min fillna astype
groupby apply eq any head reset_index drop filter bool bool
items
DataFrame DataFrame isin values values values
rename columns
stack unstack reset_index to_json
DataFrame assign
bool pivot index columns values fillna nunique groupby transform size drop_duplicates set_index
drop_duplicates items
apply
fillna add columns join groupby sum stack unstack rename columns add_prefix
columns all where equals
groupby ffill
columns astype dtypes
iloc
sort_index
DataFrame columns
pop join DataFrame index index
loc
groupby columns apply to_dict
loc
DataFrame size DataFrame T reset_index rename columns index melt DataFrame melt drop DataFrame drop columns first sort_values reindex reset_index drop
to_dict
pivot_table index columns max fillna sort_index pivot index columns fillna sort_index
T T
append append append
sample cumsum astype pop
value_counts index loc isin
merge set_index drop_duplicates groupby sum
min shift shift max shift shift sample xs xs append xs DataFrame xs columns min shift shift max shift shift index min index max plot
DataFrame
reset_index pivot index index columns ffill apply value_counts reindex T fillna astype join
align align merge columns melt
groupby columns sum rename DataFrame
apply groupby sum
columns columns columns apply
DataFrame sample sample sample assign replace groupby agg last max sample sample
where isin groupby replace keys groupby transform min where isin dropna all ne shift ne shift groupby cumsum
groupby apply min
query drop_duplicates
append DataFrame
append append append explode apply apply append append append explode apply apply drop columns
astype astype isin index loc
groupby agg min max groupby agg min max join apply min max rename columns min max groupby agg min max groupby agg min max join apply min max rename columns min max DataFrame min max values get groupby agg min max where min max min max min max max min astype rename columns min max rename columns min max
count query count query
groupby agg sum join first first groupby agg sum join
all
values items values values
append append append append append append append append append append stack reset_index drop apply
copy values
bfill iloc fillna count drop columns notnull sum columns columns
index size index index loc
apply get apply get
reset_index merge loc
columns columns columns plot columns columns columns columns plot columns
rename_axis columns reset_index rename_axis columns reset_index replace reset_index pivot_table index groupby transform join columns values index count rename_axis columns reset_index
get where apply groupby sum groupby sum
DataFrame max items keys append DataFrame keys drop_duplicates
iloc filter iloc
copy rolling mean
set_index apply loc
columns all
DataFrame DataFrame columns append
DataFrame rolling mean
merge add_suffix add_suffix diff where eq
columns
groupby agg join groupby apply apply reset_index columns rename columns
loc shape loc shape round
DataFrame DataFrame to_dict
columns isna any isnull any bool columns isnull any
reset_index groupby transform groupby transform min eq set_index append
sort_values groupby diff lt
DataFrame shift
pop pop iloc pop apply apply merge
assign groupby transform mean query iloc
assign groupby set_index reset_index
DataFrame columns columns replace dropna dropna astype empty copy merge
apply
apply astype pipe
pivot_table index columns max swaplevel sort_index groupby max unstack swaplevel sort_index
DataFrame sub
groupby agg first agg max sum sum sum sum merge copy groupby agg join apply groupby agg max sum join sum sum sum apply sub
mean
set_index bool max reset_index assign groupby first unstack reset_index rename_axis
loc select_dtypes columns
isna isna any bool
DataFrame columns join
DataFrame get reindex columns
DataFrame
loc isna loc loc fillna
replace ffill iloc fillna
iloc mask iloc eq bfill iloc
index update loc
get first filter filter
loc loc
drop apply index drop apply index
join sample append append first columns to_csv index
nlargest apply nlargest get
set_index columns clip loc loc
DataFrame columns assign at
apply
melt loc index drop reset_index drop
DataFrame iloc unstack droplevel rename_axis columns reset_index index
DataFrame isin isin
groupby max
loc
join all join DataFrame columns
columns columns
nlargest values nlargest reindex columns values combine apply values
mask loc mask loc mask loc mask loc mask to_excel index columns
set_index explode replace replace groupby index agg reset_index
append append DataFrame DataFrame
values DataFrame
append at
abs values
apply isnull abs
filter columns columns join
loc eq eq set_index at
get all values add append all
cumsum loc loc fillna groupby bfill astype
shape resample min first shape
groupby ne shift cumsum agg first sum
pivot sort_values drop drop pivot sort_values reindex index index columns columns
DataFrame DataFrame DataFrame index stack reset_index columns merge items loc astype groupby max to_dict where astype fillna drop drop_duplicates
values T count sum index count
where sort_values drop_duplicates first
isin eq shift eq get index values shift index items
groupby agg sum
groupby max query groupby max loc
sub shift gt diff lt gt assign sub shift fillna gt fillna diff lt gt assign
astype where shift iloc values
mask mask mask sort_index
agg apply agg
value_counts isin index
gt div ge dot columns
isin isin append replace replace
groupby mask eq ffill
copy drop drop groupby columns size reset_index count sort_values count
iloc values iloc values
loc
mask isna groupby first
loc loc
any values astype bool DataFrame size columns any values astype bool mean std values max astype bool mean std any values mean std values sum astype bool mean std
index all index values
columns
replace fillna append append
info items groupby sum rename_axis rename reset_index count
index DataFrame loc DataFrame index index iterrows append value_counts sum append sum fillna astype
apply apply all
loc mask
apply
loc
add replace
notnull
groupby sum reset_index drop
replace explode drop_duplicates first index replace
shape
sort_values unstack astype agg sort_values astype agg reindex unstack astype agg sort_values index mean std sort_values unstack astype agg mean std sort_values astype agg mean std
DataFrame apply values
where
groupby mean sort_values plot
add append append where DataFrame
DataFrame index columns DataFrame index columns DataFrame index columns stack stack stack keys
loc any
iloc
index add
assign sort_values
DataFrame size columns to_csv info DataFrame columns columns dtypes iloc
fillna fillna loc fillna loc
groupby head count values iterrows loc
groupby mean reset_index
DataFrame index apply
apply index columns apply
join columns apply join columns apply
DataFrame columns columns DataFrame iloc iloc pivot index columns values
index columns index columns index columns groupby index columns
groupby mean DataFrame
apply
iterrows
replace le
max apply astype info DataFrame columns columns dtypes size apply info DataFrame columns columns dtypes
melt index merge drop index drop pivot_table index index columns values reset_index
any isna loc index isna dropna all index loc isna expanding mean loc
DataFrame DataFrame
DataFrame explode values assign reset_index drop
plot
product groupby first agg groupby transform get product product groupby first agg groupby transform sum get product
empty dropna all dropna all to_excel index
append filter join
applymap
mean DataFrame
astype astype astype astype
