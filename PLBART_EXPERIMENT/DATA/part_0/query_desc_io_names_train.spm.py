loc mask bfill fillna
DataFrame iterrows abs get get items abs DataFrame DataFrame columns abs astype astype count groupby count sum get count reset_index iterrows count abs count count T count drop count
mask eq stack reset_index groupby agg join
DataFrame columns mean std astype DataFrame columns mean std
DataFrame empty shape eq eq astype DataFrame copy equals copy copy copy shift loc loc cumsum loc take items copy loc cumsum merge merge fillna astype drop empty shape eq eq astype
columns
style groupby agg size unstack plot
groupby agg sum count assign
plot plot hist
transform transform apply columns
DataFrame
groupby count to_dict apply values
DataFrame apply apply apply
where shift shift shift
columns
var var
groupby apply reset_index apply groupby apply join reset_index
iloc iloc
assign where eq pivot_table index mean mean
groupby agg all reset_index
columns columns
groupby apply nunique values DataFrame groupby apply nunique values
mean mean mean index size size size size DataFrame T columns index
groupby diff div round mul
style applymap style apply isnull any shape
any
items add replace mask eq
groupby transform count loc dropna drop
apply
lookup index
groupby agg min max size assign value_counts
sum set_index sum set_index sum at set_index sum set_index sum loc set_index loc sum sum set_index sum at sum set_index iloc sum set_index sum iat set_index sum iloc
melt astype set_index drop columns resample ffill reset_index
keys keys keys
DataFrame keys keys keys keys keys keys append DataFrame
DataFrame values columns
sub join sub join
cumsum ne shift cumsum groupby cumsum agg first first count columns columns droplevel values reset_index drop
mean diff mean diff mean diff groupby apply apply
DataFrame loc loc apply
groupby sum groupby agg sum unstack groupby get sum get sum fillna groupby index cumsum
DataFrame where
groupby get all keys
groupby agg mean join
columns
all all append all groupby sum groupby sum reset_index
all items first items last items last items iloc all items iloc first items iloc last items iloc last items iloc all items iloc first items iloc last items iloc last items
columns mean groupby transform mean fillna mean
loc loc
sum astype unstack sum drop sum astype sum drop
std mean std mean sort_values std mean sort_values
index drop_duplicates first copy merge apply
itertuples append
iloc iloc iloc iloc
get append DataFrame head
replace fillna
apply last_valid_index eq loc apply last_valid_index eq loc
loc
cumsum eq first bool
astype
squeeze keys all keys groupby
all values index
groupby apply value_counts reset_index
columns columns astype columns columns astype rename columns rename columns
DataFrame from_records loc columns columns DataFrame from_records loc columns columns keys unstack
iloc ne all iloc eq any iloc ne all bool ne all
groupby transform last
apply join DataFrame columns
where lt all idxmax
get all join join join DataFrame append empty append
groupby apply shift fillna cumsum astype
loc mask head loc loc loc loc isna loc loc loc loc isna loc
add add
ne shift lt shift groupby transform any
itertuples itertuples index
merge merge
astype radd
DataFrame
where isin
to_dict groupby apply apply columns
merge add le astype add gt astype
loc sample index fillna
first first first append
items items get DataFrame drop
melt rename columns
max eq sum values max sum
loc fillna groupby mean
groupby transform nunique gt where transform first assign where transform first
append add to_csv
DataFrame loc head iloc head iloc head iloc groupby apply droplevel
abs min diff abs diff diff min abs loc idxmin astype apply
min
groupby ffill groupby bfill
apply
groupby transform sub shift fillna abs
DataFrame DataFrame values columns apply
melt sum
sort_values diff drop_duplicates last sort_index diff
DataFrame columns iloc iloc
add_prefix append
loc loc min max min
pivot index columns values reset_index rename_axis
min notnull loc loc interpolate
set_index append unstack reset_index droplevel
replace replace pipe
DataFrame columns DataFrame columns append
sort_index shift resample first
astype radd columns T replace T T astype radd columns T replace to_dict index T
DataFrame columns DataFrame columns values values append
rename columns
astype
fillna rename columns
to_excel index
DataFrame
DataFrame at DataFrame from_dict columns index
set_index loc
DataFrame sample to_excel index
columns append DataFrame from_dict
abs abs eq all abs
DataFrame
DataFrame filter copy append join apply
isna isna isna cumsum isna groupby sum max apply max
explode join index join values fillna index
head tail columns values reset_index drop
info info
reset_index reset_index
DataFrame index at at at at at at at at at at at at at at
get all DataFrame keys DataFrame DataFrame DataFrame
rank groupby apply values pivot rank fillna columns columns swaplevel sort_index columns columns reset_index
where notna
melt info query drop info
count shape shape sum max sum count shape shape abs abs shape sum max sum
DataFrame columns join apply join
first_valid_index last_valid_index
DataFrame columns
dtypes equals dtypes dtypes equals dtypes dtypes equals dtypes
DataFrame columns append DataFrame columns
ffill iloc iloc ffill iloc apply eq isna isna
columns
empty all values values iloc first last append iloc append iloc append iloc append iloc append iloc append iloc last DataFrame DataFrame
apply
DataFrame
isin astype isin
pivot_table columns index values count reindex
index index index index diff mean index diff iloc
values loc append count DataFrame from_dict index reset_index rename columns columns
DataFrame eq eq
apply min
replace any apply explode explode drop_duplicates
drop_duplicates first
columns
merge groupby ffill
where append append plot
groupby sum drop
DataFrame rename columns to_dict DataFrame rename columns to_dict append min min
pivot index columns dropna all drop empty columns interpolate interpolate columns columns
DataFrame columns all get iterrows values index append append index drop
loc any
eq astype groupby rolling sum drop
columns
groupby transform where
get get DataFrame from_dict drop
groupby merge assign drop_duplicates groupby size
groupby groupby
apply apply
DataFrame values values applymap add assign applymap
sub DataFrame columns first all columns agg first columns get all agg agg columns assign rename drop any groupby agg agg reset_index rename columns assign apply apply apply apply apply apply apply drop to_string index apply groupby agg agg reset_index rename columns assign drop
T unstack T reset_index sort_values values drop values DataFrame values columns columns columns
DataFrame groupby DataFrame index index append append
columns loc get loc reset_index drop loc reset_index drop loc fillna reset_index drop loc
DataFrame index drop
DataFrame replace
query append append append DataFrame query query head query plot append query append append append DataFrame query query to_html
index values index values values
min iterrows values groupby count loc reset_index rename columns set_index append mean min
ge ge ge ge groupby transform all ge ge groupby transform all
groupby apply join groupby apply join
loc loc
loc apply abs mean std
transpose columns append columns
DataFrame iloc ne mask loc index iloc ne mask loc index columns columns melt merge melt
DataFrame DataFrame apply iteritems columns
gt sum gt sum sort_values
dropna all
insert index
astype mod where astype
pivot_table values columns index sum columns columns join to_csv
index get columns loc replace append drop
mean std mean mean std std
DataFrame style applymap get style applymap get style applymap get applymap
reset_index
DataFrame columns DataFrame DataFrame index DataFrame style align
loc index values assign isin keys isin assign
get append
DataFrame astype
duplicated groupby transform nunique groupby filter nunique
replace DataFrame replace
groupby transform min transform
append join groupby values eval
where isin
merge groupby max reset_index
sort_index
mask isin mask append any
items sum items join eval
DataFrame DataFrame drop columns
columns astype groupby mean
get div append DataFrame to_csv append size size style style size any all any all
astype astype
DataFrame groupby agg reset_index groupby agg reset_index
size DataFrame groupby sum
DataFrame columns sample
loc index assign groupby reset_index drop
ne shift bfill groupby cumsum add ne shift any groupby cumsum ne shift ne shift groupby cumsum
index isin isin bool isin bool
sum
between_time groupby index idxmax stack unstack loc between_time index between_time index groupby index idxmax stack unstack
isnull iloc isna iloc
DataFrame iloc pop
astype sort_values groupby tail drop index
DataFrame plot DataFrame update plot DataFrame plot
merge index drop_duplicates dropna drop rename fillna
iloc astype dtypes
pivot_table index columns values join
join join
isin any bool index isin any bool loc values
keys
fillna columns dtypes astype
round round round loc index values
index append append join join apply
reset_index DataFrame index columns merge
pivot_table index columns values mean
iloc groupby
merge
groupby count reset_index count count groupby count transform max
DataFrame apply max
sample DataFrame columns empty columns assign items empty hist DataFrame hist set_index columns columns index assign empty get first
join pop join
where abs gt
duplicated
columns
DataFrame from_records columns loc astype astype groupby agg reset_index
add_prefix add_prefix drop rename columns merge
apply where
sum to_csv
abs stack stack iloc head reset_index columns
insert insert loc to_csv index loc to_csv index
count where keys values get keys items rename columns pop get keys items combine append append DataFrame
items items append append DataFrame columns
apply DataFrame copy apply apply
columns size columns size
isna loc isna pivot pivot_table index columns values count fillna
values
product groupby itertuples index DataFrame product columns append
lt gt gt sum sum aggregate
bfill ffill drop_duplicates groupby first reset_index
pivot_table index columns values columns reset_index
join replace
replace add
get append DataFrame to_csv index
groupby index agg mean std
apply
groupby apply bfill ffill values values groupby values apply bfill ffill groupby apply values bfill ffill
astype
assign explode dropna set_index astype
groupby transform sum div mul round
DataFrame DataFrame DataFrame merge drop columns rename columns merge drop columns rename columns
values values values dot
groupby sum reset_index rename columns
iloc get iloc get loc loc loc set_index loc
set_index stack swaplevel rename items items items reindex sum apply mean std copy explode set_index lookup groupby sum mean std set_index T assign set_index set_index assign mul sum reset_index mean std
reset_index index groupby sum
merge drop
isna values any any isna values any all
isin values T astype values join any
DataFrame columns iloc groupby
groupby sum loc
apply
groupby transform
DataFrame DataFrame DataFrame isin all dropna all dropna all
loc
DataFrame columns DataFrame apply items columns
apply loc apply loc
DataFrame DataFrame merge columns isna sum sum
index index columns
pop replace get update columns columns columns columns ffill set_index rename_axis
notna notna notna bool
sub sub dropna groupby apply apply to_dict items get groupby items groupby append DataFrame columns apply apply
loc loc iloc astype loc shift cumprod head
DataFrame drop join stack reset_index drop rename
round
at drop drop index at drop drop index
loc isin apply
index iterrows index iterrows
transform aggregate drop truncate
equals DataFrame index index loc index compare set_index append shape compare drop compare rename index append
astype nlargest join
DataFrame explode index
mask items mask mask loc mask
any all shape prod shape prod
loc isin items query items where isin items
iloc columns
DataFrame
DataFrame first transform shape DataFrame columns
query groupby transform rank groupby transform bfill query groupby last query groupby last keys
values index index sort_values
DataFrame
explode reset_index drop
groupby sum reset_index sort_values reset_index drop
replace
DataFrame merge join apply merge
merge loc reset_index loc reset_index dropna query drop columns
where fillna
groupby cumsum
index values loc items
join
reset_index groupby index agg min max rename columns min max reset_index rename columns shift cumsum columns reset_index groupby agg min max rename columns min max reset_index rename columns
DataFrame columns get rank groupby rank rank
apply last_valid_index fillna count bool
merge
merge
empty groupby
sample index columns index columns
bool DataFrame query where get keys keys get append DataFrame to_csv index DataFrame to_csv index
DataFrame size columns columns
set_index first groupby first le first shift mask groupby last shift first reset_index bool
apply
DataFrame columns applymap bool
DataFrame pop fillna add_prefix index info
set_index explode loc index groupby items set_index explode index groupby items loc to_csv explode index set_index pipe explode
groupby max
DataFrame explode apply
index index
where fillna
shift loc
reset_index drop_duplicates first set_index
copy values where values sum sum sum where values values sum sum clip values sum values mask values sum sum
groupby
DataFrame columns DataFrame columns where
DataFrame size columns iloc ne iloc dot add_suffix columns mean std where columns DataFrame squeeze stack groupby agg join mean std iloc apply columns iloc values mean std
iterrows loc loc loc loc index notna notna notna any notna loc notna index loc notna index loc index
drop_duplicates pivot pivot_table index columns values first
assign astype astype merge stack
DataFrame groupby
DataFrame apply columns loc loc astype
groupby where values iloc groupby apply
select_dtypes columns apply dtypes dtypes loc loc apply
copy copy
append join set_index rename columns index to_csv join
loc notna astype
assign groupby set_index unstack sort_values reindex columns join columns
apply join apply
sort_values groupby
drop drop groupby sum reset_index columns columns reset_index transpose get index T first iloc first take columns columns iloc reset_index sort_values columns add columns index values
merge
update update DataFrame
set_index set_index index isin index index isin index loc loc
items join get where
iterrows append iloc min set_index loc
melt
count gt sum where groupby mask gt groupby transform all index index assign mask filter mask pipe
mean std stack groupby index count count groupby index nunique count
mask groupby transform mask mask values nunique count nunique gt mask mask mask mask values groupby groupby transform nunique count gt at
applymap
append DataFrame append DataFrame
DataFrame apply apply get apply apply
fillna fillna
iterrows groupby sum
agg groupby sum agg agg groupby agg sum first last agg
groupby first unstack diff iloc reindex astype lt values
isnull isnull apply
set_index apply stack astype reset_index drop rename columns
isin values
columns columns columns columns columns columns columns columns columns columns
min DataFrame keys reset_index drop columns groupby loc eq loc eq sum append loc eq
loc get loc items items items replace empty apply apply DataFrame pop keys index add_prefix DataFrame pop keys index add_prefix join join reset_index drop append
items groupby columns sum
isin isin
index loc loc clip
items index iterrows items abs abs append sum append append
astype bool bool
isna cumsum sort_values drop_duplicates sort_index
where update loc loc update items items loc
pivot_table index values
groupby
append DataFrame
groupby first DataFrame lookup notna idxmax columns columns columns groupby set_index stack groupby head unstack
merge columns reindex columns
count count append loc
sort_values groupby query agg query groupby agg
groupby agg mean reset_index query sort_values query groupby agg mean reset_index loc
apply
where astype
empty DataFrame
DataFrame columns astype loc astype loc
index dropna reindex
set_index groupby astype DataFrame shape shape shape columns index index index reset_index melt index index replace eq replace lookup index shape lookup shape pivot index rename_axis index columns reindex index index columns index index index replace columns columns replace
groupby agg values append DataFrame index columns fillna groupby sum gt astype values
groupby transform sum transform sum round set_index
groupby groupby agg sum plot plot
apply
loc isnull all loc isin mask isnull all loc mask
merge drop drop rename columns groupby agg first reset_index drop groupby agg reset_index apply drop join fillna
xs sum equals xs sum xs xs apply
shape shape merge drop shape
where apply astype
groupby keys values apply value_counts index keys values merge reset_index fillna
items replace items
all plot min max columns columns apply replace replace get apply explode astype reset_index drop add plot values max values max values max values max values max values max add plot
append get
loc index loc astype loc
replace apply loc explode drop replace apply loc
DataFrame DataFrame join apply explode set_index apply explode set_index append unstack drop reset_index rename_axis columns
loc eq eq eq groupby apply sum values astype
groupby apply reset_index index drop columns pivot columns
query query items groupby transform first
fillna columns columns fillna
sort_values groupby transform last div transform sum
DataFrame stack dropna reset_index drop DataFrame index astype dtypes
append DataFrame
DataFrame shape shape index index columns columns
where assign assign where loc
columns append add to_csv index
values squeeze values squeeze DataFrame
isnull isnull isnull
DataFrame loc set_index unstack droplevel values reset_index add index index
apply apply apply
groupby get bool transform index at index first index head index get last bool transform tail index tail index loc all index loc bool
DataFrame columns
groupby pivot_table index columns values reset_index
join apply
DataFrame index index
groupby agg items groupby agg
loc any query DataFrame
DataFrame duplicated sum loc duplicated sum
bool bool
drop_duplicates last groupby agg reset_index
where isin columns
apply
DataFrame columns
items set_index index index groupby index index sum columns
shift pct_change
DataFrame
where
apply isin apply bool
first all add loc groupby cumsum bfill groupby loc groupby add loc drop drop
apply
sample DataFrame
append append append groupby
nlargest groupby nlargest reset_index values nsmallest groupby nsmallest reset_index values iloc nlargest iloc nsmallest sort_index
add
groupby apply sum sum
T values
astype astype astype
duplicated duplicated duplicated
groupby isin size rename index
groupby head sort_values reindex iteritems
T DataFrame index columns
join filter values
DataFrame columns loc columns loc columns head head
set_index stack reset_index rename columns ne shift diff diff gt diff lt diff eq isnull groupby first
columns groupby sum
columns dtypes append
ne sum gt any groupby cumsum loc shift fillna sort_values mask duplicated first gt any groupby cumsum loc shift loc mask
eq any astype eq any any
groupby apply join transform first
merge loc abs groupby idxmax loc
groupby append DataFrame columns
DataFrame columns columns columns dropna
groupby apply reset_index groupby agg groupby agg columns
set_index index index apply dtypes DataFrame columns set_index index index astype astype apply dtypes
interpolate abs sum interpolate corr
DataFrame T
bool drop drop index columns drop
eq groupby cumsum transform count idxmax
sort_index reset_index drop
DataFrame from_dict columns head
merge drop
DataFrame DataFrame isin merge columns merge plot plot
mean median groupby agg rename_axis reset_index columns columns
diff eq index apply cumsum apply drop
DataFrame groupby mean groupby mean
apply columns apply get drop columns
get append DataFrame to_csv index
columns iloc ffill iloc values
apply DataFrame get get iloc reindex index reset_index drop columns
melt
merge merge merge
apply index
to_csv index columns
get
add
loc values loc iat values isin loc loc loc loc loc
rename_axis reset_index rename_axis reset_index DataFrame keys values keys DataFrame keys values mean std rename_axis reset_index mean std rename_axis reset_index mean std DataFrame keys values mean std DataFrame items columns mean std
groupby pivot plot
columns dropna dropna columns dropna dropna columns dropna dropna
items to_json
dtypes groupby sum
groupby apply DataFrame values T columns index rename_axis reindex
join set_index
append transpose values join
DataFrame values columns groupby size rename count reset_index
value_counts groupby value_counts
columns replace
mask mask
any loc value_counts loc values mode join
any
set_index where rename_axis stack reset_index mask drop_duplicates drop
groupby product groupby product
transform isna any
index where index index index index where index index index
apply isnull replace
groupby transform min eq ne groupby transform idxmin eq index ne
sum to_records to_records
shift shift shift
get DataFrame DataFrame isin copy astype groupby add mod ne groupby agg join reset_index
fillna append
value_counts
set_index
apply apply join
to_html style align
items join
DataFrame columns groupby max apply max reset_index drop get shape drop sort_index
replace astype sum reindex index
DataFrame columns drop merge
index index index index index values min index values index values max index values index values
append columns merge
T filter
empty DataFrame columns set_index replace astype apply append append to_excel index
assign eq cumsum eq cumsum mask eq cumsum ne cumsum mask groupby cumsum head head groupby cumsum
DataFrame
groupby agg groupby agg reset_index groupby agg groupby agg
astype values astype astype bool
set_index columns iloc set_index columns iloc add reset_index columns
iloc iloc
DataFrame add groupby all groupby add loc add loc add loc groupby apply fillna ffill fillna all all all drop add all round
apply sum apply
groupby value_counts groupby DataFrame DataFrame DataFrame value_counts groupby apply
groupby apply sample value_counts min
reset_index columns boxplot
sum append
pivot_table index columns values reset_index index columns
DataFrame columns index
set_index stack groupby add astype reset_index drop set_index append unstack columns columns columns merge
filter columns fillna assign pipe fillna filter
attrs attrs append DataFrame
rename columns rename columns rename columns
DataFrame columns to_dict items
values
fillna drop columns fillna apply
DataFrame from_dict values
DataFrame index plot DataFrame index plot
DataFrame columns
iloc iloc
astype shift cumsum groupby apply iloc min max reset_index drop
loc apply
copy index loc values append mean mean std std DataFrame index copy
axes index loc axes axes
mask loc mask
shape shape
apply apply apply apply
replace join replace replace mask apply values loc
explode index explode DataFrame values explode index join drop explode
join
astype fillna apply join astype fillna add sum astype fillna astype fillna astype
replace DataFrame columns replace replace
merge dropna sort_values
apply iloc apply take apply take apply take
values ne values ne
pivot_table index columns columns columns sort_index
DataFrame columns values values
DataFrame DataFrame isin
groupby ffill bfill
apply astype drop_duplicates set_index apply astype drop_duplicates set_index
append append append
iloc any bool
count fillna sum
all where loc all where loc loc all append loc
append to_excel index
columns DataFrame applymap
index iterrows index iterrows DataFrame from_dict DataFrame from_dict DataFrame from_dict DataFrame from_dict drop drop_duplicates drop_duplicates drop_duplicates drop_duplicates count index index index index
set_index resample T count
sample
index join join rename
groupby agg count sum groupby count nlargest reset_index drop groupby count sum sort_values index index sort_values drop count
axes transform
count items
values index
groupby count reset_index count to_excel
eq loc loc set_index set_index where eq
where isin
astype groupby
apply
reset_index drop reset_index drop
all columns select_dtypes all columns set_index all select_dtypes sum all columns sum sum sum
DataFrame
DataFrame
DataFrame values astype T loc values
where eq groupby ne cumsum transform first ffill
apply replace apply
max min iloc max iloc min
DataFrame index DataFrame index applymap to_excel
loc loc DataFrame where
DataFrame sample loc loc
groupby agg first sum reset_index
update merge
columns
index any any bool
DataFrame columns iterrows
DataFrame replace at at
set_index fillna set_index reset_index
index astype index index columns index astype mean std index mean std index mean std
groupby plot
groupby apply DataFrame to_dict
DataFrame index columns copy
set_index groupby plot
to_csv index to_csv mode index
DataFrame columns apply replace apply replace apply replace
index DataFrame index index reset_index
sort_index sort_index sort_index sort_index merge merge eq merge eq values all merge equals sort_index sort_values columns reset_index drop sort_index sort_values columns reset_index drop values all
first DataFrame index columns index index index
DataFrame plot
isna duplicated
pad
isna loc isna loc isna apply
apply
resample mean shift resample mean shift resample mean shift resample mean shift
loc astype astype astype astype
merge merge
merge groupby agg reset_index astype astype merge groupby agg reset_index
DataFrame stack
loc sort_values index
join set_index loc
iloc columns replace
ne ne all bool
values apply isnull loc loc
DataFrame
assign astype items values astype items items astype
columns columns mask loc loc mask iloc first loc mask shift loc mask values columns columns mask loc loc mask iloc loc mask shift loc mask values mask loc loc loc mask loc mask values
set_index pivot_table values last index columns pct_change set_index pivot_table values last index columns columns set_index pivot_table values last index columns columns
groupby mean resample mean set_index groupby mean resample resample mean index index groupby mean resample resample mean set_index values apply groupby mean resample resample mean
DataFrame columns plot
explode index explode DataFrame values explode index join drop explode
index shift loc loc loc
set_index drop eq rolling agg any astype rolling agg any astype eq reset_index drop
pivot set_index combine_first index combine_first
sort_values sort_values dropna
drop drop
melt pivot_table index columns values reset_index
isin loc isin loc
where where
DataFrame iloc iloc iloc append
clip
DataFrame from_records columns set_index loc DataFrame from_records columns set_index loc
set_index shift
groupby apply loc loc index loc
pivot index columns values
lookup values values DataFrame shape columns columns lookup DataFrame values lookup lookup values index columns columns values lookup values values DataFrame shape columns columns
apply join notnull groupby transform join dropna drop columns apply join notnull
index index index all at take mean loc index index mean index index index at index index cumsum get mean groupby mean
DataFrame values apply values
stack unstack apply product DataFrame itertuples product columns DataFrame
DataFrame columns merge
apply apply take apply apply
DataFrame index to_csv index DataFrame DataFrame index append to_csv index
DataFrame index DataFrame index index values index values index index loc loc DataFrame DataFrame
join replace
merge fillna join fillna set_index set_index fillna
replace
apply T to_excel astype index
columns columns mask fillna
sum sum apply
index head tail DataFrame columns columns columns head append append tail
DataFrame
assign add
groupby transform sum eq
apply
all empty add
sort_values head style
DataFrame apply apply
reset_index drop idxmax loc groupby apply sort_index reset_index
isna isna isna isna isna isna drop_duplicates drop columns
iloc sum astype iloc mask sum astype
reset_index drop reset_index drop join
compare columns columns columns columns index index index index columns align values align where ne columns index DataFrame iat iat columns compare compare align where ne columns index iat iat notna notna append DataFrame columns
index set_index get where equals get loc loc eq
query query query join query to_csv index
to_html columns values index
size unstack DataFrame index columns columns droplevel columns index index index shape empty values reindex index columns
add
index loc index loc index loc index loc index
join DataFrame all drop columns columns DataFrame
apply value_counts notna index all
groupby
shift shift get columns columns columns columns empty where where iloc iloc iloc iloc add iloc
replace replace filter filter columns ffill iloc last isnull all all apply equals equals
items DataFrame append
melt
shift all index index values columns shift columns columns shift plot
filter dot columns
dtypes astype describe describe
DataFrame notnull
melt merge
DataFrame join astype bool
product DataFrame columns sub sub merge sub sub sub drop columns sub groupby apply reset_index
rolling apply iloc astype
replace
values fillna mean values fillna mean mean std describe
DataFrame sample sort_index
DataFrame assign query count query query
columns columns
DataFrame max append filter loc plot
select_dtypes bool ne shift any cumsum columns first sum groupby agg
apply dropna stack stack stack any index stack any
any get get
sum add sum append iloc groupby apply columns append
append
where
items
apply
where index isin
DataFrame to_markdown
DataFrame to_csv index
loc
iterrows columns take last columns columns values get all get iloc iloc iloc get values append iloc iloc append iloc iloc iloc iloc iloc append iloc iloc iloc iloc iloc append iloc iloc iloc iloc iloc append iloc iloc iloc iloc iloc append iloc iloc iloc iloc iloc append add at
DataFrame index fillna
iat iat mean mean
groupby size groupby head index
DataFrame prod isin values astype bool
DataFrame DataFrame merge reset_index index isin index
merge
DataFrame columns explode
groupby astype pivot index columns values rename_axis
groupby apply DataFrame values index index
DataFrame DataFrame DataFrame append
DataFrame groupby sum reset_index pivot index columns
eq eq eq eq join join
max take empty max empty values T T T
pivot_table index columns values max swaplevel sort_index groupby max unstack swaplevel sort_index
DataFrame index loc loc
sample DataFrame columns loc merge merge
columns max
melt sort_values melt filter columns sort_values
columns columns iloc iloc
reindex
DataFrame
size size size shape shape T
loc
set_index rename columns
loc where at
clip clip
to_dict items items get append
apply columns
iloc reset_index drop
mul
DataFrame index index astype
values values values cumsum
loc apply
index
where eq drop_duplicates drop
DataFrame shape shape
columns first append join
index index index index
values values squeeze squeeze squeeze DataFrame size columns set_index groupby index apply groupby
diff fillna ne any bool
replace replace groupby sum
DataFrame sort_values groupby shift
dropna
DataFrame count groupby apply
DataFrame loc index sum sum sum sum groupby agg reset_index
groupby first
append join columns values
DataFrame columns
set_index reindex reset_index
DataFrame
apply
first diff copy copy rolling mean rolling mean abs rolling mean rolling std get get DataFrame columns columns assign columns astype last iloc last last last last
product product DataFrame product DataFrame columns
columns columns apply assign groupby cumprod mul add_prefix
sample sample
groupby count
where isna
idxmax apply apply idxmax apply first_valid_index
apply apply apply
all
index values loc loc loc DataFrame index
shape iloc iloc iloc iloc iterrows
columns index loc
DataFrame query DataFrame query DataFrame query DataFrame query DataFrame query DataFrame query DataFrame query DataFrame query
columns keys columns columns join sum to_dict
DataFrame columns max idxmax
index
append
index index
set_index dot columns drop reset_index set_index dot select_dtypes bool columns reset_index
astype sum T apply sum T
join replace
assign lt quantile assign lt loc quantile
pivot_table values index columns max where groupby rank dropna all groupby rank any any pivot_table values index columns max where groupby rank dropna all groupby rank any
DataFrame groupby agg sum reset_index plot index
loc loc transform
apply set_index
get DataFrame columns to_csv index
DataFrame from_dict cumsum fillna
replace merge
eq groupby shift set_index to_dict last
DataFrame columns
index iloc
replace replace
append append append append append append append keys values append join values append all iterrows append append append append append append append size size append append append DataFrame from_dict duplicated DataFrame groupby apply values columns fillna
set_index reindex sort_index reset_index pivot_table index columns reindex stack reset_index
loc iterrows values loc
assign items
align columns copy columns columns merge align align
dropna
loc loc iloc diff loc iloc T diff index diff
merge groupby size reset_index
iloc iloc apply
stack value_counts
loc groupby loc groupby join dropna
apply
cumsum DataFrame apply join apply join apply join
drop index
groupby sum plot
join
loc abs mean std
index
keys rename_axis columns
groupby groupby
where isin isin
merge
eq set_index assign le astype pivot_table index columns values max add_prefix join reset_index
where loc index loc min
groupby agg min max sum
groupby apply sort_index groupby apply head sort_index
DataFrame index DataFrame index
add reindex index
melt melt
DataFrame all apply apply get all apply
sort_values groupby first reset_index
DataFrame size columns DataFrame index index columns shape iloc astype cumsum DataFrame clip
groupby apply value_counts index
groupby columns values
assign explode update astype
apply squeeze merge apply squeeze
index DataFrame index index reset_index index
DataFrame join reset_index set_index rename columns notnull loc apply replace
loc sum
groupby apply
set_index index set_index sub
loc columns loc columns plot plot index index DataFrame first DataFrame index plot plot plot index index index plot plot plot columns index index append index values size shape size shape size shape size size append append values values size shape size shape size shape size shape size size size size add shape shape add add add DataFrame from_records to_csv index plot plot plot
stack reset_index columns index columns columns values DataFrame rename_axis reset_index melt reindex columns columns columns index values T DataFrame
set_index index reindex assign groupby ffill bfill reset_index
ne shift eq shift ne groupby cumsum transform first sub shift where where mul
loc
DataFrame columns DataFrame columns loc loc loc applymap
values to_dict drop groupby values count count count count values count count count count values count count count count
groupby astype apply join apply to_dict
replace stack columns shift columns shift all dropna reset_index drop
isna all all isna get all all
DataFrame DataFrame
empty DataFrame copy DataFrame iloc
nlargest columns
diff
DataFrame index index columns
sample
get keys
index reindex columns
groupby mean shift rename merge
DataFrame index columns iloc iloc iloc copy iloc iloc DataFrame index columns iloc copy iloc copy iloc iloc
join reset_index pivot_table index index columns values reindex add_prefix
dtypes
groupby size unstack join where groupby transform sum groupby transform sum where
max values mask isin update mask shape loc iloc values append update add append index loc iloc iloc iloc
isna isna apply
shift lt
replace dropna
index
shift diff diff shift
get columns at mask keys eq assign mask keys mask rename columns rename columns max filter mask any columns index get columns set_index columns values columns columns fillna fillna replace columns values loc where values replace columns rename columns columns first cumsum columns columns columns stack columns stack rename columns reset_index drop sub values fillna fillna fillna astype drop apply replace notna loc apply replace fillna fillna drop
DataFrame columns to_excel
all values append
melt values
iloc where values
loc value_counts head loc nlargest
isin items isin items
stack groupby value_counts unstack add_prefix
values apply bool
index astype set_index
copy loc loc copy
columns rename columns
get append join
merge
sub all first all
eval append items mode mode mode
duplicated cumsum groupby last
groupby apply min max
copy
groupby
get values values DataFrame from_dict index T items values append merge values keys values values items iloc
nunique
mask isin eq groupby transform any eq groupby transform any mask
loc loc
groupby sum where groupby sum values index
apply astype
shift fillna max cumsum groupby transform max min cumsum
index index index append resample ffill
set_index rename index join index
DataFrame first query DataFrame query equals
max
idxmax loc max loc max at
isin groupby mean
merge fillna drop
groupby columns size agg sum sum join size rename index append append keys
groupby apply join reset_index rename
DataFrame apply
mean abs values values values
reindex columns columns
size
groupby groupby sample groupby merge
DataFrame DataFrame set_index drop groupby count index index index values
DataFrame DataFrame merge merge sample apply apply merge merge drop
get append DataFrame columns
DataFrame columns to_csv index join
applymap DataFrame applymap applymap
replace
apply
empty empty DataFrame T merge merge copy merge columns max index
combine all groupby apply combine value_counts index values DataFrame columns join reset_index drop
append replace replace DataFrame columns
DataFrame columns apply items
DataFrame iterrows drop index index DataFrame columns append reset_index drop to_string
DataFrame head to_csv
index columns DataFrame index index columns columns fillna
apply apply agg join
DataFrame DataFrame add T size DataFrame columns size index size loc loc size size loc
T reset_index rename columns index apply join count
where
shift shift loc
to_csv mode to_csv mode
rank DataFrame rank rank DataFrame groupby rank min merge rank groupby transform eq any rank rank
isin
DataFrame columns
append DataFrame T
columns iloc iloc replace
melt T groupby T size groupby apply sum
get DataFrame eval
replace
reset_index count mul count groupby sum div count reset_index reset_index count groupby apply count reset_index
merge DataFrame values columns index index index
values last add at last index index
value_counts value_counts DataFrame columns apply loc drop
set_index
merge query value_counts
transpose iterrows rename columns first rename columns rename columns
DataFrame columns T to_csv mode index to_csv mode index
astype groupby columns agg apply join astype groupby columns agg join
apply mean
reset_index reset_index iterrows drop index drop index
fillna ne fillna ne any bool
DataFrame value_counts value_counts groupby agg count groupby agg sum groupby agg count groupby agg sum reindex index index plot index
drop_duplicates add
astype
reindex fillna
merge merge
groupby
apply any
assign set_index reindex ffill reset_index
where last where first
eq shift eq shift eq shift eq
DataFrame
astype iterrows loc loc
filter apply filter dtypes filter columns dtypes dtypes filter apply dtypes dtypes
to_excel to_excel
append append T DataFrame index index to_csv
DataFrame columns loc keys columns keys values
diff sum
DataFrame shift add values apply
loc groupby mean reset_index loc groupby mean
groupby plot
columns index index columns index index index index columns columns first columns last columns copy columns append append last insert insert
apply
DataFrame to_excel index
astype
values groupby apply reset_index drop
groupby all
loc index loc index
groupby transform last groupby transform first mask duplicated last
shift shift
DataFrame explode value_counts
rolling rolling sum values rolling rolling sum values shift
iloc
xs columns
DataFrame
astype shift cumsum astype
mean mean loc index mean hist
bool value_counts dropna
DataFrame apply
loc index iloc iloc
DataFrame groupby head rename columns groupby tail rename columns merge
apply apply head get loc loc loc loc reset_index loc loc loc loc
set_index loc index max loc index min groupby agg
join value_counts fillna
mul unstack stack reindex index mul unstack stack reindex index
loc shift shift cumsum ffill groupby resample mean groupby apply interpolate reset_index drop
iterrows append abs
ne shift cumsum
ne shift cumsum duplicated
loc
apply any
query
merge values DataFrame columns count count count astype merge assign count count sum fillna values
columns columns replace columns columns replace
DataFrame DataFrame DataFrame values dtypes stack reset_index drop astype groupby apply squeeze values columns le columns le
DataFrame columns max DataFrame columns iteritems at at loc apply
head head
columns add shape explode reset_index drop
set_index drop sum reindex index
merge
groupby agg apply bool
iterrows
apply join fillna
index index sort_index
groupby add astype
DataFrame dropna append
pivot fillna pivot combine_first
DataFrame columns filter columns
groupby transform nunique
sum abs values values groupby transform bool
columns groupby columns agg columns DataFrame agg transpose assign columns groupby columns agg assign columns columns last gt columns columns gt sort_values columns columns columns fillna set_index columns groupby loc
merge count groupby transform count DataFrame product columns merge pivot_table index columns values count dropna merge
sample append eval replace DataFrame columns
apply rename columns
DataFrame index index index rolling count rolling quantile index index count quantile DataFrame index index index count rolling apply count rolling apply
sub sub groupby transform min bool
isin isin
DataFrame apply explode astype astype DataFrame apply explode astype astype DataFrame apply explode astype astype
eq groupby cumsum
groupby apply join astype
merge index
astype apply join
iterrows loc mean std mean std
assign join
set_index
sort_values sort_values sort_values
columns
DataFrame loc isin value_counts div
replace ffill groupby agg join reset_index
columns sum groupby sum reset_index
loc loc
DataFrame shape shape head
eq isnull cumsum groupby ffill fillna groupby ffill where eq
DataFrame replace
mask isin set_index
index iterrows DataFrame DataFrame DataFrame apply
groupby
loc shift loc shift loc shift shift
DataFrame apply
assign groupby transform all
keys reorder_levels set_index groupby index append unstack reset_index drop keys reorder_levels unstack
all columns compare columns columns eq all columns values where columns columns values where DataFrame
items loc isin
loc loc loc loc sum
DataFrame columns apply explode
sample replace replace fillna fillna replace apply fillna replace apply fillna applymap applymap
mode mode apply stack reset_index max rename max groupby max reset_index mode replace groupby apply mode reset_index index pivot index columns values index index columns
DataFrame
reset_index drop replace dropna reset_index drop
replace replace
columns
where isin where isin where isin
update
merge merge merge
style apply iloc iloc iloc
assign loc shift shift assign loc shift shift fillna
DataFrame plot index index
apply
set_index unstack columns columns join pivot rename_axis columns
all drop_duplicates first to_csv
items
groupby nlargest mean groupby nlargest mean set_index index groupby transform nlargest mean
astype
DataFrame assign loc columns apply join dropna astype
index iterrows index all loc index index index values append index index index iterrows index all loc index index index values plot index index
columns replace columns query notnull
index index append index index append index index append index index append index index append
DataFrame columns index index join merge
groupby agg sum
loc loc loc
apply
drop assign groupby groupby
values values
DataFrame columns columns lookup columns loc columns loc columns isin lookup
groupby agg groupby agg DataFrame columns add_prefix
assign melt sort_values
apply
rolling mean index loc index
astype
DataFrame
drop_duplicates sort_values drop_duplicates sort_values
reset_index drop reset_index drop DataFrame reset_index drop
columns columns index index stack unstack
columns
style iloc style style
groupby transform mean where groupby mean drop join where
size size apply explode size
iterrows append
apply
merge
eq cumsum ne rename columns pivot index columns values rename_axis columns
astype where isin groupby count
replace notna notna iloc iloc values append values mask isna loc mask shift loc mask astype groupby apply
groupby agg items astype bool any
replace
columns reset_index drop add
melt product eq groupby product agg join product
apply append shift
iloc get first
loc
reindex columns columns
mask assign shift astype agg mask
join any DataFrame
min max DataFrame DataFrame index columns assign at index iloc loc iloc T to_string
copy copy loc ffill loc loc ffill loc fillna copy mean std join sub sub values mean std shape values mean std
iteritems drop
set_index to_dict sum apply
groupby sum groupby sum reset_index isin groupby sum index isin
groupby index index loc loc assign assign DataFrame columns columns groupby apply
DataFrame index plot
DataFrame drop DataFrame drop
diff ne cumsum groupby groupby sum
replace groupby apply ffill where bfill
groupby where transform quantile transform median
merge merge assign groupby transform sum
apply apply
DataFrame iloc
sort_values
iloc loc loc
get div index append explode reset_index columns set_index unstack columns columns
set_index append sort_index reset_index
DataFrame DataFrame nunique
eq all all
groupby count groupby nunique
mask mask loc mask iloc loc mask
DataFrame groupby columns agg astype sum values
to_dict
set_index columns columns dot columns rename columns reset_index set_index columns columns dot columns columns columns keys reset_index set_index columns columns mul columns replace droplevel stack unstack set_index columns columns mul columns replace droplevel stack reset_index pivot_table index columns values join
filter groupby count T rename_axis columns index index
unstack reindex fillna mean stack
columns columns
DataFrame items style apply
get
pivot_table index columns stack reset_index
iterrows replace join at
astype
groupby apply iloc count
append assign to_csv index
apply
DataFrame count apply astype cumsum
iloc iloc index index index all groupby transform groupby agg first sum sum sum reset_index set_index
columns columns any columns columns sum columns any columns
sub DataFrame T
DataFrame replace
reset_index sort_values fillna ffill
index groupby all values sort_values values groupby transform diff values first values isna loc columns drop columns
append value_counts sort_index sum
DataFrame columns corr filter drop drop corrwith drop corrwith drop
DataFrame index
shift where ge shift
DataFrame columns append index
query shape values all any values all any values bool values all bool
query
merge merge
head
astype replace astype replace dtypes bool bool dtypes bool bool astype dtypes bool bool
dropna to_csv index
to_dict items
get size get size count memory_usage sum sum sum sum
groupby sum
copy
groupby groupby loc index index index groupby apply reset_index drop
apply
columns sum mean columns resample agg
items loc
apply
get
DataFrame index to_csv index DataFrame to_csv index
head
melt melt melt loc melt melt
where
iat iat iat transform DataFrame values shape shape apply columns
eq groupby transform any astype
replace replace
assign
set_index loc
where
index to_sql append index columns columns values update values iloc values
DataFrame clip at pivot_table index columns count droplevel reset_index index rename_axis rename columns columns
merge query groupby agg first size
pivot_table index columns sort_index columns columns reset_index rename_axis
columns iloc append last
DataFrame index index all index index index index
size DataFrame to_csv mean std mean std sub __iter__ __iter__ sub sub mean std sub sub mean std mean std mean std
all index set_index sort_index columns all unstack notna any index var var interpolate
mask loc mask loc mask apply
join
get replace append replace append to_csv index
where DataFrame assign where where
shift shift
notna groupby transform rolling sum ffill groupby groupby ffill
rename columns fillna merge rename columns fillna
DataFrame columns index eq isin groupby sum reset_index eq isin groupby sum
fillna
assign append
apply explode value_counts
all
set_index assign plot
pivot_table index columns size add_prefix
first columns columns DataFrame columns columns index index items columns loc index
loc loc
apply
apply replace replace replace
rename_axis reset_index set_index rename_axis stack rename
sub DataFrame unstack loc apply DataFrame DataFrame values index index
mean columns groupby transform
pivot values index columns reindex columns
assign pipe groupby columns sum
loc loc assign loc fillna mask
shift shift iloc index
DataFrame add_prefix agg sum mean var std skew kurt median unstack set_axis index T loc set_axis index
mask mask mask
mask isin bfill iloc where isin bfill iloc
ne groupby any reset_index columns any first any any first ne groupby agg reset_index columns columns
groupby transform max drop_duplicates
loc gt lt
replace
min sort_values reset_index drop apply groupby apply sort_values reset_index drop groupby
DataFrame columns rename_axis index DataFrame columns index set_index index
loc values loc loc loc apply join
replace astype dtypes dtypes applymap replace columns isna all astype dtypes
replace
DataFrame columns index idxmax
apply astype apply items get
set_index apply explode reset_index asfreq set_index reindex reset_index
shift where
apply to_csv index
merge query filter values
columns columns columns replace head itertuples
apply to_dict
set_index groupby resample sum reset_index
set_index iloc reset_index
astype astype
groupby first rename columns assign groupby first rename columns assign set_index rename columns drop reset_index
assign replace astype query
DataFrame index set_index drop get values rolling apply combine index merge rename columns reset_index
rolling apply mean rolling apply std
sum groupby apply reset_index drop groupby transform
any append groupby join DataFrame append append to_csv
filter shape filter shape
melt xs
columns columns where columns dropna plot
sort_values index
loc isnull fillna ffill dropna reset_index drop
astype loc bool
groupby first
index DataFrame apply loc loc
values astype loc to_excel DataFrame astype
index set_index add set_index
apply explode apply join apply max groupby agg join reset_index reindex columns
sort_values plot
loc replace notna groupby sum reset_index
groupby agg reset_index groupby reset_index groupby reset_index assign apply assign apply assign apply
DataFrame DataFrame
DataFrame join DataFrame add_prefix DataFrame join DataFrame rename columns DataFrame index rename columns rename_axis reset_index DataFrame index rename columns reset_index DataFrame
DataFrame join apply join
DataFrame columns where isnull
pivot_table index columns values
drop drop
first index index at index index index sort_index
eq groupby transform max drop eq
merge all
join join
DataFrame columns apply to_dict
merge sample sample sample sample sample sample sample sample sample sample groupby sum sample sample sample sample sample sample sample sample sample sample sample sample sample sample sample sample sample sample
DataFrame where T groupby eq T cumsum groupby eq cumsum where shift T drop
explode apply
DataFrame columns index drop DataFrame
info info info replace append DataFrame
mask notna ffill groupby sum
shape astype iloc rank first ge
columns columns isin columns assign
values append iterrows
items columns columns
where duplicated
mask T where mask T T where mask T T DataFrame T columns T mean std where T T T mean std
columns set_index join set_index join reset_index
all all apply keys
merge
groupby agg count loc
melt
index loc loc index loc loc
iloc loc iloc
DataFrame columns
DataFrame columns columns append
DataFrame append set_index rename columns drop empty join dropna any sort_index last head to_csv
index iterrows index loc index index index index update index
DataFrame index index join to_dict
DataFrame replace replace astype
diff groupby diff groupby cumsum groupby transform
DataFrame DataFrame get reset_index drop to_csv
drop columns empty index iterrows append
DataFrame at all apply notnull
mask index mask mask mask
fillna apply
set_index
groupby mean reset_index pivot index columns values groupby mean pivot index columns values
columns groupby transform sum mask duplicated mask duplicated
DataFrame columns style apply iloc to_html
merge
iloc pipe rename columns columns
groupby count reset_index pivot plot
columns notna dot replace
join
astype nlargest nlargest columns
groupby where transform sum div groupby where transform sum set_index where sum sum div rename merge where
values isin
loc loc loc loc copy shape index loc eq
DataFrame
reset_index merge query index assign index isin astype
DataFrame lookup DataFrame lookup isna loc loc lookup set_index
merge
iloc iloc iloc iloc iloc iloc iloc iloc iloc iloc loc index iloc index iloc index iloc iloc iloc
sort_values
duplicated sum apply duplicated sum DataFrame columns
DataFrame query loc
get values values first at values append
duplicated groupby transform sum duplicated groupby transform sum
apply columns index index stack reset_index stack reset_index
DataFrame columns DataFrame head head columns DataFrame columns append DataFrame head head columns
DataFrame T DataFrame pop add_prefix join add_prefix sort_index columns columns
to_csv
bool
groupby apply join
groupby astype transform first astype mask index isin head index
dropna groupby agg join reset_index drop
reset_index reset_index drop
DataFrame values columns
DataFrame iloc iloc
bool
DataFrame to_csv index to_excel index first append DataFrame
DataFrame replace sum update update
loc drop_duplicates set_index
idxmax DataFrame lookup index
count index index
min style apply
append
reset_index set_index drop columns head
DataFrame columns DataFrame columns values values
dropna min max all copy merge min dropna reset_index max dropna reset_index max min min max plot dropna min max all copy merge min dropna reset_index max dropna reset_index max min min max plot min index set_index min max index set_index max
DataFrame apply apply
diff diff
select_dtypes columns select_dtypes columns loc loc fillna loc loc fillna
info append DataFrame columns
index loc values
where
iloc iloc append iloc append iloc iloc append append iloc append append
apply DataFrame DataFrame values apply
sem
apply
isin values get reset_index drop index columns update update
shift sort_index dropna all ffill
columns columns columns rename columns columns
bool
replace
index values
DataFrame DataFrame
groupby sum reset_index groupby sum reset_index merge groupby sum reset_index pivot_table index columns values reset_index rename columns columns
where where columns DataFrame columns columns index
join DataFrame columns apply
lookup index
pad shape bool mask pad pad mask mask T sum shape max cumsum shape shape where all columns
apply index apply stack reset_index drop drop join dropna index join
DataFrame groupby mean reset_index mean groupby mean reset_index rename columns mean groupby mean reset_index rename columns mean
DataFrame dtypes
append append
get append DataFrame to_csv index
items items items items notnull stack reset_index set_index
pivot index columns values
index min index min max
replace replace
mask loc isnull squeeze loc mask loc mask values
index index index reset_index index index index index index index index
first sum join first
sub
index iteritems loc index
assign diff ne cumsum groupby agg min max count reset_index drop diff ne bool
index
values size DataFrame DataFrame values
T rename_axis index stack reset_index rename columns
DataFrame count count count sum
sort_values groupby shift groupby transform shift
sub
DataFrame columns index index index index isin head
get take append all columns first append DataFrame columns
copy copy explode rename explode rename columns merge columns filter loc columns
iloc assign iloc assign
isin
groupby transform mean std mean std
append index index append index index
merge query drop reset_index drop
DataFrame
columns isnull all
groupby apply diff fillna
dropna columns mask mask sum mask index
merge where where drop
style apply min
replace
first
fillna astype pivot_table astype pivot_table index columns values count rename_axis count sum size size groupby sum
T min
DataFrame
plot
first sort_values columns groupby shift drop
append abs iloc DataFrame rank
join join bool any apply filter apply any
append sort_values
shift shift eq
iloc iloc sum sum
DataFrame DataFrame append join apply
sample T
stack stack stack stack sort_values stack stack groupby diff round fillna
groupby agg sum count
rename columns rename columns add columns
groupby transform rolling std
assign replace
columns values index max loc get last columns at index idxmax max
assign fillna groupby agg fillna max add_prefix
groupby agg sum mean
DataFrame
DataFrame keys
shift cumsum sum
iloc iloc sum iloc
copy select_dtypes fillna mean copy select_dtypes columns
apply where
sample DataFrame
apply columns DataFrame
groupby agg min min max max apply loc min index apply loc max index
DataFrame loc sum loc sum
apply apply isin
set_index sum keys
values values
shift cumsum
columns applymap columns columns columns columns apply sample reset_index drop columns columns columns applymap columns apply mean std mean std mean std
DataFrame
groupby max dropna plot groupby max dropna plot groupby max plot
append
get last index shift eq shift eq loc
assign pivot_table index columns values count resample count rolling sum
groupby update
duplicated duplicated
assign count pivot_table index columns values count sum assign count pivot_table index columns values count sum assign count pivot_table index columns values count sum
sort_values groupby diff eq shift sort_index
merge loc merge
DataFrame replace astype fillna astype join
DataFrame columns columns
DataFrame merge DataFrame
DataFrame DataFrame columns DataFrame DataFrame DataFrame DataFrame DataFrame DataFrame DataFrame stack reset_index drop
index size loc iloc values
drop columns groupby agg std
groupby apply to_dict
loc diff cumsum groupby agg iloc iloc
join keys
pipe
apply loc iloc
count where columns columns count where columns columns all DataFrame count count
sum groupby idxmax
loc columns items
sum sum
DataFrame apply apply
merge merge fillna drop
groupby transform count loc
DataFrame first apply apply
DataFrame DataFrame index index shape
DataFrame from_dict index columns div sum
values first first
reindex dropna itertuples
merge
groupby apply values
any sample sample iloc sample sample groupby apply
resample mean
rolling min iloc index
mask loc mask loc mask loc mask loc mask mask loc mask loc mask loc mask loc mask mask loc mask loc mask loc mask loc mask
DataFrame DataFrame assign set_index assign set_index loc count
groupby agg join join
DataFrame mean median columns append
index sub sub apply index
eq sum
rolling mean bfill ffill rolling max bfill ffill index loc
merge merge add_prefix add_prefix add_prefix items
fillna groupby transform mode
DataFrame replace join
DataFrame shape index index columns columns
iterrows iterrows T
keys
set_index append unstack apply sum sort_values drop columns plot DataFrame join join abs columns groupby agg sum sort_values set_index head set_index append unstack apply sum sort_values drop columns plot
rename_axis columns index rename_axis index
at applymap join merge columns DataFrame loc loc loc shape columns columns
join groupby
append add add assign assign
DataFrame
groupby sum reset_index pivot index columns values resample asfreq fillna
sample index apply sample index isnull sample empty index
rolling
groupby index count count count count
loc min
pop pop replace
to_excel index
resample mean
iloc drop index
keys DataFrame index columns
set_index stack set_index stack reset_index values rename columns values
copy rank rank rank sum rank
merge
apply
DataFrame columns index DataFrame columns index
values items replace
index iloc append copy
replace
shift copy shift fillna
DataFrame DataFrame apply values apply values apply values
reset_index drop mask agg isin mask sort_values mean std eq eq mean std join product DataFrame values mask agg isin mask sort_values mean std eq eq mean std
copy columns mean mean
index iterrows
assign
index index size nunique sort_index index nunique index nunique index index nunique index index index nunique index
index set_index stack reset_index drop groupby agg reset_index
values astype DataFrame where mask
DataFrame pop items reset_index drop join reset_index drop pop items reset_index drop join reset_index drop
sort_values
mean std mean std mean std T melt where index columns
values drop_duplicates
columns join agg rename groupby agg reset_index groupby index apply index index unstack to_dict
iloc prod eq bool
index index index index merge set_index lt gt astype count set_index index index append index resample keys sort_values drop_duplicates reindex ffill astype index diff abs fillna apply columns loc count reindex index
columns pct_change
where replace where T replace T
loc isna copy loc append drop_duplicates
loc gt expanding mean ffill
plot
fillna bfill dropna mask loc mask
DataFrame plot DataFrame plot DataFrame
DataFrame min
corr where corr values corr values stack
apply
insert insert
all all all last eq mask isna isna equals equals equals size
explode explode drop columns notnull loc iloc index index index explode
index append append eval DataFrame
columns pop astype bool DataFrame values columns values columns columns index index where add_prefix
DataFrame columns join merge groupby agg fillna
DataFrame DataFrame
xs
iloc iloc apply where isin
get DataFrame columns DataFrame columns to_csv index
loc apply loc apply
DataFrame index get index
astype nlargest index nlargest index loc
index plot append iloc
items DataFrame apply apply
drop melt drop dropna rename merge merge drop set_index stack reset_index drop
to_dict DataFrame to_json
assign merge drop
take style style DataFrame dtypes isna sum nunique index reset_index rename columns index astype to_json DataFrame index to_json take to_dict columns columns
unstack reset_index sort_values to_string index
DataFrame iloc DataFrame DataFrame DataFrame columns DataFrame DataFrame DataFrame DataFrame values DataFrame DataFrame values columns
set_index columns columns stack reset_index
filter filter eq values filter filter where where mean mask mean
groupby count reset_index
itertuples itertuples itertuples itertuples index update index first first
filter get add values to_excel index
loc
apply
iloc ne iloc any iloc ne iloc ne iloc any
DataFrame from_dict index groupby sum
groupby groupby groupby groupby append groupby apply
mask mask DataFrame mask
DataFrame columns keys
unstack set_index append unstack columns columns droplevel last ffill reset_index drop rename columns reindex columns
DataFrame columns columns columns
pivot_table size plot plot
values values values
DataFrame columns replace columns replace replace columns eval
drop
columns columns columns iloc columns
iloc
astype values sort_values diff groupby diff all groupby itertuples diff diff diff loc max values groupby transform max rename rename sum groupby transform sum all first drop_duplicates first all columns drop diff sort_values reset_index drop
apply
expanding apply iloc gt iloc mean expanding apply iloc gt iloc mean index DataFrame index index index
where shift cumsum max min any groupby apply columns astype iloc
combine update combine append last combine DataFrame
loc max
index astype astype
reindex index assign
sort_values groupby apply diff astype
columns groupby sum columns groupby sum
merge rename combine_first drop
sort_values
columns iloc columns drop
loc size loc size size
isin any
applymap applymap applymap
columns columns explode drop explode explode merge groupby agg dropna
items
apply apply join
product DataFrame DataFrame
groupby nunique eq bool groupby apply bool
DataFrame columns rename columns columns
loc isin loc isin loc isin
append filter first first append append DataFrame columns
DataFrame append
T any
sort_values groupby diff fillna eq reindex index
loc loc columns
round all loc to_csv
DataFrame merge loc max drop rename DataFrame groupby transform groupby transform drop_duplicates last to_dict items merge where where drop rename
DataFrame DataFrame query query DataFrame DataFrame query items
columns columns count
count columns shape count iloc count count
pivot_table index columns values join astype
DataFrame columns replace replace replace replace applymap
reset_index groupby apply nlargest sort_values values reindex T
loc
count count
copy sort_values groupby apply head loc index
lookup lookup get all empty empty any append append items
groupby index count reset_index
append DataFrame DataFrame
agg max idxmax T columns astype astype set_index agg max idxmax T columns astype
loc loc columns values columns
replace columns values join
loc groupby expanding join iloc loc index iloc iloc
get first get first
loc count loc count
rank astype astype groupby add astype
shift items update items update items
join groupby apply rename astype bool groupby apply insert
DataFrame query DataFrame T append
astype DataFrame astype
notnull all isnull
columns columns
DataFrame shape shape shape shape shape
groupby columns first agg reset_index
groupby get groupby count reset_index pivot plot pivot index columns values add sum sort_values plot iloc plot loc
columns dtypes
DataFrame
columns to_excel index reindex columns append columns to_excel index
dtypes astype to_dict get
any any
groupby cumsum transform where astype
loc
columns DataFrame index columns apply max
DataFrame columns columns
DataFrame melt columns
first append
loc loc items
loc iloc astype apply join loc
loc isin
DataFrame index columns filter to_csv index
duplicated first bool
merge groupby merge groupby drop_duplicates
isnull all all isnull all all isnull bool
join groupby transform add_suffix
sort_values groupby diff div mul loc eq drop drop_duplicates reset_index drop
to_csv
all values quantile
isin cumsum where eq groupby transform max where eq groupby transform min values fillna values where values eq
iloc apply value_counts shape
items columns groupby mean reset_index to_csv index
max combine append max append
columns mean std iloc rolling max iloc rolling sum mean std
iterrows
DataFrame
plot plot
nlargest drop index sum append
groupby apply astype to_dict groupby apply replace
keys columns keys groupby keys apply DataFrame to_dict reset_index items items to_dict items DataFrame size size size assign replace replace astype items mean std to_dict mean std
add_suffix product
add DataFrame add add add apply add
index DataFrame
stack reset_index set_index sort_index itertuples
merge groupby size unstack
rank rank merge filter merge query sub values sum DataFrame rank rank rank loc values loc values append
join join groupby apply join
isnull index max
DataFrame size div astype add
where values values values values values where
DataFrame values items values sort_values values fillna sort_values
max max min max
append
set_index groupby tail mean size DataFrame groupby rolling mean reset_index drop
groupby apply dropna to_dict reset_index rename columns to_json
isnull DataFrame apply
replace
DataFrame columns DataFrame columns DataFrame columns columns
DataFrame shift apply
get index values loc index values round loc replace loc index values round loc replace loc index values round loc replace loc index isin loc index isin
get query query query query query query query get query query query
DataFrame columns values T rename columns columns values
loc apply
astype resample min sum astype resample min sum
any any
loc index index all
DataFrame
sum
astype groupby agg join
DataFrame columns query
columns append DataFrame
fillna reindex
values values values values DataFrame T columns index
groupby apply shift reset_index drop
DataFrame
DataFrame
notna dot columns assign
DataFrame columns columns iloc columns columns append reset_index drop
drop index merge drop_duplicates
any append append DataFrame columns
iloc
items items append append at replace
index iterrows drop index
set_index agg isna all to_dict groupby apply to_dict
loc index loc ge loc sum
DataFrame append iloc to_excel index
iloc replace T apply first_valid_index where
any first_valid_index last_valid_index index isin drop first last sum values cumsum loc index iloc
DataFrame count count bool count
merge
groupby
merge
DataFrame index last index isin index tail
append round mean append
join groupby apply explode dropna reset_index
abs abs abs apply min
DataFrame columns loc loc loc loc DataFrame columns
assign groupby pivot index columns values add_prefix rename_axis columns reset_index drop reset_index drop groupby columns set_index groupby unstack add_prefix rename_axis columns columns columns droplevel groupby agg T apply explode add_prefix reset_index drop rename_axis columns
DataFrame columns rename rename columns
mean std DataFrame mean std apply mean std apply mean std
merge merge drop_duplicates
empty DataFrame empty
iloc iloc
groupby apply iloc
drop_duplicates
melt melt
join unstack fillna unstack fillna insert
values
merge reset_index
sub apply
apply
insert columns
melt dropna
values iloc iloc iloc iloc
loc isin loc
loc notna all to_csv
any apply DataFrame bool
DataFrame add items at
add astype
columns
set_index join
isin isin set_index index set_index index isin isin
DataFrame
assign keys assign
index index tz_localize
reset_index melt count count groupby add astype pivot count droplevel
value_counts groupby first append loc index reset_index loc index
stack reset_index columns
pivot index columns values
groupby max
DataFrame eq values shape apply explode
DataFrame isnull any isnull values sum isnull sum isnull values any
pct_change fillna round
explode get groupby agg join explode assign get
apply apply
apply
dropna all any all any any values drop all all values drop columns
last reset_index index DataFrame from_dict index columns style apply
merge sort_values drop_duplicates last assign drop merge assign where groupby agg any first reset_index
pivot_table index columns values reset_index
set_index update set_index reset_index
insert where isin style applymap to_excel
query
reset_index melt index astype index
drop_duplicates set_index to_dict
to_csv index index to_csv reset_index to_csv index DataFrame index to_csv
columns columns squeeze keys append keys
assign groupby pivot_table index columns first swaplevel sort_index columns columns reset_index
copy reset_index drop apply loc sum loc sum shape append shape
take to_dict items values take items first values apply
to_dict pop pop
DataFrame DataFrame apply min iloc
values values DataFrame from_records columns
astype astype astype
eq eq cumsum idxmax groupby eq cumsum shift fillna transform groupby apply droplevel loc ne
set_index
bool DataFrame rolling sum eq mean std all iloc mean std
values where where astype astype astype
DataFrame all append
mask isin
apply
drop_duplicates first reset_index drop drop_duplicates first
explode index explode DataFrame values explode index join drop explode
replace dropna DataFrame replace dropna
DataFrame size rename columns head groupby apply sample reset_index drop head
index index index index
merge
groupby diff shift fillna groupby cumsum diff shift fillna cumsum
append eval
count DataFrame append to_excel
merge merge drop columns
min join all assign merge assign min max clip where min max max min clip filter where sort_values groupby last filter isin
groupby agg first join head
get columns plot hist columns size diff plot
mask shape shape mask mask DataFrame columns all values all values columns mean std mean std mean std
groupby groupby groupby loc loc loc loc keys
set_index combine_first set_index reset_index fillna
apply join apply join merge merge
set_index at loc DataFrame
groupby agg sum sum DataFrame columns copy copy idxmax idxmin loc loc values loc loc values reset_index groupby apply reset_index drop get index
DataFrame keys keys keys keys keys keys append DataFrame
columns columns columns columns DataFrame columns interpolate interpolate drop values plot count loc
iloc lt any lt shift lt shift lt
ge max mask groupby transform max
merge
apply info update get get all apply
values stack
columns
DataFrame DataFrame columns assign
add apply add DataFrame apply
apply drop head
DataFrame index index index
DataFrame columns columns columns columns
DataFrame loc max max all columns DataFrame index index drop drop columns columns columns values dropna reset_index drop drop all values first fillna index replace all empty fillna loc max max all DataFrame index index drop columns drop
axes axes plot plot
get all sum sum info info any loc ge set_index
loc mean
where where where where where where
drop index
to_string iloc columns T reset_index columns astype round sort_values reset_index drop head
join items
isnull groupby size
get DataFrame filter items apply replace replace
DataFrame columns index
stack loc index index unstack astype iat
bool any any bool
join add_prefix
assign explode assign groupby
dropna explode groupby agg reindex index
all values mode values mode shift
DataFrame from_dict index iloc iloc iloc append
T T T T T T T T T T T T
iloc iloc
get keys first keys keys where empty keys append items append
DataFrame values groupby rename columns transpose index index groupby unstack copy copy
apply
DataFrame where isna
astype sum shape shape
fillna
join columns all columns columns loc columns values
reset_index drop apply apply groupby agg replace groupby
shape columns shape append iloc columns iloc values iloc values values append first loc iloc iloc iloc
update append DataFrame DataFrame DataFrame update update update
items index pivot append items pivot merge
columns columns columns columns apply
get to_excel index update to_excel index append
where where loc loc
groupby cumsum groupby first reset_index merge fillna
DataFrame apply
at mask loc mask items mask loc mask
sample DataFrame DataFrame index set_index sort_index groupby aggregate groupby agg mean min max rename columns columns mean min max columns
values DataFrame values columns
index explode mean std index mean std
DataFrame size DataFrame
max count groupby sum
set_index T drop
itertuples columns append DataFrame columns columns
groupby cumsum where
replace loc
items min max values isna sum append DataFrame
DataFrame groupby agg DataFrame from_records index index fillna T
groupby max index index
groupby drop to_dict
groupby agg first max min last
duplicated mean std groupby transform size mean std groupby filter
DataFrame DataFrame merge DataFrame DataFrame merge DataFrame DataFrame DataFrame drop_duplicates first DataFrame merge
DataFrame to_csv DataFrame T to_csv index
apply items
DataFrame get first combine
append apply groupby agg join apply rename columns apply apply apply apply to_json
merge set_index index index index
shift shift shift shift
DataFrame index columns where astype bool stack rename_axis reset_index where astype bool stack values reset_index DataFrame index columns where astype bool stack rename_axis reset_index where astype bool stack rename_axis reset_index merge
columns
index append items cumsum index append copy join all sort_index reset_index drop
DataFrame
shift apply
set_index fillna
append eq
DataFrame stack join any join stack bool mask stack any mask bool mask
DataFrame drop replace head set_index rolling mean values head
iloc ffill iloc iloc replace ffill iloc
iloc index values values index iloc index values values index
astype astype astype explode
index diff where ffill
DataFrame
isin loc loc drop columns apply
apply
select_dtypes apply select_dtypes apply columns
iteritems groupby agg sum iteritems
reset_index astype join groupby agg join set_index reindex columns columns set_index columns
loc loc loc notna loc notna
groupby rolling apply shift groupby rolling apply groupby shift groupby
DataFrame columns shift notna
columns mask
columns set_index stack groupby apply reset_index
shift fillna astype reset_index drop rename_axis reset_index head loc
append append append mode
DataFrame size columns apply quantile quantile where lt
items to_dict
where assign items
query query query add values
merge drop columns
DataFrame DataFrame append to_csv
index style
DataFrame
groupby agg join replace replace join
first all at take shape at all all all append
groupby size get first at head index
append
iat apply iat
DataFrame from_dict sum pop groupby sum pop add pop rename merge pop
DataFrame append DataFrame DataFrame
DataFrame mask apply mask
where fillna isin where isin isna
groupby sum nlargest index values
index
replace fillna cumsum shift groupby apply replace fillna cumsum shift
set_index reindex notnull astype reset_index rename columns set_index unstack notnull astype stack reset_index
DataFrame columns DataFrame
notnull set_index
merge mask astype astype mask drop
style apply style apply values
assign first assign first first first
sort_values value_counts
columns groupby transform mean sum rank sub astype groupby apply
groupby apply cumsum shift gt
loc index index index index index index index index index index loc
set_index set_index update reset_index
DataFrame replace apply apply
set_index groupby groupby sort_values index append DataFrame index
DataFrame columns shape
loc iat loc iat
DataFrame columns empty
values merge columns columns index index columns
groupby last first max min mean
plot plot last index index index index last plot plot plot values columns columns columns columns last index index index index last columns DataFrame DataFrame
to_sql replace index max
DataFrame index items values set_index index
where
values astype max select_dtypes values astype max
apply
product product DataFrame from_dict DataFrame from_dict merge pivot product
loc loc
columns mean mean mean
dtypes append iloc mean std iloc mean std groupby mean std groupby mean std
applymap
DataFrame columns DataFrame
stack reset_index values index columns columns values
columns columns astype plot plot
iloc iloc pop
count iterrows count values DataFrame columns loc to_csv index mode
reindex columns
add diff ne sum diff values compare values compare
DataFrame DataFrame merge assign assign drop
DataFrame loc loc mean loc mean
take first to_csv index
rename DataFrame
loc to_csv index
groupby apply iloc iat iloc
DataFrame get apply
transpose reset_index drop transpose reset_index drop
loc
columns columns
loc
columns apply join iloc values replace last last columns values apply apply apply join apply join
ne shift where
DataFrame join
apply
DataFrame index index duplicated
columns loc loc loc
duplicated notna notna duplicated notna duplicated
groupby filter nunique
mean join mean groupby agg reset_index
iloc min groupby
merge rename columns
loc select_dtypes
iloc keys index
plot
DataFrame
assign to_csv index
columns columns set_axis
to_csv to_csv
isnull sum
copy sum mean std mask eq combine_first mean std loc add mean std mean std replace sum mean std where mean std where mean std
melt query
groupby transform max where set_index idxmax
DataFrame columns DataFrame columns index index join merge isin iloc sort_values
dropna copy drop
where index plot
columns
DataFrame
value_counts loc eq eq empty eq eq empty ne ne
loc
min max sort_values tail groupby apply reset_index drop
iloc mean iloc mean set_index set_index
index sort_values loc loc iloc
apply
groupby count reset_index
get get replace get replace get min min min min DataFrame to_csv index
replace apply values
DataFrame stack values at stack values index T items index columns pivot groupby any groupby unstack T groupby first values T dot dot T dot values columns apply values cov apply iloc DataFrame rename keys sort_index pct_change dropna apply iteritems head
append
columns min values
set_index to_excel join first to_excel
merge where eq
pivot_table count first plot groupby count first unstack plot drop_duplicates pivot_table count plot
eq rolling sum shift groupby apply add iloc groupby apply eq groupby shift add shift add shift add shift
reset_index
columns columns assign stack groupby last melt drop dropna drop_duplicates query sort_index reset_index drop
to_pickle where
ne shift cumsum loc groupby sum
DataFrame DataFrame apply iloc iloc
filter iloc columns columns
loc apply ge
to_latex
values values append DataFrame explode explode drop join reset_index drop values values copy columns values append DataFrame
replace max max max min max max groupby product agg replace
groupby agg assign
add dropna
mean append index values
sort_values sort_values abs
items agg mean std DataFrame items isin any mean std items explode eq any mean std
values
dtypes to_dict index values dtypes to_dict items astype dtypes to_dict items
DataFrame loc index loc isin isin isin apply drop_duplicates reset_index drop
set_index reindex index min index max ffill rename_axis reset_index
mean groupby mean
replace apply value_counts gt all
set_index stack reset_index drop reset_index rename columns
reindex index interpolate reset_index drop reindex index
values values
index groupby assign mean assign values index get
resample size reset_index resample transform first transform last groupby size reset_index groupby transform first transform last
values min values max values values sum DataFrame
apply
set_index unstack add_prefix reset_index merge columns
index loc loc loc at loc loc loc at at
DataFrame DataFrame drop apply merge apply merge DataFrame rename columns where where where dropna dropna assign count first empty update merge merge count where drop index values index index
join append DataFrame apply all all all
index resample transform mean
merge rename columns merge rename columns fillna ffill
replace
DataFrame columns columns T
apply where
DataFrame melt
join
keys
shift replace
replace replace
applymap all bool applymap all columns
keys add_suffix
add
mask
min max
mask apply mask apply all
astype
DataFrame columns min max index index iterrows loc columns where
all diff join sub groupby DataFrame columns mean mean loc groupby apply
DataFrame DataFrame
DataFrame DataFrame
mask mask bool
where assign where pipe assign apply
max loc lt values ne count groupby apply reset_index drop
isin
loc first
compare T join T columns values values columns values values columns values values DataFrame columns index index compare compare style applymap
apply apply apply
items items
fillna assign drop_duplicates drop
get iloc to_csv
groupby agg values
first last isna values
columns columns where groupby transform count groupby add astype radd columns
where
rename columns sub iloc
columns columns DataFrame T
apply replace
groupby rename rename sum reset_index
drop drop_duplicates sort_values reset_index drop
replace
pop index index index index sort_index
DataFrame DataFrame where
merge rename columns rename columns sort_values fillna astype
drop head
to_csv index
where all apply apply filter apply
add duplicated isin
reset_index rename index columns index loc index loc index loc loc
loc sort_values index
all set_index product loc index groupby agg max groupby agg product join
DataFrame columns explode
loc drop to_csv
sum sum sum
min max
assign explode groupby
mean
groupby max groupby first
rename columns sub rename columns filter rename columns sub filter rename columns filter filter columns columns columns columns sub
DataFrame from_records transpose
any any DataFrame groupby apply
iloc columns DataFrame iloc DataFrame iloc DataFrame iloc
replace apply
where isnull
agg all apply agg loc loc loc
DataFrame columns replace values applymap values apply DataFrame first DataFrame values columns DataFrame columns DataFrame columns keys index reset_index drop add_prefix join all add_prefix join join add_prefix columns columns
bool
DataFrame stack reset_index columns DataFrame stack reset_index columns copy
values values append append
columns DataFrame columns columns
isna isna columns sum
replace
set_index columns columns join plot set_index plot
DataFrame from_dict index T columns
values replace replace set_index astype add prod reset_index replace set_index astype add set_index astype add prod reset_index
update keys apply apply values update values update append keys apply
values all values all iloc nunique nunique
loc apply replace
groupby shift fillna groupby shift fillna DataFrame groupby shift fillna
filter stack groupby agg join
max columns to_string index max columns DataFrame to_string index to_string index to_string index to_string index max max
DataFrame columns columns sample
select_dtypes apply
append DataFrame columns reindex T reindex
assign ne cumsum radd shape drop style apply
DataFrame apply apply apply head at at at applymap head applymap head
loc index groupby mean groupby apply groupby sum sum
DataFrame columns copy copy copy
ne groupby apply iloc
DataFrame values to_excel
assign explode reset_index drop
loc min loc min loc min
DataFrame columns DataFrame columns DataFrame columns append
first columns columns append append all columns
loc astype bool
append DataFrame index
apply apply stack reset_index drop drop join merge
min max
groupby apply min
append append
fillna pop
DataFrame DataFrame join T rename columns
index index
pivot_table index columns values first
melt mask mask melt mask query mask mask index DataFrame columns columns isin eq all set_index groupby apply add_prefix rename_axis rename
join eval join eval join bool eval join
isin pivot_table values index count sort_values reset_index groupby diff sum
apply
info info info info info count round info round round info DataFrame from_dict info count round round DataFrame
bool
DataFrame filter groupby groupby size reset_index
apply join append join apply
groupby agg first first max size sum reset_index
DataFrame values columns explode apply values join drop iloc
fillna astype apply fillna astype
apply join isin
set_index set_index keys columns index
max DataFrame drop columns append DataFrame to_csv
replace replace columns columns DataFrame columns columns loc add at columns astype to_excel
get columns head index pop insert insert pop index drop insert
DataFrame groupby agg count sum min max reset_index rename columns
assign fillna groupby rank
DataFrame set_index isna isna any bool isna any
apply astype
groupby ne shift cumsum agg head mean DataFrame shift shift ne shift ne shift ne shift cumsum ne shift cumsum shift ne shift ne shift cumsum
get all div div div DataFrame
where mean mean where mean where stack mean
groupby sum sort_values reindex index index unstack plot
where iterrows
loc loc loc isin
values index set_index
sort_values index index index groupby transform first columns loc copy pow pivot index columns values columns columns get join
index append iloc iloc isnull index
pivot index columns values reset_index
index append index append join
groupby apply DataFrame iloc reset_index iloc groupby apply iloc reset_index iloc T groupby apply iloc assign iloc reset_index reset_index values reset_index drop
applymap
fillna ffill fillna bfill groupby transform
stack explode reset_index groupby count reset_index columns count
DataFrame size columns quantile quantile all
get info info info get DataFrame get info info info get append DataFrame
index index values swapaxes
apply isin where isin
DataFrame T astype astype loc
index plot
lookup values apply lookup
mean std mean std transform mean std
groupby transform fillna max
loc T DataFrame columns
loc
apply applymap
get keys
DataFrame from_dict index columns sort_values drop_duplicates last
set_index reindex index min index max ffill reset_index set_index reindex index min index max ffill reset_index
itertuples drop drop append drop
notna isna
apply
DataFrame columns shape plot plot
DataFrame columns sub loc index loc index loc index loc index loc index loc index assign where where groupby agg sum sum sum reset_index
duplicated any mean groupby mean unstack
mask notnull mask
DataFrame insert columns DataFrame interpolate interpolate pad
index index
index set_index sort_index all count count groupby count cumsum first loc count reset_index set_index count
columns astype
unstack stack unstack stack
isna any isin loc isna any
sub sub
groupby filter index any
DataFrame groupby
astype astype
DataFrame keys
apply apply
resample agg first last resample agg first last resample agg first last resample agg first last resample agg first last
assign replace iterrows loc loc all
index iterrows append all index index all index index DataFrame
groupby DataFrame eq eq to_csv join
melt reindex add_suffix rename_axis groupby
groupby values transform join
apply groupby apply
first set_index T set_index transpose
columns
DataFrame columns itertuples index DataFrame index index index index append index index
style apply columns apply columns
keys index keys items append DataFrame items index index keys index keys items append DataFrame items index index to_csv items keys index keys items append DataFrame items index index
loc
loc head reset_index drop shape groupby sum groupby sum
size size merge size loc loc join size merge size groupby size apply sort_values head reset_index drop
melt melt drop sort_values
keys keys
mean update columns dtypes eq first first first mean mean groupby agg groupby agg
count
append append append append DataFrame
replace
groupby transform
diff diff iteritems DataFrame
append append
applymap applymap all bool applymap all applymap all applymap
iloc append DataFrame T
DataFrame append assign
where ne first_valid_index last_valid_index loc diff fillna reindex index
mode items mode mean median max items values mode mode add append append at append mean mean mean mean median median max max apply apply apply add_prefix merge
eq all isna all eq eq isna isna bool where mask ne shift cumsum value_counts eq bool any ne shift cumsum value_counts eq bool any
index apply
filter filter
DataFrame
join join
set_index diff sum loc loc
groupby set_index reindex min max isnull index
where where where loc where where where loc
apply fillna stack unstack fillna
groupby last drop_duplicates last
DataFrame columns iterrows T values stack droplevel values keys keys stack sort_index droplevel
replace
pivot_table values index columns sum
join append join append append iloc to_csv join index DataFrame columns to_csv join index
DataFrame rank groupby rank min reset_index rename columns rank merge
max min DataFrame groupby max min mean
values fillna columns where notnull any iloc columns groupby columns fillna transform mean
rename columns where assign apply where where set_index to_dict index where where where
mean std mean std mean std
DataFrame fillna set_index stack dropna reset_index columns apply apply pivot_table columns index values sum
columns columns any all
DataFrame select_dtypes columns dtypes
groupby agg sum reset_index pivot index columns values
groupby apply size
apply DataFrame columns columns
DataFrame DataFrame merge
loc index get all index values loc groupby ffill fillna ffill
index columns sample DataFrame columns iloc cumsum astype T columns iloc head index T index columns
drop drop
where append
DataFrame apply compare merge compare isnull compare compare compare compare compare compare
DataFrame drop columns columns copy
apply rename columns DataFrame values columns columns
mean
dropna pivot rename_axis reindex columns reset_index rename columns
DataFrame T reset_index columns
stack
groupby apply assign diff groupby shift cumsum cumsum drop columns reset_index drop
groupby agg sum columns
values stack T empty shape
groupby filter eq any
groupby tail loc ne
reset_index drop rename_axis reset_index
add
iloc columns iat columns loc
set_index
DataFrame sample reset_index drop
set_index to_dict apply mean explode groupby mean
loc
drop_duplicates last sort_values sort_values
diff bool diff where bfill astype
DataFrame
loc loc loc
DataFrame size groupby index agg join
values DataFrame values index keys values columns
set_index reindex groupby apply reset_index
groupby plot
copy loc groupby sum reset_index merge where drop
set_index index loc
index copy loc fillna ffill loc
DataFrame dtypes astype dtypes
DataFrame replace replace first first replace apply columns shape columns
median median loc loc groupby mean
get div append DataFrame columns
values get values DataFrame columns
columns columns
melt groupby value_counts unstack rename_axis melt rename_axis
DataFrame columns columns index iterrows loc loc loc loc index loc drop columns count duplicated columns reindex columns columns values
join
DataFrame columns groupby DataFrame
add all append where any mask any loc mask
astype groupby transform cumsum shift fillna
DataFrame index DataFrame index columns loc
ne shift cumsum groupby agg first join reset_index drop
groupby index agg last mean
pivot add_prefix reset_index
index index DataFrame DataFrame set_index index set_index index loc index dropna any
index all index isnull all shape at all iloc index isnull all shape iloc
min max join where min min join where
to_csv index
eq loc eq ge le apply any DataFrame apply apply apply apply any bool
assign max max drop columns drop columns
loc
where mean
DataFrame index plot index columns index sample plot index axes plot index axes
DataFrame values
copy copy first DataFrame DataFrame copy append index copy append
DataFrame count loc isin count
DataFrame columns index fillna astype to_csv
DataFrame columns
groupby dtypes
DataFrame apply apply at at columns
DataFrame groupby sum unstack unstack rolling div sum mul unstack take sum rolling diff abs rolling sum stack
drop assign agg to_dict
unstack T T
DataFrame from_dict DataFrame from_dict any loc values apply
DataFrame groupby idxmax max groupby idxmin min append set_index append stack rename index reset_index drop set_index index loc values set_index sort_index max min max min max min max min max max min min
append
DataFrame
groupby count groupby count groupby count index
assign pivot_table count rename_axis columns index droplevel reset_index drop
T iloc plot
sort_values groupby agg join reset_index
DataFrame index join rename columns
sum head
apply
apply where isin
first duplicated first empty loc
DataFrame loc DataFrame DataFrame get DataFrame groupby DataFrame sort_values shift get groupby apply index groupby droplevel sort_index
mean mean
sub sum columns columns sum
first groupby transform first groupby transform values mask drop
groupby filter
index
set_index value_counts rename_axis reset_index
groupby size
apply
first sum sum sum first first drop_duplicates groupby agg reset_index drop
drop columns DataFrame columns append append
replace DataFrame columns replace
apply
append
append DataFrame columns DataFrame
T rename columns
apply
dropna dropna
columns columns columns rename columns append
replace replace replace columns first plot
bool
pop DataFrame copy to_dict apply pop pop
loc values at
replace replace sample DataFrame DataFrame columns DataFrame
stack dropna
mask groupby mask loc mask
DataFrame first assign groupby first columns first
assign merge assign groupby agg
copy values copy copy copy
reset_index drop sort_values groupby
loc values DataFrame loc
sort_values where
index index index index
index index astype at at
DataFrame DataFrame any all columns
notnull notnull rename index columns merge rename index columns drop rename index columns merge rename index columns drop
merge rename columns fillna fillna first loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc
info DataFrame
abs div cumsum
first mask mask assign astype groupby sum assign groupby sum
reindex columns columns columns update
apply
isna
DataFrame index columns DataFrame index columns iloc
mean std DataFrame columns mean std mean std
eq eq all bool
values values index index columns shape shape sum join DataFrame columns columns DataFrame index columns columns rename_axis apply ge sum div reset_index values values index index columns shape shape sum join DataFrame columns columns append DataFrame index
value_counts index
DataFrame values DataFrame values apply apply
astype
columns
ne apply
loc loc set_index
apply sub
items sub sub items DataFrame items T rename_axis reset_index
abs
loc
replace fillna astype mean
assign loc
corr corr corr DataFrame index index columns columns
shift
all
query join query query query query query query query iat
sum
merge merge values merge fillna DataFrame to_csv index
index index index index index index first first first join first DataFrame all join join replace replace all all
where where where where where
melt melt melt groupby idxmin iloc set_index rename_axis
where
DataFrame columns values columns
reset_index rename columns index index
sort_values sort_values reindex index columns
DataFrame explode
groupby eq cumsum transform cummin
apply apply apply mean std mean std mean std lookup DataFrame index index lookup index mean std
eval
DataFrame columns query query count count
replace replace dropna
groupby sum DataFrame from_dict index index index isin where index isin groupby sum reset_index
groupby value_counts unstack fillna apply join index astype astype rename reset_index groupby value_counts unstack fillna plot
apply apply
DataFrame product apply product
apply
apply
assign
eq query
dropna all
value_counts head index fillna
melt sort_values reset_index drop set_index stack dropna reset_index rename columns
columns iloc join DataFrame T columns
DataFrame iterrows at pow
value_counts index where isin value_counts value_counts iloc append iloc sum index
apply
keys
reset_index columns
apply round
DataFrame sum values reset_index drop sum mean std sum values mean std astype div sum mean std
pivot index columns values
apply join drop_duplicates
groupby apply add cumsum reset_index drop groupby cumsum rename columns
append index apply apply loc drop
sum rename_axis reset_index count count
to_dict
eq groupby cumsum eq
duplicated bool duplicated groupby transform sum where duplicated
isin all
bool
eq groupby pop sum plot
size size eval DataFrame size notnull drop notnull drop size notnull drop size
set_index append
DataFrame
var DataFrame query query var
replace groupby agg sum reset_index loc
head index iterrows get at index to_csv index
DataFrame to_dict set_index to_dict to_dict
sort_values iloc iloc groupby first groupby last drop
fillna
groupby shift shift cumsum
merge loc add mask join DataFrame loc mask iloc iloc values values append append DataFrame set_index to_dict transform DataFrame get get values reindex reset_index drop merge set_index merge set_index sum sum drop apply join sum DataFrame size astype join mask DataFrame loc mask
apply DataFrame from_records columns values reset_index drop
eq groupby size reset_index groupby size reset_index merge fillna
groupby groupby to_csv to_csv
DataFrame set_index to_dict
append min
groupby ffill groupby bfill drop_duplicates
round
groupby transform mean
all drop
replace replace
DataFrame values assign rank keys rank
DataFrame dtypes
replace eval replace applymap eval
groupby update
mul apply DataFrame columns
where groupby cumsum
index iterrows
to_csv index
values eq fillna values
fillna
set_index sort_values groupby where dropna assign set_index join reindex columns columns
groupby ne shift cumsum apply diff fillna
DataFrame apply columns groupby sum
items DataFrame to_excel
DataFrame
keys keys keys items items items items items items items append items DataFrame from_dict index index index unstack droplevel
at
isin loc loc shift
append append DataFrame columns
set_index stack groupby apply unstack reset_index transform transform transform transform values DataFrame columns columns index index
merge drop_duplicates
DataFrame DataFrame DataFrame eval columns
fillna first
apply join
set_index loc sum reindex sum apply reindex
astype astype index
loc iat loc values loc loc iat DataFrame loc iat loc mean std mean std mean std loc mean std
astype where
DataFrame copy copy
index
join values DataFrame append join
value_counts sort_index index astype
merge set_index sum DataFrame index reset_index drop
columns columns drop head
groupby transform pipe min pipe max pipe max min
DataFrame set_index rename columns items index index
DataFrame isin bool cummax cummax bool bool min index max index
DataFrame add_prefix columns
where where
merge fillna fillna fillna columns drop columns rename columns
size plot
copy loc mean mean loc loc loc std loc loc loc loc mean loc DataFrame get
append to_excel
sort_values groupby last
value_counts value_counts
DataFrame columns where iloc iloc
groupby agg T to_json pivot_table columns to_json
columns loc loc columns
set_index mask eq astype astype astype apply
merge fillna pop
shift notnull cumsum columns shape index loc values loc shape values shape columns
transpose drop apply
DataFrame min max
get DataFrame DataFrame reset_index drop to_sql replace index
replace replace replace append reset_index drop index
groupby apply reset_index drop groupby apply reset_index drop diff loc loc loc groupby apply apply reset_index drop merge all merge merge apply join apply join apply join
sample index DataFrame index index index cumsum index groupby
groupby transform size groupby transform size groupby transform groupby transform mask mask groupby transform size groupby transform size mask mask groupby transform size groupby transform size mask mask value_counts value_counts DataFrame size size
apply
pop copy DataFrame columns join pop
DataFrame stack dropna isna groupby value_counts unstack plot
loc loc eval
apply select_dtypes columns apply
sub iterrows values apply apply add_suffix to_csv index mode
replace
DataFrame from_dict index T to_csv index
DataFrame columns DataFrame values columns columns index index size T set_index columns T stack reset_index drop
index index index index index index
DataFrame append to_excel
index index index append drop_duplicates reset_index drop head keys to_csv
apply eq sum
values sort_values
values astype values astype loc
rank rank loc rank rank
set_index columns
join eq any isin any
assign
ffill groupby ffill
groupby groupby groupby groupby product product product product append append product append append append
groupby last groupby first
query max groupby idxmax loc set_index max count gt groupby sum
align
max max columns first update groupby agg
set_index index resample sum sum values reset_index
values DataFrame where index index columns columns
groupby sum reset_index replace
DataFrame groupby count rename count join count
set_index set_index columns index loc reindex columns sort_index fillna fillna
index rolling mean
DataFrame DataFrame columns min min style
merge
loc isnull mean loc isnull sum shape
index columns drop
DataFrame groupby apply
append groupby apply
append
xs xs loc loc
sub
DataFrame from_dict empty empty max mask mask mask loc mask mask max head max empty empty max apply
apply dtypes
where
sub columns columns shape shape
mean mask mean index isin index loc mask mean
index
mask apply isin any mask isin any mask
fillna isin isin isin isin isna
keys DataFrame mean std DataFrame reset_index drop mean std
DataFrame values index index columns columns apply sort_values values
get keys keys assign to_excel index
reset_index drop reset_index drop reset_index drop
DataFrame to_csv index
eq groupby transform sum var groupby sub var var
join
loc isnull index loc isnull values columns notnull
sample reset_index drop add
apply
where take take where where
mask eq any mask
stack rename reset_index boxplot
count shape sub shift eq count eq groupby count transform first assign where drop count bool count bool
set_index between_time reset_index reindex columns columns
merge iloc query
columns columns
iloc to_dict
diff diff shift astype cumsum to_string index
iterrows
xs min fillna xs max fillna xs min astype xs max astype
apply loc groupby idxmax transform transform
isna sum apply
DataFrame groupby agg min max mean min max mean join DataFrame columns min max mean index index
set_index groupby agg sum
DataFrame cumsum set_index unstack
shift fillna astype
loc iloc
pivot columns set_index unstack drop columns columns columns reindex columns columns reset_index
max min columns columns values values drop values itertuples columns DataFrame columns
apply loc
DataFrame set_index stack rename reset_index DataFrame groupby diff values columns index index join
groupby apply to_dict groupby apply
rolling apply sum astype
DataFrame DataFrame columns
groupby transform join
join rename set_index append reorder_levels index rename get set_index append reorder_levels
loc index at index columns iloc iat values
replace replace replace replace
min fillna astype mod mul min apply min min min min min min min min min min min min min min
DataFrame from_dict
pivot ffill stack reset_index gt
equals values values values values values
stack count count unstack stack count count unstack
merge set_index reindex sort_index reset_index
update groupby sum
iloc iloc
DataFrame DataFrame
rename columns index index index
loc DataFrame loc DataFrame
melt pivot_table index columns values first add_prefix reset_index rename_axis index columns
apply
T index values
loc ge product set_index product product
where isin keys
where
groupby transform tail isna sum
loc loc loc loc diff min diff append append
get copy transpose reset_index rename columns index astype melt count sort_values reset_index drop
sum cumsum groupby any
join get apply join get apply
reset_index drop reset_index drop
apply
DataFrame product groupby product
apply round apply round
iloc iloc sum apply
merge merge
apply index index apply
index index astype DataFrame astype astype reset_index set_index index join reset_index drop set_index
sample sample DataFrame melt sample sample sample sample sample sample sample sample sample sample sample sample sample sample sample sample sample sample
groupby join join groupby apply
groupby transform max drop assign index
loc query index loc index loc query index loc query index loc query index
replace get items
DataFrame where
DataFrame DataFrame add DataFrame DataFrame DataFrame DataFrame DataFrame add
sample DataFrame from_dict index replace any sub join append index apply
mask mask
assign groupby pivot_table index columns values add_prefix rename_axis columns reset_index pivot_table index columns groupby values add_prefix reset_index
columns drop columns mask max
replace
DataFrame rename columns
DataFrame apply apply apply
values stack reset_index values values pivot_table index columns values groupby values dropna
DataFrame iloc iloc sub iloc sub iloc
apply apply
index
merge rename columns
ffill eq index all
index index values apply
where gt cummax
DataFrame head DataFrame head
DataFrame drop columns DataFrame reindex columns
groupby all itertuples itertuples iloc
apply
values apply
select_dtypes columns abs
apply
loc fillna ffill loc
join append
iloc columns
iloc axes iloc axes
idxmax where any
groupby eq cumsum sum apply groupby eq cumsum sum
iloc values iat values iat values iat values iat values values iloc mean std iat values iat values iat values mean std iat values mean std
melt explode dropna reset_index drop
keys
DataFrame
add values isna append assign fillna astype apply append append
applymap sub items
DataFrame groupby groupby groupby groupby
items items apply apply
columns groupby sum
fillna astype reset_index groupby index apply sum drop index
stack value_counts sum stack value_counts mean std mean std mean std
shift rolling sum where
merge merge query
quantile diff quantile diff diff
DataFrame set_index stack reset_index rename columns melt
first_valid_index index
DataFrame head apply head head
apply
DataFrame join notna
mask lt mean
columns groupby aggregate min index reset_index
index
keys values keys append keys append DataFrame index
DataFrame from_dict index columns rename_axis
DataFrame items columns pivot index columns drop columns columns droplevel
apply loc isna loc isna
bfill ffill loc isnull DataFrame columns bfill ffill loc isnull DataFrame columns bfill ffill loc isnull DataFrame columns bfill ffill loc isnull DataFrame columns bfill ffill loc isnull
DataFrame drop to_sql to_sql to_sql
loc
DataFrame iloc iloc apply
groupby transform count gt isin loc groupby transform count gt isnull loc
DataFrame columns index index join
pivot duplicated pivot_table index columns values mean plot mean plot
duplicated
bool join join astype
apply
groupby agg sum groupby agg sum first first
iloc
DataFrame iloc pop apply
pivot_table values index columns count
loc
DataFrame index transpose
sort_values shift columns rename columns shift columns columns copy columns columns take at values loc drop columns
sample DataFrame columns index explode DataFrame from_dict to_dict index explode take groupby index join DataFrame index index groupby agg index explode DataFrame from_dict to_dict index explode take DataFrame index index join filter join groupby agg
xs xs
set_index reindex_like set_index reset_index fillna plot count plot count index values
eq groupby transform
columns columns
where where
DataFrame columns groupby sum
sort_values last groupby last
loc loc
iloc groupby apply
loc loc loc loc
DataFrame apply join filter pivot pivot_table index columns values first
DataFrame items columns values aggregate values groupby aggregate join reset_index aggregate values groupby aggregate reset_index
sort_values drop_duplicates first sort_index
DataFrame transpose columns columns shift compare shift filter values drop columns drop
DataFrame
groupby agg to_json
apply columns apply merge DataFrame apply columns drop isna
join astype
to_csv
resample
any any index
loc where
DataFrame groupby all
groupby sum reset_index sum drop_duplicates duplicated drop merge
join add add_prefix add add_prefix add
size size mean std mean std
DataFrame columns apply mean std apply mean std apply mean std
columns set_index
melt count groupby count max count count query
DataFrame join round sort_values
DataFrame columns transform transform
groupby transform round sum astype
DataFrame index DataFrame index
index reset_index reset_index index index index set_index index join
where size where size
fillna drop_duplicates set_index fillna
sample index isna mean values dropna sample index isna mean isna mean dropna sample index loc
info append info
rename_axis reset_index where duplicated
apply isna values
to_csv mode index
dtypes dtypes
loc iloc loc iloc
get columns apply combine all iloc iloc index interpolate set_index reindex resample all ffill resample mean mean
count count DataFrame
loc copy loc copy groupby groupby rename columns rename columns last merge last last fillna
mask notnull eq notnull mask mask dropna all
groupby transform mean round
merge
info
apply
DataFrame
sum values values
join join
shift iloc apply columns where max
mean
empty join
keys keys DataFrame columns get div div div div append DataFrame columns
groupby
groupby sum groupby apply sum
DataFrame columns groupby sum unstack plot
all
first where drop first index drop drop
columns columns astype dtypes dtypes
where
set_index loc
loc isin mean
first set_index index dtypes astype to_dict bool
groupby cumsum cumsum sum
groupby product apply assign rolling median reset_index drop
groupby value_counts unstack min groupby value_counts unstack reindex min
DataFrame
ne values query eval DataFrame size columns ne ne values
apply join isna apply join first isna
DataFrame items columns DataFrame items columns
DataFrame index DataFrame
set_index set_index merge merge
DataFrame count DataFrame count
size iloc dot columns where size
drop_duplicates first
assign explode groupby agg assign explode groupby agg join
apply groupby shift last
where min le mean std where mean std min le mean std where mean std
isnull all index dropna all to_excel index
DataFrame groupby quantile DataFrame groupby quantile
merge sort_values reset_index drop
apply apply
mask shift
loc
size DataFrame size index sort_values groupby DataFrame shift apply cumprod mean std groupby shift keys cumprod mean std
replace assign filter where eq groupby add isna fillna
loc loc
drop columns merge head
values
groupby count filter groupby count groupby count
dropna DataFrame values index index join
diff cumsum index count mask
groupby
groupby sum replace
min iloc iloc max iloc iloc
cumsum get all bfill merge at merge groupby last where shift where shift drop drop
mask eq mask groupby transform sum ge
replace
where
apply
sum max shape iloc reset_index drop loc abs loc loc loc
values
shape columns index index
groupby ne shift all cumsum apply drop columns dropna
where iloc isna any
get
append
DataFrame columns style apply
groupby groupby
count sum
melt drop sort_values
at at astype
astype groupby sum
DataFrame columns columns iterrows copy replace size update replace size size append append reset_index drop mask any mask loc mask loc mask loc mask loc mask loc mask loc mask apply replace loc mask size loc loc apply replace loc size size
select_dtypes apply any
DataFrame columns plot
iloc append append append append append append append append append append append append DataFrame
mask isin loc mask values index iterrows index values DataFrame index index iterrows
astype
join get
bool
fillna astype fillna
eval eval
iloc DataFrame columns index columns index
count eq shift eq cumsum count
DataFrame index columns add values values index apply DataFrame index columns values values index apply apply index
merge query merge query
DataFrame columns values sum loc
merge drop columns rename columns
DataFrame values items
items add items append values values values
product product where isin
cumsum lt sum
DataFrame DataFrame apply apply
index add_prefix groupby agg sample mean
iloc iloc any values iloc
set_index reset_index all insert join append append
DataFrame columns DataFrame
apply
loc loc merge sort_values reset_index drop columns index
reindex mode reset_index drop lt max lt max ge min le max
pivot_table index columns values
loc values loc values loc values loc values loc values loc values loc values loc values
reindex columns
set_index index index index index reset_index
set_index append groupby pct_change dropna all reset_index drop
append append index iterrows append DataFrame append
style apply
columns columns
index index index
DataFrame size
apply fillna astype DataFrame fillna astype replace DataFrame columns
columns
where count gt
filter mask columns
DataFrame DataFrame round drop_duplicates first sort_values
values DataFrame
eq any sort_values shift eq eq any eq cumsum first gt all
columns DataFrame DataFrame abs values shape DataFrame columns columns index index style apply
mask apply apply mask add rename columns columns sort_values T T
itertuples index to_dict
drop
index index shape index iterrows index loc index loc index
reset_index apply index
set_index loc loc loc loc
join
columns columns mask mask
groupby
mask where mask where mask where mask
join
to_records index index
append
last groupby pct_change groupby pct_change groupby pct_change at DataFrame loc groupby idxmin reset_index drop rename columns merge assign values fillna loc isnull fillna loc isnull fillna loc isnull
index iterrows count count index iterrows count count
DataFrame
loc groupby eq cumsum agg min max values
columns columns values values
explode explode explode
index isin
loc reset_index drop loc reset_index drop append reset_index drop
shift cumsum squeeze apply
values
diff any shift any
apply merge
ne shift eq shift fillna eq eq shift fillna cumsum
sample DataFrame DataFrame columns merge div mul
DataFrame groupby agg reset_index
get head
DataFrame
to_string replace
loc columns isin merge reindex columns
sub sub append
append index
where max sample where apply eq where mean std apply mean std
where DataFrame size size where mean std mean std apply mean std
applymap
first first columns drop
groupby sum
first drop_duplicates first replace to_html
DataFrame columns isin
sample assign
copy copy copy std
DataFrame size size iloc shape shape iloc to_csv index
values iloc values values iloc values any astype set_index drop isin any astype loc eq any astype apply values set_index isin pop any astype
DataFrame dot values values columns columns
loc
assign fillna fillna fillna fillna
DataFrame columns explode
DataFrame
pivot_table values index columns plot
loc loc loc
pivot_table index columns values fillna astype bool DataFrame index index columns index min sum max sum DataFrame index index columns index
copy values isnull values applymap isnull iloc copy DataFrame loc values round round loc sum index round append append DataFrame reset_index drop index index astype
isin replace
DataFrame from_dict index columns index where values index
pop DataFrame values columns columns
loc columns
applymap applymap columns applymap
index index first iloc apply sum
columns loc
sum set_index groupby ne columns sum eq cumsum append unstack add_prefix groupby ne apply join columns eq cumsum sum set_index append unstack add_prefix
join join
groupby agg count sum mean reindex values values
DataFrame loc size mean std abs copy loc loc max loc loc min join median mad abs copy loc loc max loc loc min join
mask mask
eq eq
DataFrame filter sum filter sum filter sum
replace
DataFrame
DataFrame groupby max reset_index values
DataFrame
groupby columns groupby columns merge loc
astype groupby apply join reset_index agg apply groupby agg join reset_index
index index index index
columns columns replace dropna
DataFrame columns DataFrame columns
values
apply index
merge drop_duplicates groupby sum reset_index
merge dropna
isnull loc loc iloc iloc loc loc loc loc
apply abs
where eq sum first cumsum le loc
head style size align style head to_html index style
loc loc loc
values DataFrame DataFrame columns values
first sort_values first groupby first reset_index groupby agg max reset_index set_index join set_index
DataFrame columns sub
sample DataFrame columns copy columns columns columns append DataFrame from_dict index columns
describe apply apply count mean std min max count mean std min max
join apply
empty apply drop empty apply
isin loc
groupby transform mean
count explode groupby count
DataFrame size columns
index index sort_values drop columns
index loc
iteritems
groupby apply join rename
DataFrame
plot
iloc iloc
DataFrame apply apply apply
DataFrame columns
DataFrame index astype columns values values at at all values all values values
div keys count rename columns count columns columns rename columns replace filter columns mul round any get applymap loc columns
get append update DataFrame columns get append get get attrs attrs append div attrs attrs attrs append append DataFrame
merge mask isnull isnull mask
loc loc columns loc isin
DataFrame columns set_index groupby index keys values mean values values values loc loc values DataFrame loc index
items all sample apply get sample all items sample apply get sample items all all items
DataFrame loc
columns min max
DataFrame describe rename columns squeeze groupby
columns
explode loc reset_index drop drop loc loc rename columns explode reset_index drop drop rename columns
join query
merge merge merge
DataFrame add_prefix items index index
reindex bfill iloc isna index index index append interpolate index round diff astype groupby values cumsum apply reset_index drop resample apply index min
any any
idxmax
loc shift cumprod shift fillna loc
DataFrame astype join
set_index cumsum rename set_index groupby append reset_index index index set_index append unstack rename columns reset_index last last last
DataFrame columns
apply index
append replace replace DataFrame to_string index
append append append
loc loc fillna loc loc fillna loc loc fillna
set_index set_index update reset_index
DataFrame stack apply ffill unstack T reset_index drop
apply
astype values mean std
index index insert loc drop columns rename columns index index columns to_csv index
apply apply
duplicated any duplicated any duplicated any duplicated any loc drop_duplicates drop_duplicates
apply explode
min max columns min max
pivot_table index columns values rename_axis reset_index melt reset_index drop rename columns loc eq
astype replace to_json
fillna
melt reset_index columns
where eq isna groupby value_counts dropna unstack add_prefix reset_index rename_axis eq isna groupby value_counts dropna unstack add_prefix reset_index where eq isna fillna dropna add_prefix rename_axis reset_index
loc iat loc iat loc iat
DataFrame iteritems DataFrame
DataFrame columns loc to_csv DataFrame columns loc loc loc loc append columns DataFrame columns columns
groupby apply set_index to_dict index to_dict
columns dtypes ne where
applymap
div DataFrame columns
sort_values astype astype groupby agg min max iterrows min max
replace
groupby apply rank filter var add_prefix rank DataFrame groupby apply rank filter var add_prefix rank
query sort_values drop_duplicates
apply
replace
set_index columns columns stack loc index index
join rename
values values values DataFrame columns set_index loc apply loc apply values loc apply values drop
astype
DataFrame columns index DataFrame columns index index DataFrame set_index index index
value_counts index sub index head
groupby agg sum sum sum sum sum mean mean mean
loc groupby
isin drop_duplicates set_index style style style drop_duplicates set_index style style loc style style loc style style
columns select_dtypes columns all columns columns loc isnull loc mode iat loc isnull loc mode iat loc loc fillna loc mode iat loc loc fillna loc mode iat
loc eq mean mean std mean std groupby count groupby mean std query query mean std
resample transform size sort_index
columns columns rename columns
resample agg first max min last sum first count columns columns join query
DataFrame DataFrame DataFrame DataFrame DataFrame count index iterrows loc index count at index at index at index count DataFrame iterrows append loc add append loc append loc append product all count loc count count
bool
sort_values drop_duplicates columns sort_values replace drop_duplicates last set_index filter drop_duplicates copy replace last loc loc
DataFrame DataFrame
DataFrame from_dict DataFrame index apply replace merge fillna
shape iloc copy max iloc round iloc iloc
corr corr corr corr corr corr
cumsum take sample query groupby sample take query groupby sample replace take query groupby apply sample min reset_index drop
query
DataFrame columns DataFrame columns columns columns columns columns columns
filter filter sum sum eq where loc columns
replace mean std replace mean std mean std mean std
loc index index
rolling mean ffill bfill diff cumsum bfill astype max first append
loc isna isna
mask mask groupby mean
mask where mask groupby transform mean
index index first last index min to_period to_timestamp index max to_period to_timestamp set_index append unstack reindex stack dropna sort_index reset_index
groupby expanding mean reset_index drop
query query index query size agg size size agg agg agg agg keys items append agg keys append agg DataFrame
all loc isin
isin reset_index apply set_index groupby resample sum unstack T plot style
iloc
iloc
apply
loc
DataFrame index index dropna
index index
loc
where
apply apply
DataFrame columns append append
DataFrame append rename columns
append DataFrame columns columns
bool
values values values all values count values
shift
rename columns columns columns get append columns index columns
apply join
groupby product value_counts groupby nlargest reset_index product
index index index index index index index index drop sort_index drop_duplicates
index groupby sum
groupby transform all astype eq groupby transform all astype
assign mean std mean std
DataFrame shape shape columns index columns columns index index
index index shape index
groupby items to_csv index
index
where DataFrame where DataFrame
columns DataFrame columns columns index loc
apply
assign
first first transform index index index values set_index style
apply groupby sum
DataFrame columns
ne ne any bool
where ffill rename columns loc mask duplicated
groupby diff dropna
index iterrows DataFrame
squeeze
index index
loc index values items loc index values loc index index isin index values
DataFrame columns add columns groupby transform groupby transform max first columns sum max get last columns columns max groupby max add max combine sort_values reset_index drop max max max max max
groupby size values copy groupby size values
apply rename columns groupby sum reset_index
columns shape columns index index DataFrame columns sample get columns apply
mask isnull
items loc fillna
loc DataFrame loc
mask apply
join rank merge assign filter bfill iloc assign filter max sort_values
groupby apply nlargest sum nlargest index values
DataFrame
DataFrame columns columns index index plot
iloc iloc iloc iloc
cumsum DataFrame size size copy copy copy count index iterrows count at index count at index count apply cumsum cumsum mean std mean std mean std
shift ge shift ge shift ge groupby transform groupby transform
to_string where index all index values all index values index isin
values values bool
where isin columns lookup index
DataFrame columns index index DataFrame append DataFrame index index append
index rename rename reset_index
replace groupby columns apply to_dict reset_index to_dict groupby columns apply to_dict apply apply apply replace groupby columns apply to_dict reset_index merge groupby agg merge to_dict groupby columns apply to_dict
mask where mask
DataFrame values rename columns join
astype astype pct_change to_csv to_csv
mode pivot index columns values columns columns join reset_index filter mode filter mean
add columns clip clip combine
DataFrame DataFrame isin isin head
plot DataFrame dot dot
sort_values values shift groupby apply reset_index drop
rename columns
DataFrame all combine rename columns columns
cumsum groupby agg join size
set_index join
pivot index columns values fillna reset_index
compare drop_duplicates reset_index drop
replace
agg groupby unstack
DataFrame iloc shape iloc shape iloc shape iloc shape
where merge
DataFrame columns DataFrame columns DataFrame join DataFrame columns
join join join join mean std mean std set_index join mean std
sum
diff set_index index isin
mask mask mask
loc duplicated drop_duplicates
drop_duplicates drop
drop_duplicates
to_csv mode columns columns eval append append DataFrame columns
mean groupby transform sum sum loc
DataFrame columns lookup lookup lookup loc isnull isnull apply lookup get
DataFrame loc loc loc
DataFrame apply
bool iloc iloc mask all
mask where mask where mask mask assign where mask where mask
set_index lookup isin isin columns copy set_index lookup update combine_first
columns DataFrame columns columns columns DataFrame columns columns DataFrame columns columns
max add plot index append append DataFrame reset_index append index append
iterrows iterrows
DataFrame from_dict
any add columns drop columns columns
apply apply apply bool apply bool
astype groupby shift isna cumsum mean loc values
reset_index drop shape iterrows isna loc sort_values groupby apply reset_index drop
sort_values drop index
where isna index index
index isin all index eq all
pivot_table index columns values sort_values reset_index
replace apply join
DataFrame loc loc
append head
index get max groupby transform max sort_values max max sort_values max max drop max drop max
to_excel
join replace
update update items sum items keys DataFrame values index reset_index
groupby add astype
DataFrame loc loc diff groupby diff
groupby agg join sum reset_index merge merge isnull eq apply
merge fillna drop columns
join join
isin isin isin
applymap applymap
groupby index count median align
columns columns columns
eq where fillna
groupby transform max min add
mul align sum sum columns align sum sum
product DataFrame product columns
apply apply
DataFrame groupby columns
sample DataFrame columns DataFrame columns index
index index
get apply apply get DataFrame get apply apply get apply get apply explode
applymap stack apply unstack
any dropna all dropna all at dropna dropna
fillna
where
astype astype astype astype
set_index iloc columns
loc get loc values
DataFrame items items DataFrame apply transpose append update items T values add append update items T values append
keys loc loc values loc values
groupby loc loc sum index groupby loc loc sum index groupby apply
copy index DataFrame index index columns columns columns columns iloc
drop
where ne isnull
apply
all apply mask mask
add index first DataFrame to_dict index index assign groupby index agg bool
rename columns groupby DataFrame rolling expanding mean append rolling join columns to_csv
isnull
apply apply apply
groupby
DataFrame sort_values groupby cumsum
dropna groupby agg mean dropna groupby agg mean
set_index join set_index round update round join set_index round
replace apply replace replace replace append append replace replace replace replace sub replace replace replace replace append append replace apply DataFrame columns
reset_index drop join
DataFrame index columns merge rename columns
replace set_index assign assign replace set_index set_index fillna assign assign set_index fillna
where duplicated where groupby transform size
DataFrame append append
loc
get get get get get get get get get get get get get get append DataFrame columns to_csv index
isna transform join
DataFrame index set_index stack rename columns rename columns set_index append sort_index groupby apply set_index resample fillna pad reset_index drop reset_index where astype values sort_values drop_duplicates sort_values
DataFrame index values columns values values
items astype get DataFrame columns
groupby apply max index idxmax min groupby apply max index idxmax min insert loc groupby idxmax values loc groupby idxmax values groupby apply max index idxmax min values
DataFrame index columns
values iteritems to_excel index shape values iteritems to_excel
melt to_dict
index index groupby apply index reset_index
append
sub sub
index set_index
iloc to_dict groupby
columns columns merge fillna
loc
isin loc any any
merge fillna sort_values fillna
mask iloc join mask
iterrows to_dict DataFrame columns keys to_csv index columns mode
columns columns DataFrame values columns
reindex columns
values apply apply apply apply apply apply apply
columns join
groupby apply mean
DataFrame mask mask mask mask
index index index index
join
keys sum T
to_period pivot_table values index columns sum columns columns
lt gt all columns loc all
DataFrame iloc values mean
filter isna filter isna all mean std all any mean std filter isna rename columns filter isna rename columns all mean std
items rename columns rename columns
groupby apply reset_index
mask mask loc mask mask
groupby bfill drop_duplicates reset_index drop DataFrame combine_first set_index reset_index
get
DataFrame
info info DataFrame shape shape DataFrame describe mean std std
query copy diff fillna ne cumsum groupby head reset_index drop
explode groupby agg
resample mean reset_index groupby mean reset_index set_index resample mean reset_index groupby mean reset_index
shape shape shape DataFrame
columns columns columns columns sum columns columns columns columns index sum
iat columns iloc columns at index loc index
set_index groupby resample T pad reset_index head
where count count index count count idxmax
sort_index filter filter where notnull mean div mean values values values where notnull mean div mean values values values
join apply join
count values count count count
query where query DataFrame columns
loc groupby transform max
drop drop columns apply all columns applymap columns add items combine all values reset_index drop index set_index get values index pivot columns values index columns index
eval
any
std mean agg std mean where mean std mean std ffill
astype
apply apply
iloc items
append DataFrame columns DataFrame columns
interpolate T size append DataFrame columns interpolate min max apply apply T apply T index rename columns apply T index rename columns sort_index
loc loc
keys append
to_html index
loc reset_index drop loc
dtypes
values all insert all values columns columns append columns columns
DataFrame isnull isin isnull isnull loc loc fillna loc loc fillna
where
groupby transform all all
index count loc reset_index drop
isin set_index sort_index isin set_index sort_index isnull isnull
any values
clip clip
iloc iloc values values values values
melt DataFrame values index T columns columns set_index stack DataFrame values index index index T
stack astype stack index
sample DataFrame filter where
append sum index
sum sum values sum
fillna
sort_values reset_index drop
loc count shape T T count shape loc gt mean columns T gt mean
iterrows isin
merge rename_axis reset_index assign rename_axis reset_index assign assign groupby sum
loc loc
DataFrame mean groupby columns values DataFrame mean columns values
keys keys
get append append DataFrame
groupby cumsum sum
apply
values columns
reset_index drop
DataFrame items columns DataFrame items columns
pivot_table index columns astype values pivot_table index columns astype values set_index stack reset_index pivot_table index columns astype astype values reset_index
pivot index columns swaplevel sort_index
append
set_index groupby stack unstack columns set_index groupby astype unstack columns columns join
reset_index drop join
eq max dot columns
get where isnull index insert insert DataFrame insert insert loc insert loc index reset_index drop
eq iloc isna all eq loc isna all
filter corr groupby corr
copy loc loc values groupby sum combine_first
append index index index index index DataFrame index index index index DataFrame
DataFrame astype bool style apply to_excel style style apply
stack stack rename columns pivot_table index columns values join
loc isin loc isin apply
DataFrame size columns loc loc loc dtypes
merge query
loc loc loc
all any
drop_duplicates sort_values loc duplicated
product agg product mean std mean std agg shift rolling apply agg product shift rolling mean groupby apply columns agg product mean std mean std agg groupby shift rolling apply agg product groupby shift rolling mean columns
DataFrame columns to_html
fillna copy all empty empty index iloc empty iloc append iloc append iloc append
reset_index merge reset_index assign merge assign drop drop set_index
keys count count
index set_index join apply set_index index drop_duplicates drop rename columns
sub
DataFrame DataFrame
loc
items append DataFrame columns
apply values all
DataFrame DataFrame loc values columns
apply items
isin round loc loc
DataFrame drop index
iloc iloc
mask fillna apply mask
set_index resample agg sum sum sum
DataFrame DataFrame columns set_index drop rename_axis index to_stata
set_index drop iteritems index eq rename add_prefix replace insert columns columns filter
index T index explode groupby value_counts unstack
DataFrame apply join replace
pivot index columns
apply get
replace
groupby agg size sum
index head head head head head tail tail tail tail tail head tail DataFrame index info DataFrame sort_index info DataFrame
astype cumsum notnull pivot index columns values rename_axis columns index groupby first at DataFrame columns columns columns index info groupby agg first iloc iloc first info at
iloc iloc to_dict max iloc iloc to_dict max
append groupby min join
replace replace
DataFrame columns
columns DataFrame columns to_json
astype astype
groupby apply reset_index last any append sum apply groupby apply index
append
assign
loc set_index index isin set_index index
DataFrame apply DataFrame apply explode drop_duplicates last merge
groupby sum groupby set_index groupby sum reset_index set_index
loc
DataFrame columns columns values values values values values values values values dtypes max max idxmax set_index max idxmax idxmax
DataFrame columns iterrows index append index append
product groupby replace sum product
index iterrows at index
stack value_counts rename_axis reset_index
isin isin
DataFrame apply
iloc to_csv
empty
DataFrame DataFrame DataFrame to_csv to_csv to_csv all append DataFrame join keys columns
median set_index replace astype median values median set_index stack astype median values median
where where DataFrame
groupby apply to_dict reset_index set_index to_dict index
isnull apply apply
mask notna loc mask loc mask apply
groupby loc tail index nunique ne
set_index sub reindex index
DataFrame
DataFrame DataFrame astype astype items loc all loc loc loc append DataFrame
lookup index
DataFrame apply
mean
index index reindex index index index min max reindex
isin apply index mask diff le diff eq mask diff ge diff eq mask drop loc isin mask mask diff le ne shift eq shift mask
values any astype
DataFrame replace replace
set_index unstack droplevel reset_index melt sort_values
product set_index items
groupby drop columns drop columns
count max groupby count add at T
replace fillna
T drop T columns append T append T DataFrame T columns
loc
eq index index index index
at index
replace first apply columns drop drop columns replace join replace replace replace replace last replace at last
apply
loc loc loc
DataFrame size dtypes memory_usage sum agg min max mean mean std mean std astype dtypes memory_usage sum agg min max mean mean std mean std
apply
shift add_suffix shift add_suffix shift add_suffix
get head
DataFrame insert values values iterrows insert
append append join DataFrame columns
assign groupby pivot_table index columns values first add_prefix
groupby agg mean count max mean mean count max mean columns columns join
sample applymap mode astype sample applymap astype all sample applymap astype any
shift shift gt lt lt gt eq eq
DataFrame apply explode reset_index drop agg apply
DataFrame items columns merge div
get index DataFrame index index
DataFrame index columns apply join head to_string index join join
iloc iloc dtypes
where index ffill
mask groupby loc loc groupby transform min
append merge
DataFrame columns DataFrame columns merge
mean mean mean mean apply
set_index groupby rolling mean index duplicated last merge rename
le le astype le le astype le transform astype le transform astype
DataFrame items columns
index index
index index index
shift
reset_index drop
first DataFrame first loc loc
values
index index index index
join items query
iloc iloc apply
DataFrame sum sub apply apply
DataFrame
merge join set_index
shift shift explode explode
index copy product copy set_index product join product copy join where abs reset_index drop index
reset_index drop corr
to_csv index
apply
index columns loc loc assign
sum
columns groupby agg isin sum
apply
mean std DataFrame index index isin any mean std ne mean std gt mean std
replace merge
shift cumsum groupby cumsum
groupby agg sum sum sum
apply
plot add drop groupby sum unstack fillna astype values
to_csv join mode
notnull all max where where
DataFrame apply
fillna notnull
set_index rename groupby apply groupby cumsum transform first reset_index drop set_index rename
index style isin any style applymap apply
DataFrame DataFrame reindex index reset_index drop
groupby shift
isin groupby size
DataFrame index groupby iloc values index index T
DataFrame
DataFrame count value_counts plot plot first plot plot columns plot values
append append join append DataFrame
append DataFrame
loc columns loc columns loc loc loc loc DataFrame sub replace astype replace astype replace astype replace astype index loc loc loc loc loc add loc loc loc astype loc loc loc loc loc loc
apply apply replace join loc
update combine_first
columns applymap all columns iloc apply
apply
groupby agg count sum sum reset_index rename columns
sum
items fillna
join items drop to_dict assign mean std join items notna drop to_dict assign mean std
DataFrame apply join groupby apply
fillna apply drop to_dict
DataFrame DataFrame isin values astype DataFrame sample sample index index isin sample reset_index drop reset_index drop copy values values copy apply
DataFrame set_index pivot columns values swaplevel sort_index
DataFrame
columns values corr round iloc
index columns columns index index columns columns index
join join
replace set_index stack reset_index drop reindex index
loc drop_duplicates loc
copy set_index rename columns
merge rename columns T groupby index mean T
index shift DataFrame insert values values columns
DataFrame loc lt assign round query eq
loc sample index
where
eq reset_index drop assign drop index sort_values reset_index drop
ffill astype assign dropna groupby sum
DataFrame unstack reset_index dropna apply sum
where values to_dict where to_dict values
dropna isin all apply last drop_duplicates last
apply value_counts dropna lt
groupby plot groupby plot
shift
applymap diff dropna diff dropna
pad shape bool mask pad pad mask mask T sum shape max cumsum shape shape where mask mask
DataFrame index index columns columns iloc reset_index drop style apply to_excel index
join filter
any any columns columns any columns any columns columns any columns columns any
columns columns DataFrame
DataFrame
copy
drop_duplicates columns columns items items append
replace
append DataFrame columns astype index tail pct_change
iloc last index iloc last index
fillna apply astype index fillna apply astype index DataFrame DataFrame DataFrame fillna apply astype
apply columns isnull astype bool loc groupby shift cumsum agg min max first first reset_index drop
sort_values groupby first
pivot_table index columns values sum
size
groupby columns columns sum groupby sum
set_index sort_index plot
sort_values iloc diff where isnull shift isnull shift isnull shift isnull shift diff diff iloc
values values
set_index agg to_dict to_dict
where where eq
loc replace
fillna filter fillna fillna filter fillna columns duplicated columns loc index set_axis loc index set_axis
pivot fillna astype drop columns join columns drop pivot fillna astype drop columns join
head
set_index replace dropna astype columns apply join dropna apply replace dropna astype apply join dropna
set_index append join
pivot index columns values stack rename_axis index values columns reset_index values first
apply astype any
DataFrame DataFrame
to_csv
DataFrame columns copy append append
DataFrame index
set_index set_index mask mask columns mask
isnull groupby size
merge product groupby product product transform size
DataFrame groupby sum reset_index
assign filter filter sort_values filter
set_index groupby sum sum rename index plot
sum reset_index
DataFrame iloc
columns DataFrame columns mode to_csv mode mode index DataFrame columns columns DataFrame columns to_csv index mode join columns append to_csv mode index
merge
index shape append iloc index
droplevel axes droplevel
count loc count loc count loc count loc count loc count count
DataFrame apply columns DataFrame apply columns
replace groupby first
to_excel first index columns get index
DataFrame items to_dict index
shape astype astype
isin sum
prod
append
DataFrame index dtypes values DataFrame info
apply
DataFrame explode reset_index drop merge apply drop columns columns
join explode join rename
copy
where astype astype astype astype
DataFrame where duplicated isin sum where duplicated isin sum values values values values
values
mask first mask mask mask first
drop columns append columns fillna
apply
set_index set_index
values sum
ne shift eq shift eq loc
index iterrows loc
plot plot
columns columns iloc
empty
all values bool
values
merge merge set_index set_index reset_index
DataFrame iloc iloc size style apply
sub style style style style to_html style style style style sub style style sub append style style style sub join sub style style style style head style
merge drop filter
merge merge rename columns merge rename columns rename columns merge rename columns
bool
where where where where
value_counts T bool value_counts value_counts
groupby apply iloc reset_index drop
merge
ne shift bool
pivot index columns
merge
values values
DataFrame to_string index dtypes
dtypes plot loc at
DataFrame at last query join query DataFrame at last query query
shape
assign index merge drop
DataFrame index columns columns groupby transform mean
dtypes groupby last assign sub append sort_values reset_index drop
merge loc loc
columns
fillna groupby transform mean fillna mean
DataFrame columns mask filter mask loc mask
lt eq cumsum groupby shift where sub
apply
assign groupby index
ne shift
DataFrame all columns DataFrame apply all columns DataFrame columns apply all DataFrame columns astype astype astype astype astype DataFrame dtypes infer_objects dtypes convert_dtypes dtypes convert_dtypes infer_objects dtypes
index iterrows values values at index values values loc apply
duplicated eq shift astype
min values values
groupby transform eq any fillna
append apply
columns groupby groupby aggregate all columns drop columns drop values groupby aggregate drop
append to_csv to_csv
groupby value_counts groupby nlargest
reindex columns
notnull replace
axes loc eq eq plot axes loc eq eq plot axes axes eq eq eq eq axes loc plot loc
append to_excel
rename columns
loc values assign DataFrame columns loc
sort_index T
values values append
loc index reindex
pop stack reset_index drop rename join groupby size reset_index count count dropna groupby agg join agg DataFrame values index index stack astype reset_index count rename columns count
fillna astype
all astype bool items
DataFrame assign merge drop
first index DataFrame columns DataFrame columns DataFrame columns keys first index plot index DataFrame columns index index DataFrame columns index index DataFrame columns index index plot
merge DataFrame
reset_index sort_values drop
rename columns lookup index
loc index isin loc index isin
groupby size unstack sort_values stack items
sample groupby max rename join where last astype astype loc duplicated last drop columns
sub pop abs idxmin set_index pipe
columns columns loc
get div add add append add append DataFrame head T isnull sum
axes groupby plot groupby keys axes
DataFrame groupby nunique reset_index merge reset_index count count
apply
index index
count
groupby
reindex index index
astype astype values
DataFrame columns apply
DataFrame to_csv index
loc
any T DataFrame cov T rolling
merge groupby transform size
values append join
DataFrame groupby resample sum index columns columns reset_index sort_values loc columns
columns columns columns mask loc mask
keys any apply sample DataFrame reset_index drop keys any
iloc cumsum sub gt any index idxmax iloc index append index append iloc index index append index index append
sort_values ne shift cumsum groupby where eq rename agg first size
groupby shift
items items items DataFrame T mean std DataFrame T mean std DataFrame T mean std
sort_values assign groupby pivot_table index columns first rename columns columns
count
columns columns columns columns columns columns columns all index all rename columns all all all all
iloc iloc
DataFrame drop columns append DataFrame columns drop columns append join
groupby apply count groupby apply sum groupby apply mean groupby apply std columns size sum mean std groupby agg count sum mean std rename columns count size rename_axis groupby agg size count sum sum mean mean std std rename_axis
DataFrame set_index index
append index duplicated last
apply dropna apply apply dropna apply apply apply dropna apply apply apply
get max values max compare eq at eq any eq any
apply
mask groupby transform diff abs all groupby transform diff abs all mask bool
index iterrows index append groupby transform join replace replace values dropna
mask eq index mask at join astype at join astype
join rename columns
sort_values abs values replace replace replace abs values replace replace replace
columns rename columns
all
loc eq any at eq any iterrows at any
apply
groupby transform diff fillna groupby transform cumsum groupby transform cumsum transform
sum
copy
melt sort_index rename_axis reset_index
shift shift isnull
append DataFrame T explode rename_axis reset_index
all iloc all columns columns all bool iloc iloc all bool
DataFrame index index add_prefix
join pop astype bool
DataFrame columns DataFrame columns columns index join T add_prefix DataFrame columns DataFrame columns join DataFrame index index add_prefix DataFrame columns DataFrame columns columns iterrows iterrows append columns items DataFrame columns DataFrame columns columns index join T add_prefix DataFrame columns DataFrame columns assign transpose set_index index astype T assign merge drop DataFrame columns DataFrame columns T reindex index ffill add_prefix join DataFrame columns DataFrame columns join DataFrame index index add_prefix
index
groupby nlargest reset_index rename columns loc
merge pivot eval rename
eq groupby transform idxmax idxmax first eq groupby transform idxmax index first loc index index copy filter bool get
iloc columns iat columns loc index at index
sum loc sum loc sum loc apply gt apply le
get index sort_values drop
bfill dropna
DataFrame DataFrame boxplot
first get diff fillna gt cumsum
mask where iloc mask mask loc mask
min max min max style applymap min max min max style applymap to_excel index
groupby merge groupby apply iloc iloc iloc iloc reset_index groupby groupby apply iloc iloc to_json
merge
index index
ne shift eq shift eq gt
mean
apply
merge
DataFrame columns set_index reindex index index reset_index drop
sample loc isin loc isin mean std mean std
mask isin loc loc mask loc mask
loc groupby agg max query gt assign where isin groupby sum replace unstack ffill at set_index unstack droplevel fillna ffill fillna columns index values loc
columns append DataFrame
DataFrame
DataFrame replace
DataFrame apply join iloc astype
DataFrame columns dtypes index items index bool tz_localize at index dtypes index dtypes dtypes
apply join
DataFrame columns
index index groupby
le groupby transform max isna all loc
sum isnull sum isnull sum
index loc index all
DataFrame size size where mean std where mean std where mean std where mean std mean std mean std mean std mean std replace apply values apply
iloc columns stack reset_index count rename columns ne shift where where count iloc columns stack reset_index count rename columns count bool where count where count
iloc loc mean iloc loc mean iloc loc mean iloc join
assign shift cumsum assign shift cumsum pipe groupby agg values values iloc values iloc prod
eq drop groupby mean
merge drop DataFrame DataFrame
items
apply values
DataFrame columns unstack apply
apply apply
columns isin all infer_objects select_dtypes bool columns columns dtypes bool
T swaplevel T stack stack reset_index columns fillna
set_index groupby unstack columns columns
empty last empty
append
astype astype
filter loc notna sort_values groupby groupby mask mask copy DataFrame size size size size loc notna sort_values groupby mask mask copy update
DataFrame merge drop
update join append append min first update count join
assign values index where notnull iloc iloc pipe groupby
set_index groupby resample ffill reset_index assign
append append DataFrame columns
product join groupby product transform join to_dict all join items product all product product product product product product product product product product
where
append append sort_values groupby
items append items append items append items append head
fillna fillna
resample mean head
join join keys keys keys
DataFrame
values values apply
first replace
between_time index loc loc values between_time index loc loc values between_time index loc loc values between_time index loc loc values
sub columns sub index T T columns transpose
groupby count
index loc loc values
groupby apply iloc reset_index assign groupby drop
replace replace replace replace join replace replace replace replace replace DataFrame replace replace replace replace join replace replace replace replace replace DataFrame
join explode all append explode mean std mean std explode mean std join mean std
groupby size index values plot
size astype groupby index size mean groupby index size mean groupby index size mean values
values groupby product join product get get
nlargest columns query nlargest columns sort_values head
groupby cumsum mask loc mask mask shift diff mask mean
lookup items lookup apply
groupby shift
mask mask
DataFrame columns to_csv mode index
apply dropna all dropna all
query
groupby max nlargest all loc isin copy
shift groupby groupby cummax transform first diff bfill ne cumsum diff bfill eq eq mask diff bfill eq eq mask
set_index apply explode reset_index set_index apply apply stack reset_index drop mean std mean std
groupby cumsum agg
mask any values values mask
stack index
melt merge
rename columns iloc iloc sort_values reset_index drop first_valid_index first_valid_index first_valid_index loc isna
loc
apply max apply columns
index index index
fillna diff sub loc diff apply max min columns max min columns loc diff T apply max min max min
cumsum get groupby transform first first update loc gt groupby shift fillna update gt bool
DataFrame dropna
var add copy add
groupby mean
keys values items DataFrame from_dict index items reset_index columns set_index
index droplevel isin
dropna droplevel reset_index
apply DataFrame
eq eq all bool
index iterrows filter at index join
query
DataFrame columns itertuples DataFrame from_dict index rename columns sort_values join loc loc index isin sum sort_values
loc shift shift loc shift shift shift shift loc all all shift all where
DataFrame T fillna rename columns merge drop
columns values values index index index index
index index
drop index index reset_index drop
any any notnull any
where groupby transform max
merge drop columns
drop columns drop columns
drop_duplicates groupby sum
sort_values groupby diff
DataFrame index DataFrame index DataFrame index
set_index notna eq
to_csv index to_csv index
DataFrame values T groupby sum max reset_index
first loc loc loc loc loc loc loc
set_index max min idxmax
pipe pipe pipe
groupby rolling mean sort_index values
join columns join
eq cumsum groupby cumsum where duplicated last fillna
sort_values reset_index drop sort_values set_index
loc ffill loc fillna ffill
add
shift eq eq any bool
replace
melt
loc loc index
min index drop
sum join join join where join where
keys filter filter
astype keys
merge loc columns
index cumsum groupby columns iloc values append append iloc
DataFrame index loc cumprod
groupby sum
groupby sum DataFrame count iterrows drop_duplicates groupby size reset_index count
drop values
assign groupby ne shift cumsum shift dropna
max apply groupby apply set_index idxmax max update columns groupby max values loc isin values
merge drop_duplicates combine_first
drop size any apply any apply groupby apply
filter columns isin notnull notnull sort_values xs
DataFrame replace
loc mask mask index
columns columns
DataFrame sample sample sample sample DataFrame
DataFrame columns apply eval
gt idxmax radd mask any
DataFrame shift first add apply isnull
DataFrame append head
DataFrame columns apply sum apply count
join DataFrame
apply
isin isin
append
isin groupby sum merge groupby sum
isnull isnull index max reindex fillna ffill dropna columns replace
DataFrame merge index drop columns merge set_index apply apply stack columns columns to_string
DataFrame values index index
DataFrame var index tail index loc
groupby agg reindex index min index max reset_index groupby apply reset_index
pivot abs pivot
var isnull sum round groupby apply var var isnull sum round groupby apply var
set_index to_dict
transpose
to_csv DataFrame from_dict columns DataFrame to_csv index
apply value_counts
apply
drop add_suffix rename groupby merge
isin
empty loc index iterrows
replace
melt merge
plot plot plot
shape join
pipe
columns mask fillna loc mask mask T fillna mask mean T
columns set_index index at index sort_values get first sort_values index set_index
set_index shape index values sum
assign T assign T assign T groupby plot T
DataFrame
count value_counts sort_values count count
shift rolling mean loc loc fillna ffill fillna
loc loc loc loc
groupby sum all columns index drop_duplicates last set_index drop last set_index reset_index reindex columns index columns
DataFrame values astype where values
groupby agg sum reset_index
DataFrame replace replace
to_excel first index T columns get index loc columns index loc loc loc loc
count count count count count
apply apply
join mask dropna columns columns columns
apply apply columns to_csv index
DataFrame
set_index unstack reset_index pivot_table index columns values first reset_index
DataFrame set_index keys drop append DataFrame columns DataFrame columns append set_index append drop
loc loc
shape min DataFrame to_excel index to_excel
groupby mean reset_index
mask loc mask loc mask set_index index values fillna
max count size size size
applymap applymap max columns
DataFrame where isin
index index value_counts drop
DataFrame loc corrwith loc index T
pivot index columns values reindex reindex fillna
where join
DataFrame DataFrame append groupby apply
isin shift fillna
columns values columns values T reset_index drop columns values mean std columns values mean std columns values mean std columns values mean std columns values mean std columns values mean std columns values mean std columns values mean std
assign explode
loc items loc items to_csv items
assign rename columns
mask mask apply loc
apply copy copy keys any update values any max keys items any apply drop
index idxmax
set_index groupby unstack index groupby loc columns
pivot columns reindex columns fillna astype agg reset_index
where
groupby agg apply set_index merge set_index index
DataFrame get
columns where isna dropna columns columns columns replace
sort_values groupby tail
var var DataFrame
loc apply nunique loc loc
assign astype astype aggregate mean pivot_table index columns values mean assign pivot pivot_table index columns values mean pivot index pivot columns
any
columns
DataFrame size size size DataFrame T columns keys
index iterrows values append iloc index
DataFrame from_dict apply
drop_duplicates
count count count DataFrame from_dict index T reset_index drop
min round round round columns columns iloc astype iloc astype size astype insert insert size replace
get transpose
dtypes
T sort_values drop
values get min max iterrows values get min max values index iterrows
DataFrame keys to_html
loc sum to_dict
groupby astype
DataFrame copy index empty join DataFrame columns
DataFrame pivot_table pivot_table index columns size
DataFrame groupby apply max reset_index columns merge
transform drop_duplicates
plot
shape keys DataFrame
iloc copy T
pivot columns index values swaplevel sort_index
groupby get to_csv
apply replace apply replace apply replace
DataFrame count count sum sum count
DataFrame
groupby keys filter loc eq get get empty eq loc get get mask notna notna mask empty loc mask iat groupby apply
reset_index
add apply
DataFrame head groupby min apply head
apply min idxmin assign min idxmin
values values values size size bool append pop pop assign pop pop get first at last assign update append copy loc items DataFrame
items join assign sub
isin query
plot
resample bfill resample bfill
groupby agg
DataFrame columns DataFrame columns
DataFrame
isin isin merge merge combine_first
merge
loc median where median
set_index groupby apply resample ffill reset_index drop reset_index
min max loc mean
sub
groupby agg to_dict
groupby apply to_dict reset_index rename columns to_json groupby apply reset_index rename columns to_json groupby apply reset_index rename columns to_json
keys stack dropna droplevel keys stack dropna droplevel reset_index drop groupby DataFrame keys stack dropna droplevel reset_index drop groupby
set_index apply
columns columns columns astype astype astype append
min max groupby agg groupby agg max min
value_counts mask sum lt replace where where isin mask index mask mask sum index where index isin mask index index sum
DataFrame index
astype
sample loc sample info columns columns dtypes sample index product columns columns dtypes
get assign
loc eq loc iat iloc first loc at
join values append DataFrame columns astype
DataFrame
groupby groupby groupby groupby
groupby agg count sum reset_index rename columns groupby agg sum count sum count groupby agg sum count sum count
sort_values loc head loc head
index isin index loc index
join groupby aggregate mean min max
groupby transform loc loc index idxmax
DataFrame append append append append
get all join join
DataFrame values append values
select_dtypes columns add values add values
diff bfill astype
index values values loc astype
DataFrame apply sub
DataFrame rename columns loc
merge fillna groupby mean
where isna where notna ffill loc
DataFrame keys
append append DataFrame
DataFrame columns iloc dtypes dtypes loc
apply DataFrame apply isnull
DataFrame DataFrame DataFrame
apply loc index apply
index
columns columns columns DataFrame columns columns drop columns
replace join dropna explode agg join groupby agg join fillna at at at at at at at at
query notna
value_counts
groupby min index apply last_valid_index
groupby apply
apply
DataFrame
mode iat
assign merge query groupby mean
groupby loc all groupby loc apply all loc bool groupby loc all loc bool
astype nunique drop
sort_values groupby apply values values rename reset_index
index iteritems
DataFrame DataFrame
DataFrame columns columns index loc sum columns index DataFrame size columns columns index index stack all
DataFrame
loc loc replace astype
groupby count count count shift cumsum apply nunique all loc iloc sort_index reset_index drop
reset_index groupby max reset_index values loc index count loc index groupby count idxmax count
where
DataFrame columns
any
melt columns drop sort_values
sort_values groupby shift dropna
pop join DataFrame index index add_prefix
update index loc index loc index loc index loc index DataFrame update update head
eq loc loc fillna groupby transform median
copy loc copy
melt first groupby first unstack join columns set_axis columns melt first groupby first unstack sort_index columns pipe
pivot_table count assign apply value_counts to_dict get groupby apply value_counts to_dict get value_counts to_dict get
loc loc loc loc
columns rename columns merge
columns loc columns loc corr loc cov
apply
apply
copy columns
index iterrows iterrows loc loc sort_values reset_index drop loc isna fillna
copy copy duplicated sum
reset_index groupby apply set_index count to_dict
sort_values
sort_values drop_duplicates first reset_index drop assign index sort_values groupby first reset_index drop assign index drop_duplicates last reset_index drop assign index groupby last reset_index drop assign index
replace
query query apply query query query query apply
join pivot join pivot reindex
apply groupby append index iterrows
merge
append append
items
loc index
merge
columns columns stack index index
replace
values values
empty items
mask isin where isin
columns attrs iloc drop index
transform transform values
index index index
applymap iloc applymap isna applymap
shift loc
DataFrame astype astype
drop
append DataFrame from_dict DataFrame size columns append DataFrame DataFrame size columns append DataFrame columns loc size
DataFrame append DataFrame
replace astype isin isin merge loc loc loc copy to_csv index
assign mean mean set_index index join
replace add update groupby replace add update groupby
groupby
merge copy copy
isin fillna
value_counts groupby transform count sort_values drop
merge sort_values fillna
diff cumsum groupby agg values values values first groupby mean
values isnull
DataFrame groupby reset_index merge drop columns index
groupby sum groupby sum groupby sum
min min min min
sort_values
DataFrame columns values astype any
DataFrame columns replace loc
groupby agg nunique rename columns nunique count values columns columns join
DataFrame append all index iterrows append append append append append append append append append add append
DataFrame loc isnull loc isnull
fillna copy loc loc fillna
where loc
set_index index index replace
clip iloc ewm mean
loc add shape
join index DataFrame columns
groupby agg sum
assign between_time groupby apply loc between_time mean any between_time values loc last_valid_index
loc values
groupby apply
merge
groupby agg join reset_index
median mean apply fillna fillna apply fillna
groupby count sort_values iloc
set_index reindex min max drop columns fillna groupby apply reset_index
apply apply columns merge melt dropna DataFrame apply stack reset_index drop rename columns drop merge
DataFrame
groupby sum
index
index max index append reindex loc loc ffill
where eq groupby ffill fillna astype groupby cumsum
columns
where replace dropna where replace dropna index isin index to_csv
dtypes dtypes
join
assign assign
DataFrame columns columns columns drop_duplicates values
apply apply mean std append append mean std apply mean std append append mean std
replace
iloc lt iloc sum count T count values DataFrame sum columns columns index count count
iloc iloc add_suffix reset_index drop columns
DataFrame columns DataFrame columns merge columns
groupby groupby merge merge
groupby sum
loc iloc
astype bool
product product loc apply loc apply product index index product loc apply product loc apply product
dropna sort_index index min index max reindex ffill
replace dropna
loc iteritems
sort_values drop_duplicates set_index
columns columns columns columns shape
apply
DataFrame index index columns columns last iloc apply last style apply to_excel
replace apply
abs mean
set_index unstack rolling mean shift bfill unstack rename join set_index reset_index sort_values
T
DataFrame rolling mean iloc reset_index drop groupby index mean mean std mean std
pivot_table index columns values rename reset_index
groupby cumsum eq groupby agg sum groupby agg sum bool
where values values all T T transpose
first get index explode set_index index index set_index index isin all columns values index index groupby assign merge groupby sum rename sum
sum sum iterrows sum sum groupby groupby
index iterrows
merge
loc set_index loc set_index unstack columns columns columns index merge
loc loc values dtypes astype dtypes astype values
columns columns columns
rename columns replace reset_index merge set_index unstack add_prefix merge
insert cumsum values add at merge groupby ffill
count
sort_values loc shift cumsum ffill fillna cumsum ffill fillna add sort_index groupby filter groupby groupby filter
DataFrame size columns shift columns
index index loc copy rename columns reset_index drop loc copy copy rename columns reset_index drop take index index index index all columns merge
loc loc loc
values values
replace query loc
apply
pivot_table index sum
mean max mean max mean max mean max
assign explode values
sort_values groupby diff gt cumsum sort_index loc notna loc notna groupby transform first sort_values groupby diff gt cumsum loc notna loc notna groupby transform first mask groupby
T
resample sum query
to_json index to_json
astype groupby sum reset_index astype groupby sum join fillna astype groupby sum reset_index astype groupby sum reset_index merge fillna
reset_index index reindex drop_duplicates sort_values reset_index drop rename columns rename columns drop_duplicates sort_values
columns dropna iloc dropna iloc ffill pct_change cumprod iloc
append DataFrame
sort_values
DataFrame drop index apply
mul gt sum sum mul gt sum sum
groupby agg join groupby agg join set_index unstack reset_index
DataFrame DataFrame DataFrame append DataFrame index
values values index columns where where columns DataFrame columns
index index
columns columns
columns
set_index add add astype radd shape reset_index
merge merge
index
merge values
eq loc loc set_index set_index where drop_duplicates set_index where merge
all
pivot_table values index columns first set_index pivot index index columns
DataFrame set_index loc loc
index isin mask ffill fillna
groupby size reset_index count groupby count reset_index count
groupby transform sum
where isnull
drop_duplicates first drop_duplicates first
sort_values groupby first
isin all where where
append
eq shift eq cumsum add
pivot index columns values reset_index
loc loc eq
index iterrows
apply sum at apply apply abs loc isin isin sum groupby agg first reset_index rename columns apply sum apply abs loc isin isin sum append apply apply min values values empty apply items loc
isin
append size size size
join index set_index index merge index set_index index
assign fillna loc isna pipe index fillna
apply
values items merge isin values
DataFrame set_index explode DataFrame index index values mean std DataFrame add_prefix explode reset_index drop join DataFrame pop add_prefix mean std append DataFrame columns mean std DataFrame transform mean std DataFrame columns mean std DataFrame index mean std
groupby agg applymap join
shape T reset_index T append columns set_axis append columns iloc sum sum append T reset_index T append append rename columns to_excel index
index loc index loc fillna fillna astype
count set_index index groupby size
DataFrame columns append
index sum
isna loc sum
append DataFrame index tail
shift rolling apply mean mean shift rolling apply mean mean
get append DataFrame
DataFrame T T T T T T T apply
join columns itertuples index
apply stack reset_index drop join
all groupby count to_dict values fillna astype
replace min shift shift cumsum groupby replace
add append
info
groupby agg sum reset_index rename columns merge
DataFrame groupby groupby pivot columns add pivot values pivot pivot_table index values values sum mean mean mean pivot_table index values values sum mean mean mean
mask isna loc mask sample index loc fillna
index index
DataFrame columns DataFrame columns loc index isnull isnull index loc loc loc loc loc
index index resample sum resample sum index index
DataFrame index apply apply loc iloc columns
join stack
astype apply
DataFrame groupby max groupby max all loc isna
empty iloc empty notnull all fillna sort_values groupby apply reset_index drop
apply apply apply
min max astype
DataFrame astype
pivot index columns values
items
DataFrame var
size size size append apply
rename rename
iloc
DataFrame itertuples index
columns mean round mean round round abs dtypes
assign astype astype merge drop
isin loc
to_html
where eq
DataFrame shape index index drop between_time index values index all values index index isin index
DataFrame apply head
shape
DataFrame size columns DataFrame size columns
groupby columns columns sum
sum
columns values append drop
groupby index agg first last min max
drop index
sample columns apply join values columns
DataFrame columns DataFrame columns values
apply
sample sample reset_index drop sample groupby sample index mean
DataFrame
cumsum
drop_duplicates set_index
first count last count count append count append count
cumsum astype cumsum astype
columns pop columns
DataFrame DataFrame
dropna
apply
value_counts gt rename_axis reset_index
gt groupby mean mul
append combine
groupby apply mean groupby apply mean
merge combine_first columns
set_index columns columns groupby count ge all any bool loc columns columns join reset_index
where isin where isin loc isin loc isin isin isin values values
plot min max values values values
first
iloc append iloc first append iloc
stack reset_index columns
loc agg min max
xs columns DataFrame where fillna ne fillna index index columns columns
fillna shift add fillna shift add select_dtypes loc columns fillna shift add
diff rolling sum
min values min values
stack value_counts
merge
mask mask any mask mask any mask mask any mask
DataFrame
DataFrame apply stack columns count pivot_table index columns values count sum max idxmax sum join apply
merge combine_first combine_first columns
groupby apply value_counts reset_index pivot plot
merge dropna all fillna
values sort_index values values expanding apply sum size values values values sort_index values values expanding apply sum values values
set_index groupby transform isna dropna all reset_index
cumsum cumsum
iloc bfill iloc fillna
groupby apply rename merge
where cumsum bfill
DataFrame index index sort_index rename columns join
DataFrame columns median loc median sort_values median iloc median iloc
DataFrame set_index replace
shift
groupby apply min max min index apply groupby apply min max min index insert sort_index reset_index drop
merge fillna astype
at index
groupby apply set_index isna sum sum set_index size astype reset_index set_index isna groupby sum sum set_index groupby size columns size astype reset_index
apply DataFrame columns
columns replace
loc groupby size
astype astype iloc where astype astype where astype astype
replace
groupby transform first loc duplicated ffill astype duplicated mask groupby transform any loc mask groupby loc mask groupby transform first drop
loc loc copy copy
transform isna filter loc columns loc columns transform isna
loc
add append append DataFrame columns columns to_excel
DataFrame
loc isnull loc isnull
iloc
DataFrame index columns
DataFrame DataFrame
loc eq isin eq mask eq isin eq bool
query groupby mean mask groupby mean
melt set_index stack reset_index rename columns
groupby transform count groupby transform count
loc mask loc mask assign all columns loc mask mask index index loc mask
apply idxmax loc
DataFrame index columns iloc index drop
set_index loc keys swaplevel sort_index
get append DataFrame columns
DataFrame astype where astype where
sort_values groupby transform first sort_index
columns items DataFrame columns append columns iterrows items loc loc at reset_index drop rename columns mode mode
append DataFrame columns
min
index index values index astype astype astype
groupby rename rename mean
T columns columns T plot transpose T plot plot T
all append add append all columns max columns DataFrame head to_csv index
iloc all DataFrame size iloc all melt iloc reset_index index query sort_values index
DataFrame DataFrame melt melt diff merge diff diff diff
set_index index set_index index columns get dropna reset_index
DataFrame items columns reset_index drop rename
groupby apply shift bool shift shift groupby apply reset_index drop sort_index
groupby transform min astype
DataFrame copy index
DataFrame mul mul apply sum mul apply any
groupby apply quantile
columns
DataFrame columns loc DataFrame columns loc index index reset_index drop reset_index drop
astype assign
join replace replace apply replace apply apply replace join join DataFrame apply
pivot_table index columns values mean stack
loc sample iloc loc index
DataFrame DataFrame apply sum iloc apply reset_index drop copy index iloc iloc iloc equals
DataFrame plot style
values loc apply merge loc apply
values T mask any
DataFrame apply
columns merge apply ge
DataFrame
values values values isnull astype
apply
T assign shift shift T
groupby apply sample groupby apply sample groupby apply sample
pop append groupby rolling agg
DataFrame columns values pivot_table index columns values values reset_index index columns columns columns join dtypes convert_dtypes to_string
index loc
set_index loc
DataFrame columns
apply values values
drop columns drop columns
duplicated notnull
DataFrame values stack index DataFrame values stack groupby value_counts unstack
groupby sum reset_index sort_values drop_duplicates last
DataFrame columns DataFrame columns
mask loc mask loc mask
DataFrame to_csv index
DataFrame DataFrame values index values apply sum join
DataFrame index index columns columns mask ffill style apply DataFrame index index columns columns mask ffill style apply
index
transpose to_excel index
where astype astype astype where
round round columns loc loc apply
drop_duplicates last reset_index drop
columns columns unstack iterrows
iloc groupby idxmax values sort_values set_index resample asfreq reset_index drop iloc groupby idxmax values ffill bfill dropna drop columns astype shift shift
groupby sum
where median
index min index max DataFrame index min index max DataFrame groupby apply
columns columns columns columns replace columns columns replace columns columns replace
DataFrame columns index
value_counts
pivot_table index columns values values sum set_index index loc sum loc sum
iloc
empty DataFrame groupby describe reset_index
T T stack astype sum
astype astype
DataFrame index DataFrame DataFrame values columns columns index sort_index reset_index drop bfill ffill sort_values mask loc loc apply replace loc mask loc mask apply replace drop columns sort_values set_index drop columns index reset_index set_index index index min index max index isin index sort_index fillna drop columns get sort_index reset_index drop rename columns index notna set_index index reset_index set_index isna loc loc apply replace loc loc apply replace set_index index reset_index set_index sort_index reset_index drop sort_values shift first first
groupby transform first
index index
astype shift merge drop columns
drop gt groupby transform mean groupby sum astype rename columns reset_index gt groupby transform mean groupby sum astype rename columns reset_index
add reset_index merge merge where drop pivot pivot_table index columns values count add empty reindex reindex fillna add reset_index merge merge where drop groupby count groupby count reset_index pivot astype astype shape size size
eq product eq groupby product size reset_index count query groupby product size reset_index count product count
DataFrame sum
index groupby agg last dropna all
shift shift iloc
set_index filter replace groupby groupby size max max replace merge add groupby apply join
apply astype replace applymap
append append append DataFrame
columns where isnull columns
sample
where
DataFrame mask iloc mask all values min expanding apply DataFrame mask iloc mask size size index idxmin values loc index index min values append values values values append values
join columns items loc
DataFrame items columns groupby count
values values values
min max min
pivot pivot index columns values mean rename columns columns columns
DataFrame DataFrame combine
assign duplicated groupby sum astype count items count
groupby last to_dict index
max sub loc filter loc sub index
append index values DataFrame columns
DataFrame join astype values join values groupby apply
groupby apply notna isna shift notna columns filter drop drop
explode
loc ne shift
loc fillna
groupby transform size groupby transform size ge groupby filter items groupby transform size groupby
DataFrame replace replace bool
DataFrame apply
DataFrame apply
groupby
where fillna apply any where where
merge apply drop columns rename columns merge apply drop columns drop columns rename columns
columns columns
groupby columns
DataFrame sum sum DataFrame sort_values
values where index index
eq any bool mask loc mask
shape append join get
index set_index
eq astype columns eq astype all
pivot columns values assign groupby max reset_index drop columns DataFrame describe T
iteritems iloc iteritems
eq sum notna sum dropna eq dropna loc eq
add isin
groupby agg reset_index
loc
set_index to_dict set_index index
melt pivot reset_index rename_axis
loc sum sum sum sum shape index sum
rename columns set_index stack reset_index
DataFrame columns index loc
apply
loc
sample
agg min max min max
astype astype groupby agg sum count rename columns
max
astype groupby first reset_index groupby transform first astype
where gt shift shift loc iterrows loc loc loc loc loc
set_index update set_index reset_index
groupby agg min max join reset_index
DataFrame columns join
any
append groupby sum assign sort_values loc eq append sum groupby
replace
groupby apply set_index groupby apply
astype mask groupby agg reset_index
to_pickle
append DataFrame from_dict
reindex columns pivot_table index columns values mean round fillna columns reindex columns columns columns columns astype
DataFrame
append DataFrame append
apply min max DataFrame insert iat iat iat apply min max
explode index explode DataFrame values explode index join drop explode
update update update update update
columns hist
max assign
xs xs
describe describe DataFrame DataFrame append DataFrame
iloc groupby nsmallest index head columns set_index head columns
where ffill mask ffill
iterrows columns iloc columns
DataFrame values
set_index groupby apply to_dict
get items div xs product items append append product append div product product append div product product product append div product product append append div product product div product product product append append product append append append append append append DataFrame
apply DataFrame ne all
select_dtypes apply nunique drop index select_dtypes apply nunique reindex columns loc
index append
set_index index get
filter isin groupby plot
replace
loc idxmax
DataFrame DataFrame columns bool values columns values append sum bool append DataFrame DataFrame bool bool count sum columns index index index index index index
join
DataFrame apply apply
set_index columns columns stack rename reset_index rename columns
iloc
DataFrame columns idxmax apply explode groupby agg mean rename columns
DataFrame
DataFrame last any product replace join get apply join get
apply nunique
join pop join empty values astype bool
replace
all all all all all all
rename DataFrame rename rename DataFrame rename
iterrows
groupby agg count first query count sort_values count
stack
rename columns
diff
any iloc
first empty append join DataFrame to_csv
DataFrame assign take sort_values reset_index drop assign index drop columns plot groupby count unstack plot size groupby count get query drop columns copy reset_index drop
index iat
fillna max apply max values isnull
DataFrame loc where cumsum cumsum DataFrame loc values values
at first last
drop_duplicates value_counts
any DataFrame
merge columns drop rename columns reset_index drop
DataFrame shape size round shape iloc iloc columns
join join
merge drop groupby sum
groupby size count sort_values drop_duplicates last merge size loc rename columns
DataFrame index DataFrame index index append sort_index
replace apply to_csv
columns columns columns sum columns sum groupby apply join rename
loc
groupby mean reset_index values values
append
append
loc value_counts index value_counts columns value_counts index
set_index between_time between_time resample mean
max get
join apply apply apply apply
DataFrame groupby count groupby count melt groupby agg sum reset_index
sum value_counts sum value_counts mean std sum value_counts mean std
max apply style columns style apply size
items to_excel
size
iloc append apply
groupby apply values reset_index apply first last last get last
apply get
groupby transform loc quantile quantile mean describe loc quantile quantile groupby transform mean
replace first add replace replace
where
size replace where index isin sample index where index isin
join count values query query
replace index
add
loc
columns columns columns
all apply apply to_csv index to_csv mode index
first all transpose merge append
empty mean empty std shape shape empty rank values T corr rank shape shape shape empty shape shape T
apply where isin
set_index index isin merge DataFrame columns
columns mask apply values values mask
apply loc
columns columns
loc index replace index
DataFrame fillna DataFrame
pivot_table index
DataFrame index get
query set_index assign
copy set_index replace apply join dropna reset_index
mask eq shift ne mask
DataFrame size mean std kurtosis mean std mean kurtosis
DataFrame DataFrame to_excel index to_excel index DataFrame DataFrame to_excel index to_excel index
DataFrame to_excel index
set_index reindex reset_index
groupby shift eq loc
merge isna isna sample sample sample join merge sample sample
loc isin sum loc isin sum
groupby apply astype mean mean apply
sample DataFrame all dropna
empty values append append sum DataFrame columns get
where
loc
loc loc apply apply apply ewm mean ewm mean loc loc index apply astype shift apply apply apply apply astype apply
pivot reset_index groupby
apply
columns columns columns set_index stack rename reset_index
keys size iloc
index index iloc
append append append
columns columns columns
mask where
reset_index drop reset_index drop index index
groupby filter mean
columns
DataFrame columns groupby apply set_index rolling count reset_index
value_counts rename index
index reindex index fillna apply drop stack reset_index drop reset_index drop set_index groupby index append set_index groupby index append set_index groupby index append all index join join reset_index drop
max mean values
isnull ffill
DataFrame columns columns DataFrame columns columns
merge
join pop stack reset_index drop rename reset_index drop
sort_values mean rolling apply
melt drop sort_values columns melt drop sort_values
loc
unstack sort_index T columns columns join
cumsum cumsum cumsum cumsum
index index
get DataFrame columns
apply
isna sum index index
DataFrame
agg values T first
assign count pivot_table index columns values count sum
items keys get append
astype replace
replace replace
DataFrame DataFrame
append
index resample sum values get values index index get values index resample sum index index merge sort_index
columns groupby all columns sort_values diff le any duplicated loc mask isin mask
DataFrame
apply apply
reset_index diff diff diff fillna
sort_values sort_values
dtypes sub
apply
DataFrame values values columns
append
set_index groupby columns append reset_index set_index columns groupby
DataFrame DataFrame merge explode reset_index explode reset_index index merge explode reset_index index merge loc reset_index index merge drop columns index
groupby
groupby join shape copy join join
loc loc loc loc loc loc loc loc loc
add sum add append
where
append append append DataFrame
ne count sum
head T reset_index T reset_index drop columns head
index append append
values
replace groupby transform fillna mean
merge idxmin append
join columns any apply DataFrame join mean std apply mean std mean std apply mean std
plot plot size
values index
mask index mask drop
count DataFrame count index values index values index values set_index
replace
abs rolling mean
ne shift cumsum groupby groupby add
groupby sum assign where eq groupby sum
DataFrame size iloc size reset_index size drop index
at at at get first mode append to_csv mode index
loc loc isin
index index iloc
where isin join set_index rename merge rename columns
isin
iteritems append append append append index rename index rename groupby apply join
merge columns reset_index rename columns index index
div DataFrame div div empty DataFrame first div DataFrame div div append
DataFrame
iloc index xs xs index xs xs
T plot T
isna loc apply
drop query index dropna drop query index query query
merge fillna
DataFrame value_counts nlargest to_excel
items items
groupby transform nunique reset_index drop set_index append unstack columns columns droplevel
DataFrame columns
append DataFrame
groupby apply groupby apply columns values groupby apply
append columns values append append
items
apply join join apply
copy set_index plot
sum rename_axis reset_index
astype astype
min max interpolate plot plot plot values min max at interpolate plot
mask loc duplicated index
apply bool bool
index iterrows first first
ne shift cumsum count groupby transform size count drop_duplicates loc eq count gt
astype astype
sort_values groupby transform shift replace ffill fillna astype
DataFrame size apply apply apply drop head
first DataFrame first
astype astype min max pivot_table index columns values sum pct_change
DataFrame set_index T reset_index drop
pop items reset_index drop combine_first reset_index drop
DataFrame from_dict append
apply explode groupby transform count groupby agg sum apply groupby transform count
DataFrame apply
iloc values columns DataFrame columns
set_index unstack columns columns reindex groupby fillna ffill bfill stack reindex reset_index
DataFrame DataFrame
groupby apply assign
iloc loc loc loc drop pop pop pop pop pop pop
abs stack stack iloc head index cumsum groupby apply reset_index columns loc loc
copy bool shape index columns
index values index values
shift cumsum
applymap
DataFrame columns assign count loc notna sum
cumsum groupby agg join
groupby apply abs ge all
DataFrame columns DataFrame columns add_prefix
any columns columns columns drop
empty isna rolling rolling rolling rolling
drop agg join dropna applymap T agg dropna agg dropna
empty empty shape sum
groupby aggregate first first first first first first first sum sum sum sum sum sum sum sum
DataFrame from_dict index astype loc
melt mean std stack mean std
fillna
loc eq loc eq groupby agg loc loc
mask mask mask mask sum apply
groupby apply
astype astype apply join merge
loc loc loc loc loc loc loc loc loc
groupby
DataFrame applymap
DataFrame columns index index fillna astype astype bool where astype sum
reset_index reset_index
columns head dtypes sum index set_index head index
mode to_excel
explode reset_index drop columns DataFrame columns
astype astype
empty append get div div div product DataFrame append all to_csv index
where
index iterrows index iterrows
DataFrame T to_csv T to_csv T to_csv mean std T to_csv mean std
loc loc
index xs groupby items loc
columns columns isin
dropna all rename_axis index columns reset_index reset_index reset_index dropna all reset_index reset_index reset_index dropna all index iloc droplevel iloc set_axis index reset_index reset_index reset_index
T items index index
var var columns var var filter var mean
groupby first
index iterrows min iterrows abs min min abs loc index loc index
get rank items rank rank update values items keys
items DataFrame from_dict
value_counts shape append DataFrame shape value_counts update to_dict append DataFrame values fillna
rank astype
set_index replace idxmin iloc idxmax min assign reset_index
DataFrame from_dict nunique groupby transform count bool sum sample replace
plot
filter
DataFrame nunique apply nunique nunique nunique sample sample
iloc groupby transform sum groupby sum add_prefix mean std mean std
astype apply reset_index drop astype apply
mask apply
values values
where astype dot dot where astype dot dot
DataFrame var var var to_excel groupby var
values index replace T T
loc
mask mask mask loc mask
first mask lt gt gt gt mask mask eq mask min values groupby idxmin loc
DataFrame DataFrame DataFrame append merge drop merge drop
shift shift shift ffill bfill index index
value_counts loc isin index
DataFrame columns count columns groupby apply reset_index count
sum sort_index index iloc
any
columns columns astype
merge where where
product columns all DataFrame from_dict index columns
isin copy
columns columns
select_dtypes apply any loc
loc
dropna
DataFrame sub sub apply sum
DataFrame columns DataFrame columns replace astype
index
tz_convert
set_index squeeze
DataFrame groupby size reset_index rename columns count count
fillna
merge mask apply loc mask assign merge assign drop drop reset_index drop
copy groupby agg loc loc groupby agg loc loc T groupby apply
append DataFrame
groupby rolling sum values index index groupby shift keys groupby keys rolling sum values index index groupby keys shift keys groupby keys rolling sum values index index assign groupby keys shift
first add iloc iteritems last values where
query
mask index index where ffill index index index copy apply apply columns shift fillna columns iloc columns shift fillna dropna all columns iloc dropna join astype reset_index rename_axis
groupby mean rename
DataFrame columns get to_csv
values groupby last reset_index drop columns
get
mul sum
groupby agg mode
T set_axis T columns
loc
copy copy apply apply drop
apply min max
set_index set_index eq sum loc
sum eq sum shift ne cumsum first all
pivot_table index columns values pct_change mean min max iloc DataFrame
values groupby transform sum where
assign
apply groupby sum reset_index
apply apply apply
DataFrame columns add groupby sum astype groupby apply join unstack reset_index columns
values astype groupby sum
min max index values keys DataFrame index index reset_index
columns eq any
replace iloc replace
index index empty where loc
join count count count
columns columns to_dict columns set_axis columns to_dict
loc
drop columns drop index first columns columns columns get columns columns columns drop last
where where where
DataFrame groupby sum reset_index max
reset_index merge query set_index index
median median
DataFrame
sort_values drop_duplicates last groupby lt copy set_index unstack rename columns columns columns reset_index
isin where isin where isin
DataFrame rolling max
DataFrame drop_duplicates
index groupby index agg
groupby rank sort_values groupby
all columns filter all product columns product filter product columns columns product product shape product product columns product
groupby eq eq
loc isna mean isna mean
join rename columns groupby sum reindex add_prefix
columns join eq
DataFrame
fillna groupby transform mean fillna groupby transform mean fillna groupby transform mean fillna mean
filter join columns join filter transpose reset_index drop astype columns iloc reindex index drop reset_index drop columns astype head merge columns to_json
empty
apply eq any eq
join rename columns rename_axis index rename_axis index join groupby mean reset_index
first astype astype astype astype first astype astype astype astype merge
merge rename groupby apply join reset_index sort_values assign
groupby count
replace join
groupby rolling sum reset_index rename columns merge
replace
loc
sort_values groupby expanding sum reset_index drop astype sort_index drop astype equals
apply loc values explode apply
merge merge
fillna stack fillna stack ne ne DataFrame fillna equals fillna
assign groupby mean
loc groupby shape transform first lt
DataFrame columns columns notna columns columns notna
sort_values drop_duplicates first
max set_index idxmax loc
columns columns
index iterrows append
all values isna any
apply
last iloc sum
explode value_counts loc index DataFrame apply
diff dropna iloc last index loc last loc last filter groupby apply
groupby index diff ne cumsum groupby index transform max eq assign mask mask first last max first count groupby index agg reset_index drop bool
sum
astype to_dict items join
melt append drop_duplicates
DataFrame replace apply
DataFrame
apply groupby value_counts idxmax apply
mask where mask fillna ffill fillna mask groupby mask mask shift cumsum transform cumsum values mask apply where mask
join
columns
min groupby shift groupby transform first astype
DataFrame size index add_prefix append diff dropna abs mean std loc loc loc abs mean std loc apply abs mean std loc abs loc values loc values mean std
DataFrame reset_index melt index query index max
interpolate interpolate
count groupby eq cumsum groupby eq cumsum apply cumsum take count groupby eq cumsum count transform max count take
apply
apply apply
all set_index unstack set_index unstack set_index unstack
astype fillna astype astype
astype groupby agg join join first reset_index astype groupby agg reset_index values take apply apply apply apply values take apply apply apply apply apply apply join apply apply join apply join apply join join sort_index columns columns columns columns fillna
DataFrame apply iteritems columns set_index
rename columns reset_index drop
columns columns columns stack reset_index drop loc columns columns append columns columns columns stack reset_index drop
columns columns shape
DataFrame columns index iterrows append append groupby value_counts unstack fillna
round
DataFrame index columns
index iterrows mask
shift cumsum groupby apply join shift cumsum rename groupby apply join sort_index
get all replace mode combine join DataFrame columns add add append copy all keys items all combine all reset_index drop keys first iloc
values sum
values shape shape
apply loc values mean std mask mask mean std
mean diff sub groupby transform mean std groupby transform std columns loc abs diff std std isnull columns drop diff std
DataFrame values append
DataFrame DataFrame DataFrame DataFrame
isna
DataFrame DataFrame columns index iterrows loc index loc index
DataFrame mean mean sub mean
abs iloc abs iloc
dropna all droplevel reset_index
rename items merge max values rename items dropna
columns eq any
style
DataFrame pivot_table index columns count values fillna astype
append
apply apply mean std
groupby agg mean
memory_usage sum memory_usage sum
merge merge fillna astype dtypes merge fillna astype dtypes set_index assign set_index reset_index merge fillna astype dtypes set_index index
DataFrame columns assign apply join assign groupby values transform count sort_values drop
at loc
DataFrame columns div where div gt loc columns mean div
iloc groupby sum iloc groupby sum
apply
duplicated sum
columns set_index update set_index reset_index reindex
transpose
columns
DataFrame eval empty eval empty merge eval
merge loc isna loc isna loc isna
values filter values abs size aggregate
copy
axes set_index iterrows axes gt sum index
DataFrame index assign DataFrame index assign keys rename_axis reset_index groupby apply resample sum
loc loc loc loc isin
DataFrame pivot fillna reset_index to_dict
eq sum
isin isin isin isin isin isin apply
pivot_table index columns count
groupby filter count groupby filter nunique DataFrame columns drop_duplicates last sub drop first index index drop
all keys
all DataFrame groupby drop columns to_dict index items items all
iat
melt
at append at
astype sum astype sum astype sum
groupby groupby DataFrame astype
loc columns
drop drop columns transpose drop T drop plot plot apply plot apply
DataFrame from_records columns DataFrame from_records columns merge
values
columns astype pivot index pivot_table index columns values reset_index columns merge merge
append
index index index
first replace mask bfill mask bfill mask
to_period groupby sum groupby sum
DataFrame columns
iloc iloc
append DataFrame columns append DataFrame
notnull notnull DataFrame apply
apply isna
groupby agg
DataFrame columns iterrows
stack stack
values max min shape max min shape
groupby index apply set_index index index columns
query
columns columns melt groupby agg mean var T mean var
astype values fillna index index
merge
insert loc items apply fillna
groupby apply index groupby agg first last stack loc reset_index groupby agg first last stack set_index append reset_index drop rename_axis DataFrame loc groupby apply reset_index groupby agg first last stack set_index append reset_index drop rename_axis
reset_index index reset_index index
agg join
count assign loc loc values groupby sum DataFrame count mean std
merge
items nunique
fillna fillna merge drop
append DataFrame columns
shift cumsum where groupby cumsum drop
DataFrame DataFrame
groupby groupby
drop_duplicates items
apply
groupby sum groupby sum reset_index
stack set_index unstack add_prefix join assign sum
loc index groupby groupby
join to_csv
apply replace
apply size size size join apply size size size
cumsum add
DataFrame columns columns columns isin isin columns
query groupby agg sum loc DataFrame query value_counts loc query groupby size loc
plot
at filter assign sum isin mean append plot
values DataFrame index add_prefix DataFrame index
cumsum groupby agg join groupby index agg join
apply apply
append keys keys
DataFrame columns columns join join apply mean std count sample rename columns sample pivot_table values count mean std index columns sample index all apply mean std count mean std count fillna DataFrame index index columns index fillna index all loc apply mean std count mean std count fillna
apply replace replace replace apply
astype all loc
diff
DataFrame DataFrame DataFrame DataFrame DataFrame DataFrame
DataFrame join first add empty columns iloc
nsmallest eval sum nsmallest eval sum
plot plot plot
shift shift where eq shift fillna
append all
replace to_csv index
get groupby index
groupby head groupby apply sample
mask eq mask
get get index apply
DataFrame groupby count reset_index columns
DataFrame explode
sum groupby transform sum drop_duplicates groupby sum
iterrows iterrows
groupby add where
shift shift eq loc shift
astype at
applymap
groupby transform
apply
DataFrame columns groupby first to_dict fillna
info shape shape info shape shape dropna dropna DataFrame dropna shape shape dropna
DataFrame add any apply
append DataFrame columns
DataFrame isin sum isin sum columns
set_index combine_first set_index reset_index
T reset_index index index replace iloc iloc astype groupby index sum
groupby agg first first first sum sum reset_index
groupby agg DataFrame apply index index join
index
groupby plot
DataFrame join groupby transform sum groupby transform count apply to_string index DataFrame groupby transform sum groupby transform count apply DataFrame loc isna join loc isna join fillna explode drop pivot index columns values reset_index fillna merge
DataFrame apply rename columns
DataFrame index columns filter index columns set_index filter index columns insert columns index
DataFrame DataFrame
join groupby count sum reset_index join
to_csv
apply columns apply value_counts apply value_counts mean std stack groupby value_counts unstack mean std
astype bool
DataFrame columns
columns mask eq mask values columns mask mask
loc loc
apply eval explode DataFrame to_dict groupby index agg
DataFrame columns iloc
pivot plot loc
columns rename columns loc shift iloc columns columns rename columns
apply
melt index pivot_table index columns values sum fillna index
sort_index values index
groupby filter
all dtypes
rank itertuples index append rank items DataFrame T reset_index index apply
apply index index
keys
filter columns set_index join groupby sum
mask loc mask idxmax
groupby apply to_dict reset_index groupby apply to_dict reset_index groupby apply to_dict reset_index merge to_dict
values DataFrame T dot index columns columns columns DataFrame T dot values columns
values columns columns astype
size idxmax iloc iloc groupby shift cumsum groupby shift cumsum last last last first nlargest iat last loc index iat
diff sort_values diff
values join get
DataFrame columns groupby shift groupby shift isna isna astype
astype astype astype astype index get get min max min max min max bool mask get values replace where mask update first columns
loc append loc columns rename columns dropna sort_index
items
iterrows loc loc loc mask loc mask where mask
DataFrame columns append append
replace replace assign apply replace
notna all bool notna all bool
reset_index index cumsum index diff ge cumsum all set_index index groupby apply reset_index groupby index apply max min
ne shift cumsum groupby groupby agg first join min max
loc loc loc lt eq
astype astype set_index astype astype set_index index isin index sub loc index sub sub loc loc index
insert append
var DataFrame columns append keys
mask ffill
loc loc
where index where
melt loc loc merge get loc DataFrame groupby max loc
DataFrame min max groupby agg reset_index rename merge
apply
iloc to_json iloc to_json
clip where
values append astype
set_index resample mean add_suffix
groupby abs transform sum eq
abs groupby first idxmax index max loc get at
astype bool sort_values index
loc
columns index index columns DataFrame index columns index index columns iloc
merge fillna
size head head to_html head head style align bool
loc duplicated
DataFrame from_dict index T columns index iterrows where first columns index shape loc index
DataFrame columns index columns T append reset_index drop
append append append append T DataFrame DataFrame columns
items items copy DataFrame var
index tz_localize index index
values DataFrame isin any where drop first to_dict first to_dict
values values DataFrame merge
iterrows append iterrows append
groupby set_index groupby index groupby
resample max reset_index groupby index agg min max head
lt isna ge cumsum where diff groupby
reset_index drop join rename_axis index set_index append
append append
DataFrame assign replace replace fillna assign replace replace fillna
shift
index rename set_index append swaplevel
columns
values values keys apply
where
combine combine apply astype astype
var var bool empty bool any all empty bool any all empty bool any all empty bool any all empty bool any all empty empty bool bool all any
astype
apply
DataFrame index columns loc loc copy loc
DataFrame loc empty append to_csv
DataFrame columns loc
DataFrame DataFrame columns columns DataFrame T columns
groupby count nunique rename
count fillna groupby apply append count replace reset_index drop
melt sort_values
size DataFrame columns head groupby DataFrame
apply replace
groupby
index
stack index reset_index drop
drop_duplicates groupby drop keys all keys all
set_index update set_index reset_index
query query query query query query query query query query pivot_table index query columns first query query columns query query
replace
values
append items to_csv
columns columns index set_index update set_index index reset_index
drop_duplicates loc eq rename columns drop columns merge loc eq rename columns drop columns merge
groupby transform sum div
DataFrame get items append apply keys append apply apply keys
assign groupby merge assign groupby drop columns
DataFrame at head apply head apply
groupby groupby empty replace iloc iloc to_csv index
columns astype sort_values take first groupby first drop astype sort_values groupby first drop
values index columns values
loc groupby idxmax reset_index drop loc groupby idxmin drop set_index astype astype
groupby groupby sum all shift cumsum fillna astype index index
DataFrame columns astype dtypes
DataFrame rolling median add shift median shift all where median where sum columns columns sum shift median shift all where median where sum columns DataFrame size rolling median size
loc any loc isin any loc isna any
sort_values groupby apply count div count shift sub join dropna duplicated sort_values count count div count shift sub eq shift drop
loc sum
index iterrows
apply
to_excel
join
groupby astype radd pivot index columns droplevel
replace replace replace
iterrows get
columns fillna
duplicated eq
groupby product transform diff ne cumsum where diff ne insert transpose T insert insert loc index T reset_index drop
axes squeeze axes
dot replace
dtypes apply
columns DataFrame columns DataFrame columns DataFrame columns any any index index itertuples count loc loc loc loc loc loc index append loc index
shape DataFrame columns shape drop columns loc shape
join
index replace
astype any loc
sample index DataFrame index all values values values append values values
DataFrame
groupby transform sum
get mean mean mean get std std mean std mean std min max min min max min
merge values values groupby loc idxmax values
columns columns columns values
pivot_table index groupby columns values join rename_axis columns sort_index
replace DataFrame items merge
set_index append count stack
DataFrame columns
min append append loc append loc values
groupby filter isin all DataFrame
reset_index assign index sort_values groupby pivot index index columns values
DataFrame
sample DataFrame set_index groupby sum
astype shift astype columns astype shift astype apply
astype assign columns
DataFrame columns set_index sort_index
isin values index
eval eval eval apply
stack round astype add mask isna unstack
replace astype index index astype stack value_counts
eq groupby transform mode max drop_duplicates reset_index drop groupby transform mode max eq groupby transform mode max bool eq groupby transform mode max eq groupby transform mode max drop_duplicates reset_index drop
columns DataFrame join reindex columns columns columns DataFrame columns iteritems loc loc
size index resample mean plot index
apply sum
get
groupby apply dropna DataFrame
merge
apply first_valid_index last_valid_index set_axis
DataFrame columns index loc sample loc sample sample sample sample keys astype bool append sum sum
iloc iloc sub set_index applymap mean std set_index sub mean std
replace replace
pivot index columns values columns join columns columns set_index update DataFrame DataFrame pivot index columns values columns join columns columns set_index update
DataFrame DataFrame
groupby transform sum
empty DataFrame columns DataFrame size columns add add sum transpose get T T to_csv
DataFrame index index index iterrows index iterrows apply last at index index at index at index index iterrows at index apply
columns loc insert sum insert ne sum
join product
DataFrame values DataFrame
groupby apply iloc reset_index drop iloc reset_index drop index set_index index
pivot fillna reset_index columns values pivot fillna reset_index columns values
dropna
DataFrame columns
DataFrame columns index
boxplot
melt pivot_table index columns values size
iloc all columns rename columns rename columns set_index apply head reset_index groupby sum index to_csv join index
index
isin
where where
index
columns rename columns columns columns
DataFrame columns shape DataFrame columns set_index drop plot items head values T
round abs round abs round abs round abs round abs
ffill assign shift cumsum groupby ffill assign shift cumsum groupby apply
loc
where eq bfill ffill fillna
append
items loc mask isnull all fillna
stack reset_index rename columns
to_csv
DataFrame all loc sort_values drop_duplicates
DataFrame DataFrame columns round items update
drop reset_index drop drop
abs all drop columns abs all
copy
values values merge merge
where
groupby idxmax loc
groupby DataFrame
apply loc drop_duplicates last drop columns
shift
groupby
mask eq eq loc eq replace
set_index squeeze to_dict empty columns fillna
eq eq
DataFrame astype apply
DataFrame
DataFrame fillna columns loc columns loc loc apply loc loc loc loc
loc ne any
bool append any all apply values bool
get where ne shift index index style apply
astype set_index plot tail
join
DataFrame loc groupby size nlargest all reset_index columns
fillna
where index
eq ne groupby cumsum cumsum add ne groupby cumsum add
copy columns index min index max resample pad
merge DataFrame
DataFrame DataFrame set_index drop set_index drop join set_index drop set_index drop fillna join drop_duplicates
set_index unstack columns columns reset_index
values replace
index index index index max empty max empty index index itertuples mask index index max mask max mask max
columns
merge
shift bfill cumsum rename groupby agg first last reset_index
insert apply
append DataFrame DataFrame apply
merge groupby apply to_dict reset_index to_json merge groupby apply to_dict reset_index to_json DataFrame DataFrame merge columns groupby apply to_dict reset_index to_json
where astype mask where mask mask astype
values DataFrame values index resample median DataFrame values groupby median
filter filter loc copy DataFrame DataFrame first groupby copy merge rename columns DataFrame DataFrame get merge
groupby value_counts reset_index set_index groupby agg idxmax max idxmax max join rename columns set_index groupby agg idxmax max idxmax max join rename columns mask duplicated
at eq iat at eq iat at eq iat
DataFrame apply notna DataFrame
isnull isnull any bool
append index
items
value_counts value_counts items items sum iloc
apply
shift bfill cumsum groupby transform count shift where apply fillna
DataFrame columns
apply min max apply min max
copy values assign values last loc index isin index
where where eq any min
values first copy copy copy
groupby sum unstack pivot_table index columns values sum
reset_index drop groupby
groupby agg first first first sum sum
merge
DataFrame apply
lookup index lookup index
apply apply bool DataFrame index eq any bool values astype T any index bool join mul any bool values astype bool index bool
count apply
append values
DataFrame columns DataFrame columns iloc iloc iloc reset_index drop
eq drop columns merge assign div astype drop merge drop columns
DataFrame columns DataFrame columns DataFrame columns DataFrame columns
DataFrame to_csv empty empty DataFrame to_csv DataFrame to_csv empty index empty empty
groupby describe count rename columns count loc groupby describe count rename columns count
append
get values values index index add items loc replace values values loc astype loc astype columns style apply to_excel
eq dot columns assign where ne add isna dot columns assign where ne add
DataFrame columns where where fillna fillna mask
DataFrame groupby apply eq mean groupby apply eq mean mean std eq groupby mean mean std groupby agg nunique groupby agg nunique groupby agg nunique DataFrame sort_index compare sort_index empty
pivot index columns values abs mean mean std std reset_index rename_axis
replace replace
filter filter astype bool
all query query
DataFrame columns loc columns columns loc
astype groupby agg mean std columns join columns values
set_index sort_index
DataFrame apply to_string apply groupby last transform last combine_first combine_first pivot_table index columns values first reset_index merge agg first last columns rename_axis to_string index
iloc apply dropna to_dict mask iloc isnull any mask iterrows mask iterrows isnull index replace values append copy fillna T
DataFrame DataFrame replace replace replace apply apply apply apply merge
stack value_counts
sort_values
DataFrame
loc isin reindex columns fillna loc isin
iloc
droplevel set_index append astype join div round unstack drop
items items items append append append append DataFrame pivot index columns droplevel rename_axis columns
size index diff add first index last index shape iloc plot iloc plot
DataFrame DataFrame
replace replace
items
apply eq eq apply insert update drop
T sub T count drop_duplicates value_counts count drop_duplicates value_counts add add astype count drop_duplicates value_counts count drop_duplicates value_counts add add groupby apply unstack fillna astype
xs columns columns xs columns columns insert insert insert
shape shape
any
values
DataFrame index index T DataFrame index index T DataFrame index index T DataFrame index index T
product product product get attrs div attrs div attrs div attrs append append append DataFrame items to_csv
sample apply apply set_index sample values apply apply explode columns rename apply rename columns diff values apply apply explode columns rename apply rename columns merge all columns merge diff assign diff diff diff diff diff
DataFrame columns DataFrame columns loc isin columns loc isin
to_csv index dropna to_csv index iloc where isnull
index DataFrame
index loc
loc idxmax loc idxmax
apply apply DataFrame DataFrame columns DataFrame join
DataFrame insert values insert columns apply insert columns apply
join join astype values
DataFrame DataFrame transpose where T
isin
T T values
set_index dropna columns rename_axis reset_index DataFrame
all columns columns columns all all columns columns reset_index drop rename columns iloc first drop index first iloc iloc to_csv iloc iloc
merge
drop_duplicates groupby value_counts unstack rename columns
append
at
select_dtypes columns dropna count all
groupby index reset_index pivot index columns rename_axis
columns astype where mask mask to_string index iterrows append append columns values astype at index iloc index index iterrows index loc index
mul rmul DataFrame mul DataFrame DataFrame
merge keys values mean mean get columns groupby agg reset_index merge
columns copy
prod prod columns
mask mask
abs all all apply
get get get DataFrame columns
drop replace columns iloc drop
count
apply apply
add
join DataFrame columns fillna
DataFrame from_dict index items DataFrame
sort_values astype iloc astype
index rename rename index reset_index index
shift gt groupby cumsum transform idxmax
loc DataFrame loc
merge
DataFrame DataFrame DataFrame isna append append append append
applymap apply join all empty join to_csv sample index
loc isin eval isin loc
set_index unstack T columns columns set_index unstack columns columns
filter join assign join
sort_values sort_values sort_values
columns columns apply apply any
diff loc astype shift loc
min apply sum assign min fillna min min min min
index
T
DataFrame shift min max min shift shift max shift shift min max
iteritems iloc loc values apply
index values items values
apply mask isna notna
min
loc apply drop groupby agg sum size index
columns columns loc mean columns iloc mean
where shift shift shift shift shift shift shift shift shift shift shift shift
ffill bfill
apply
at
pivot_table index columns values reset_index columns join columns values
replace astype
groupby diff gt astype lt astype rename
abs cumsum DataFrame T abs mask where mask max assign mask shape T mask T abs T cumsum DataFrame T
where all bfill count isin index all all all all all all all all all bool
groupby sum reset_index rename melt astype sort_values
DataFrame add loc values
keys replace
melt query mask eq set_index stack reset_index
DataFrame columns index DataFrame columns index merge drop_duplicates mean std drop_duplicates first mean std isin mean std merge drop_duplicates mean std
iloc dot columns iloc values columns values join
loc index isin copy index isin drop drop index index isin
set_index sum groupby transform sum
groupby sum
columns filter index filter filter filter filter filter filter filter filter filter filter
head
sum
drop astype pop set_index stack reset_index rename columns
to_excel
min max reset_index DataFrame drop_duplicates reset_index set_index reindex
DataFrame loc loc astype
iloc iloc
DataFrame columns iterrows
mask mask plot
explode index index
keys DataFrame append DataFrame append sort_values
DataFrame
columns iloc add_suffix mul iloc shape
values
DataFrame columns index DataFrame columns index to_xarray
add columns to_excel index
apply
update eq
mode
DataFrame astype astype drop_duplicates first drop
groupby apply cumsum shift
merge div groupby transform size
loc isna sum resample apply pct_change sum mul
apply
sub DataFrame columns first all columns agg first columns get all agg agg groupby agg agg reset_index rename columns assign merge drop columns merge DataFrame values drop columns to_string index
apply apply apply apply replace replace
eval
columns values append count DataFrame columns
eq iloc groupby cumsum ne mask
columns columns columns append join
merge merge
groupby size count count pivot reset_index pivot index columns values count plot count index groupby size reset_index count pivot index columns values count plot count index DataFrame groupby size reset_index count pivot index columns values count plot count index
reset_index
merge loc merge loc
ge
replace
astype columns loc astype columns loc
sort_values drop_duplicates set_index
loc loc
index index DataFrame index
loc iloc join iloc DataFrame values T
query query get count append DataFrame
set_index set_index update reset_index join set_index merge merge fillna pop
apply replace replace
groupby transform max astype groupby max to_dict astype
replace replace fillna mode iloc
groupby apply columns iloc columns columns get stack reset_index merge pivot reset_index drop T merge drop set_index T reindex columns columns
index index apply DataFrame rank rank round rank
values values
DataFrame values columns index index columns columns isin
lookup index
values
get min max min max merge DataFrame groupby groupby apply merge drop reset_index fillna columns groupby ffill
DataFrame itertuples itertuples join
shape
count
value_counts sort_index
align
isnull mean
reset_index drop rename_axis reset_index
apply apply pivot_table values index columns columns columns
groupby transform where index index max
DataFrame set_index
size query index index query size DataFrame
pivot values index columns
groupby agg sum count sum count reset_index
add apply get
DataFrame DataFrame DataFrame
to_sql append
DataFrame axes groupby plot axes axes axes axes plot axes axes
DataFrame
columns columns
pivot_table values index columns sum fillna pivot_table values index columns count fillna
values all at max max update DataFrame items columns
merge pop eq
apply join DataFrame index index join DataFrame apply index index
set_index unstack
where
DataFrame index index fillna astype reindex columns rename columns join
query sub
plot plot
where groupby mean
DataFrame diff diff where diff drop columns diff
merge merge drop columns
where
loc sum loc bool bool notnull bool notnull bool notnull sum
iloc iloc eq cumsum astype
reset_index pivot index columns values rename_axis
columns size
append
DataFrame columns apply dtypes
explode all columns astype
pivot_table index columns
DataFrame apply
DataFrame index index
eq isin isin isin
index
columns loc values
iteritems append append itertuples DataFrame sample sample where iteritems append append append append
DataFrame
eq eq eq eq where eq
where isin
loc
DataFrame from_dict index columns
columns apply
set_index stack rename_axis assign reset_index
join DataFrame replace
round groupby apply
pivot_table index rename columns rename values mean
set_index index get fillna
notnull mask apply apply isin sum mask mask to_csv index mask apply isin sum mask mask apply isin sum mask bool
DataFrame columns first columns iloc first iloc reset_index drop
assign groupby transform any
values values index loc index min
DataFrame groupby count
values append values
eq groupby nunique merge fillna
index where first max index all at reset_index drop
shape iloc index iloc index iloc dropna all
replace replace
DataFrame index index index
any apply
sort_values shift loc isna shift sort_index
any values
index eq
columns iloc
loc isin groupby transform all ffill
loc dot transpose transpose DataFrame index index columns columns loc values
bool
columns max info plot
filter columns columns columns values isnull values DataFrame DataFrame
loc loc
DataFrame merge
loc at at
mask all any index mask
filter stack value_counts index
apply
DataFrame append DataFrame append
columns sample columns groupby median get groupby index reset_index
groupby describe index index reindex count mean std min max
iloc
items copy DataFrame values items copy
where DataFrame loc
apply replace
DataFrame DataFrame isin values shift values DataFrame isin values shift values
DataFrame columns groupby transform nunique pivot_table index columns values fillna
DataFrame columns columns
values T
mask loc mask query
where where where where
columns iloc
apply add_prefix apply add_prefix rename columns
groupby agg max
rename columns merge merge
groupby loc loc
groupby transform mean
pop DataFrame pop
loc
empty DataFrame
DataFrame
loc apply
index index index index copy
to_period columns to_period to_period columns to_period
DataFrame
iloc reset_index drop iloc reset_index drop join
join append join rename columns
DataFrame rename_axis index columns DataFrame rename_axis index columns
sort_values iloc columns iloc columns loc index loc index
rename columns sort_index
set_index to_dict to_dict size DataFrame DataFrame set_index loc apply mean std to_dict mean std loc values mean std
DataFrame from_dict index reset_index rename columns index apply groupby max groupby nlargest reset_index
first
groupby shift sub fillna assign
replace join
idxmax idxmax any
groupby rank first
sort_values reset_index drop append
eq index groupby cumsum agg first last
append append
values
loc loc apply loc
DataFrame items astype
apply apply apply combine_first drop columns
assign assign
apply
replace sample drop_duplicates groupby agg sum sum sum sum columns columns reset_index merge
mask where mask loc mask
fillna values values values values
groupby agg to_dict
sum
apply
to_dict index
merge
fillna
isnull all all isnull all all columns isnull all pipe
get replace append DataFrame columns head
first drop_duplicates set_index last drop_duplicates last set_index
get DataFrame
DataFrame DataFrame join
index T items columns index
index iloc iloc index index iloc
DataFrame columns last DataFrame columns last groupby size set_index index get
where isnull
DataFrame DataFrame DataFrame DataFrame DataFrame columns merge rename columns merge rename columns merge rename columns merge rename columns
attrs
assign merge assign query drop
apply apply
groupby pivot index columns values rename columns reset_index drop reset_index isin loc loc
values values
reset_index reset_index div reset_index set_index
columns columns where notna columns DataFrame columns info columns info
shape
loc
join
items insert append append
sort_values head
pop min max min max
stack index index to_dict
append append loc
DataFrame values index size columns columns index index index
lt groupby transform all
fillna ffill
first replace sum abs get where replace copy apply
where ge sub add
drop
count values index
get DataFrame
assign eq
drop drop index groupby mean
isnull isnull
loc index loc DataFrame index loc index loc append
groupby agg min max
DataFrame T DataFrame index DataFrame from_dict index mean std DataFrame from_dict T mean std
eq max at at
where astype replace replace astype bool astype
sort_values cumsum mask value_counts mask copy sort_values cumsum mask value_counts mask copy DataFrame
groupby transform where drop
apply to_csv index
index join index index index to_timestamp index index index replace index index to_timestamp
melt
columns apply
apply
DataFrame apply
groupby groupby index sum loc query index query
append
apply where join apply where drop columns
groupby transform nunique
pivot_table index columns values reindex index columns
loc groupby index idxmin loc groupby index idxmin value_counts loc groupby idxmin value_counts
DataFrame dropna any where any all any all any any values drop all all values drop values columns DataFrame
sum sum max max min min all all any any sum sum sum mean mean mean prod prod prod std std std var var var median median median max max max min min min cumprod cumprod cumprod cumsum cumsum cumsum get aggregate
shape shape shape shape shape diff astype all any groupby apply bool sort_values shape shape shape shape shape diff astype DataFrame all
replace
DataFrame bool dtypes to_dict dtypes to_dict keys keys keys keys update update reindex columns astype DataFrame
apply
DataFrame columns DataFrame columns set_index set_index mul
plot
iloc index index apply assign merge assign groupby size reindex fillna rename reset_index mean std index apply mean std DataFrame merge mean std mean std
append DataFrame
DataFrame columns replace
shift cumsum shift cumsum ffill fillna astype
dropna iloc apply pop items reset_index drop join reset_index drop columns columns
explode index explode DataFrame values explode index join drop explode
groupby transform count copy add copy sub
DataFrame columns
DataFrame DataFrame columns iloc style replace
rename columns columns rename columns dtypes bool rename columns dtypes index to_sql replace
at
set_index reindex groupby transform ffill bfill reset_index
agg idxmin idxmax unstack reset_index merge idxmin idxmax idxmin idxmax idxmin idxmax idxmin idxmax
DataFrame DataFrame
apply apply apply
columns hist hist
pivot_table index columns values assign sum reset_index dtypes
groupby apply iloc size
value_counts apply
where groupby
to_html
apply mean std mean std
items
bool where apply isna where any apply
apply
DataFrame columns loc
set_index index between_time filter reset_index index
append append assign
sample columns copy columns plot all assign plot boxplot append
join DataFrame index
DataFrame all dropna all dropna rename columns merge loc isin reset_index loc count get count merge loc isin reset_index loc count loc count append
to_sql replace
groupby shift groupby shift groupby agg count
columns sort_values drop count count count count count count append count count count count columns groupby size reset_index count to_csv index mode
shift bfill astype loc astype astype pivot_table index columns count values rename columns count rename_axis reset_index
groupby apply groupby columns apply drop_duplicates reset_index drop reset_index drop fillna
join columns
merge query groupby columns mean
all columns values get DataFrame columns
all bool all
index columns index columns index columns columns index index columns index reset_index rename_axis columns index reset_index
DataFrame where diff
iloc iloc where le iloc iloc clip
get sum lt lt values columns loc mask gt set_axis mask loc
apply join where apply join where replace
reindex columns
loc isnull loc isnull apply
groupby filter nunique
apply
copy copy
take transform add DataFrame assign DataFrame assign all pivot_table pivot_table index values columns
reset_index groupby index diff index bool
min max
replace dropna
append
unstack idxmax to_dict
explode
gt shift lt shift index index loc drop_duplicates
columns T loc dropna columns
index loc loc
loc
DataFrame bool
merge rename reset_index merge prod prod get groupby agg sum sum clip
values astype values astype values astype values astype
mask groupby eq cumsum transform any loc mask loc mask fillna
groupby apply join reset_index
where groupby cumsum where groupby ffill fillna
DataFrame columns isnull any columns take columns
groupby transform notna cumsum sample groupby
mask max min where mask
merge iterrows isna append append DataFrame columns to_excel index
to_dict
where where
explode astype set_index append loc index columns
merge mean mean
index loc T values groupby DataFrame columns
join
apply any where values drop DataFrame loc loc columns columns append columns dropna columns update update
set_index set_index loc update loc
groupby apply sum groupby apply sum sum groupby nunique groupby apply groupby nunique groupby apply groupby nunique
DataFrame DataFrame
groupby shift fillna groupby shift fillna
max abs min abs min
index iterrows mask filter mask index index loc mask
diff shift diff DataFrame loc diff loc diff diff
loc copy rename columns
get
loc index index iteritems append loc index copy index at at at
join values
DataFrame columns sample to_csv set_index rename columns
set_index values iterrows values append DataFrame index index
loc
all columns iloc all iloc all columns
loc
DataFrame apply
DataFrame astype astype shift apply
agg pivot_table index values count rename columns reset_index groupby count rename columns reset_index agg merge size
loc idxmax loc idxmax
eq isin filter isin any
plot iloc iloc iloc iloc
DataFrame where values columns columns loc loc apply astype values values values values values astype astype astype loc loc apply
pivot_table index columns values
apply replace
head values head values bool
dropna
groupby size groupby size reset_index
set_index set_index apply index merge
index
shape mean DataFrame values shape
groupby apply to_dict reset_index to_dict set_index groupby apply to_dict reset_index to_dict
groupby diff fillna cumsum
astype div
set_index index
apply keys
append loc
iteritems index
values values apply add_prefix apply add_prefix
groupby count transform mean std
groupby agg apply lookup groupby agg lookup
loc get loc get loc size
groupby agg count reset_index
replace ffill drop_duplicates last sort_values update
columns groupby columns sum
index notnull rolling apply loc min loc min dropna rename join fillna
loc iloc
eq sum idxmax empty get last index shift eq loc empty index empty
columns
DataFrame append
DataFrame columns DataFrame columns DataFrame columns keys
where where keys values columns columns shape columns shape columns shape items values values values values count items
groupby transform mean groupby transform mean groupby transform mean
loc columns columns
ne idxmax first
loc count var
apply fillna mask isin astype mask astype apply astype mask applymap astype
set_index iterrows at at set_index assign iterrows at at at at
value_counts count mul
pop DataFrame columns head
columns columns
replace
astype replace
DataFrame
shape insert columns fillna columns sum round shape join
DataFrame div DataFrame DataFrame DataFrame
DataFrame apply duplicated
items DataFrame apply values explode explode where all values keys values items dropna dropna
set_index reindex fillna reset_index
DataFrame columns replace replace to_csv index apply get columns
min
values loc values
loc loc
append DataFrame columns
first items append
merge
sample round stack DataFrame index columns
loc groupby idxmax merge sort_values duplicated last merge
DataFrame columns
eq groupby transform any copy mask
index set_index groupby transform rolling sum fillna
DataFrame columns shape iterrows iterrows
join
pipe prod prod
DataFrame set_index reset_index apply append DataFrame columns columns set_index melt apply reset_index set_index drop dropna sort_index
eq eq eq eq eq eq eq eq
DataFrame size loc all iloc
apply
itertuples index loc replace values values loc isin
size size DataFrame size columns index values index transpose columns values values values values values values DataFrame values columns columns index index index values columns values index values columns values DataFrame columns columns index index mean std mean std
apply bool
DataFrame
dropna all groupby isna ne isna shift any cumsum
assign sub merge
groupby agg mean std
ffill groupby first dropna groupby agg join reset_index drop
loc where index isin
all
keys DataFrame columns merge keys
apply index
DataFrame
xs xs xs xs xs xs
apply isna apply isna apply replace apply replace
groupby cumsum notna index index loc loc loc apply index DataFrame columns first groupby apply iloc groupby apply any index index groupby apply loc
loc where loc sum
iloc groupby apply groupby apply to_dict
get to_period groupby size reset_index count count plot
add append combine all to_csv stack rename_axis reset_index drop drop first set_index first index index rename index add to_csv append combine
mask duplicated mask mask duplicated
columns columns rename columns columns values columns values
astype eq astype where apply
to_csv mode first
DataFrame DataFrame apply replace round min apply replace round min loc values values values values where
DataFrame where at DataFrame astype merge merge
loc
groupby apply loc apply reset_index values values index values join drop columns
where mask loc mask
columns loc columns isin rename columns columns rename columns drop columns sort_values rename columns
resample mean resample sum
max groupby apply reset_index reset_index copy drop columns apply asof set_index groupby agg count sum reindex rolling sum fillna join fillna rename_axis
join loc
DataFrame index groupby index index transform
columns columns fillna
mask duplicated cumsum
mask loc mask where mask items loc
DataFrame
append items
head
loc duplicated groupby mean squeeze unstack
groupby groupby merge drop
values gt values bool value_counts ge values bool value_counts ge reindex index values bool
columns astype values sum keys loc
DataFrame copy set_index set_index index index
groupby transform iloc fillna reset_index
fillna sub sub
merge where isna reset_index drop drop
values values values
assign append diff values astype diff assign where where
index index astype set_index append unstack loc columns notnull
groupby apply join iloc iloc groupby apply join
set_index apply index index reset_index mean std pop drop index join DataFrame index index index mean std set_index apply index index reset_index mean std drop index join DataFrame index index index mean std
sort_values drop_duplicates last
DataFrame DataFrame explode explode dropna merge drop_duplicates astype
groupby filter
groupby agg sum sum
values values values values sum
reset_index rename index copy at round
index iterrows at index at index
append append DataFrame columns append
isin
melt pivot_table index columns count reset_index
groupby astype set_index values stack unstack columns columns join
DataFrame apply
explode query
shape round astype round shape round
join merge join merge
mean mul round astype astype sum astype sum sum index index astype add unstack rename columns
iloc columns values iloc apply values iloc columns values iloc apply values iloc mode mask eq eq mean iloc apply value_counts drop max iloc shape where iloc max
DataFrame isnull bool
reset_index index
assign diff eval
DataFrame abs
groupby pivot index columns values plot
values values DataFrame columns
DataFrame DataFrame index loc index
DataFrame groupby groupby groupby at keys keys sum DataFrame groupby groupby sum
apply sub
DataFrame
DataFrame apply
pivot index columns values div reset_index melt sort_values
eval
isin astype
transform style apply head head to_html transform DataFrame values columns columns iloc
apply append append
merge query
loc apply join dropna groupby sum
columns replace replace replace columns
loc eq columns items any any loc eq columns items any any loc eq columns items any any items bool bool
groupby groupby append values update keys keys update get keys groupby keys groupby groupby update values
iloc
DataFrame apply apply
loc max shape groupby apply groupby apply
columns
DataFrame fillna groupby sum
replace
DataFrame columns append astype
columns groupby ffill set_index groupby ffill reset_index set_index groupby ffill reset_index
append
values
melt astype
where any
append
groupby
mod columns assign rename keys apply rename rename columns columns columns
values
apply apply to_csv index
pivot_table index columns values count sum index items
loc eq assign sub
columns all duplicated columns value_counts index items
columns last
shape
append DataFrame
max append add columns DataFrame columns
bool assign
mean
T plot hist
T T T T T T T T T T join agg
DataFrame size set_index sum sum sum sum sum sum columns columns columns reset_index to_feather set_index to_csv to_hdf to_pickle to_parquet sum sum sum sum sum sum
index isin
append loc to_csv
sort_values sort_values explode rename columns
index loc
mode assign mode assign mode set_index mode reindex reset_index
filter applymap filter applymap
count groupby astype count
index set_index loc mean
DataFrame columns combine assign melt DataFrame values index index all values pivot index columns values plot plot
iloc iloc DataFrame shape shape iloc iloc assign iloc iloc min min
drop columns
dtypes stack replace replace dtypes bool bool bool groupby agg all all all sum
DataFrame append
DataFrame columns DataFrame columns groupby apply merge
join add dropna mask eq ffill count
where loc loc
apply
fillna where groupby notnull cumsum apply iloc iloc isnull notnull
sub join
where add lt mean std where mean std loc mean std
sample sample sample sample
groupby agg apply keys
apply
DataFrame columns DataFrame columns
merge apply
mask notna fillna
columns apply columns columns add_prefix columns apply mean std columns mean std dot columns mean std mask eq stack reset_index groupby agg values mean std
sum
sum min min count loc
DataFrame columns sum apply apply sum
assign groupby pct_change
merge sub
T rename columns index T columns
fillna ffill groupby ffill DataFrame size columns loc copy copy ffill groupby ffill
astype groupby apply rename columns to_dict reset_index groupby apply rename columns to_dict reset_index to_json
columns columns
DataFrame drop_duplicates last groupby count isin index
append iloc append iloc
merge abs abs diff query drop columns
DataFrame from_dict groupby any reset_index
set_index unstack columns columns reset_index
keys merge keys keys
copy copy append
rename columns
index index index iterrows iterrows index
DataFrame columns DataFrame columns index first at any join join filter keys
plot plot query iterrows
loc sum
columns replace astype
groupby groupby head
apply
nlargest index loc
merge
all append DataFrame compare append
DataFrame columns DataFrame columns index join merge DataFrame set_index drop stack reset_index columns apply apply drop pivot_table index columns values first reset_index rename_axis
columns columns values append sum add columns append append append append columns columns append append sum columns columns values append
keys dot columns
isin isin
combine join all reset_index drop set_axis index columns merge merge join index drop index
DataFrame columns loc loc
append append
sub
index index index
merge
set_index reindex max
DataFrame apply apply
dropna any
astype sample max stack count
keys assign keys DataFrame mean std mean std
index
astype
loc loc apply loc
loc values
loc T columns DataFrame size columns all index index iterrows T columns
loc index loc index
DataFrame index columns DataFrame loc isin resample sum values
merge query drop columns
iloc isin iloc iloc isin iloc
loc
DataFrame
ne eq loc head iloc loc tail iloc
groupby value_counts unstack diff replace stack set_index index
info info append join items values DataFrame explode
groupby add groupby transform size eq ne eq eq ne ne ne eq replace groupby transform sum drop drop_duplicates first columns drop columns eq ne eq eq ne ne ne eq groupby transform sum drop columns
iloc
replace DataFrame first first replace first select_dtypes columns astype replace first
sum first first shape index first columns sum index
columns iloc replace columns values
DataFrame DataFrame reset_index columns rename merge groupby count reset_index groupby count reset_index rename columns merge drop columns groupby index merge drop min
join join apply columns apply columns
DataFrame astype to_csv index astype dtypes to_string index DataFrame astype to_csv index columns astype dtypes to_string index
append loc columns columns fillna
mask count join values astype bool mask
values values values values values values values values all
eq sum ge eq sum ge
DataFrame columns
stack assign shift dropna
iloc iloc
columns columns merge
where where
DataFrame loc join
eq eq any eq any sum eq bool
groupby sample
apply replace replace set_index plot
bool
columns columns
columns columns loc columns insert loc
pop columns
loc
groupby filter
iat iat
DataFrame loc all at loc loc insert
melt sort_values reset_index drop
set_index resample asfreq index
groupby size head index
DataFrame DataFrame bool
where values DataFrame index index columns columns columns at columns any all columns all loc DataFrame index index reindex columns columns
merge
groupby value_counts isin groupby value_counts reindex index index groupby count
where isna
DataFrame columns groupby apply groupby apply to_dict
ne sum
merge
where agg dropna to_dict where apply dropna to_dict to_dict
DataFrame index columns
DataFrame values columns groupby size unstack reindex index columns fillna pivot_table index columns size reindex index columns
DataFrame DataFrame apply join
drop_duplicates last index loc duplicated last bool astype
DataFrame DataFrame reset_index drop reset_index drop values values groupby agg min groupby agg min
groupby to_csv index
DataFrame DataFrame isin value_counts
index index
join sum join join
values loc values append apply
DataFrame index add_suffix reset_index DataFrame count groupby size reset_index count
groupby sum
cumsum groupby transform size ge groupby transform size ge bool
between_time value_counts
product product DataFrame
groupby diff fillna gt cumsum
assign drop_duplicates groupby size nlargest
apply
mask isna loc
DataFrame rename columns iloc columns
DataFrame at all get apply astype any
loc isna
all columns columns drop_duplicates first
groupby pivot index columns values sort_index droplevel reset_index rename_axis
DataFrame columns index reset_index melt index query index
columns dropna replace astype
astype astype where where index index size size mean std astype mean std where mean std where mean std where index index mean std
DataFrame reindex columns append DataFrame reindex columns append iloc
style applymap to_excel index
DataFrame index columns
groupby cumsum diff shift loc astype fillna loc astype
groupby size groupby agg unstack groupby apply value_counts agg unstack keys astype astype groupby keys apply value_counts agg
append join
shift
loc
head tail itertuples count filter where min filter where max filter where filter where filter where append head join tail count filter where min filter where max filter where filter where filter where count filter where min filter where max filter where filter where filter where count filter where min filter where max filter where filter where filter where count filter where min filter where max filter where filter where filter where count filter where min filter where max filter where filter where filter where count filter where min filter where max filter where filter where filter where
count count
apply diff
count
values items values
max min groupby filter groupby aggregate sum
nunique groupby nunique eq index
sample
value_counts
DataFrame DataFrame eval join filter
index
drop join DataFrame from_records values explode sum
mask keys columns
groupby groupby size
groupby max
iloc columns columns index at index
iloc isin dropna
melt loc groupby agg reset_index
DataFrame columns
first all transpose merge append
explode reset_index drop
rolling apply mean min
filter melt filter melt aggregate count mean groupby agg
ne cumsum groupby replace fillna ffill set_index unstack replace dropna columns all columns columns values reset_index drop columns
join join groupby join append to_csv index mode
index index astype index astype
DataFrame set_index plot plot
groupby iterrows append
index index columns columns index index
empty
values
pivot plot
duplicated
assign merge query groupby head max
head
groupby agg max min mean mean reset_index
reset_index reset_index merge
reindex index head
size size size where eq
apply iterrows values loc loc drop columns where loc eq
groupby nunique query
sort_values sort_values groupby size reset_index
merge
all max all columns loc values loc values
DataFrame to_excel index
replace apply
to_dict
to_csv index
merge set_index
bool
iterrows mask loc mask loc mean loc max min
keys loc loc values
sum
append
DataFrame size columns index melt reset_index index index index index style loc
iloc columns
DataFrame columns plot plot loc
apply first last first last first last
apply
select_dtypes columns select_dtypes columns select_dtypes columns columns
groupby apply
reset_index DataFrame loc resample asfreq columns DataFrame columns reset_index fillna astype groupby set_index
where
set_index values
DataFrame columns apply join
merge DataFrame columns loc loc loc loc loc loc loc DataFrame columns loc loc loc loc loc loc loc DataFrame columns loc loc loc loc loc loc loc columns columns merge iteritems
DataFrame iloc
apply
DataFrame index apply dtypes DataFrame index
groupby sum mask groupby apply shift
iterrows
append DataFrame columns
sample get div div DataFrame div div div div where div div div div div div where div div div div div div div div div DataFrame where columns DataFrame where columns append append reset_index drop fillna sort_values drop_duplicates reset_index drop iterrows loc at at head to_string at at at at at at at at at at at at at at at
sort_values head
reindex index index join
apply
DataFrame isna assign
first
loc groupby apply duplicated reset_index drop rank groupby rank astype
DataFrame columns append append DataFrame columns
DataFrame
set_index append
to_period max min max min
mask loc mask
DataFrame apply apply columns bool DataFrame apply apply columns
loc
DataFrame from_dict index reindex columns columns index index fillna
melt
join DataFrame pop
diff lt cumsum transform mask abs groupby transform sum last values duplicated last eq
DataFrame duplicated last where
loc isnull
pivot_table index columns values sum
T items pivot_table index columns values join T
groupby join groupby assign fillna
isna columns
replace index
iterrows iterrows
fillna applymap apply join to_csv index
to_dict mod
DataFrame astype astype
insert keys items count count append DataFrame mode values to_csv index to_csv index
empty join assign rename columns
assign
columns columns values columns T values
melt dropna pivot_table index columns groupby add values first add_prefix rename_axis index melt pivot_table index columns groupby add values first add_prefix rename_axis index mask notna mask mask shape mask T T T mask T DataFrame values index index columns columns
columns mask iloc iloc loc mask
mean std mean std mean std melt DataFrame from_dict index reset_index rename columns index sort_values reset_index drop items reset_index columns items items DataFrame columns values values all
loc isnull sum reindex index index fillna ffill loc isnull sum join columns
DataFrame droplevel groupby
DataFrame DataFrame rename columns
mask where DataFrame where index index columns columns
merge ge le min min ge min min le min reset_index drop min min min DataFrame columns columns columns
ne mask shift ffill
assign merge assign query drop sort_index
DataFrame apply
DataFrame dropna
DataFrame columns DataFrame columns iterrows iterrows append append DataFrame columns
DataFrame
set_index mean mask loc mask mask set_index index get
fillna
merge drop drop fillna
DataFrame columns apply apply
assign columns isna all assign columns isna all
DataFrame columns DataFrame columns DataFrame columns DataFrame columns DataFrame columns DataFrame columns
DataFrame apply
pivot_table index columns size sum fillna
round astype round
apply
columns
join
iloc groupby var diff lt any groupby var transform max
dtypes dtypes
to_csv index
apply
groupby transform max eq astype where duplicated where ne groupby transform max eq astype where duplicated where eq
explode astype groupby index
columns notnull all
apply T sum sum append append iloc apply T iloc sum iloc mean append append
DataFrame at all get values applymap get index stack index
sample assign sample groupby transform max loc ne all drop to_csv
join apply
values applymap sum product product product
loc value_counts
items DataFrame dtypes items update update append
explode groupby agg loc isna isna sum
replace bfill iloc bfill replace bfill
reset_index astype astype set_index
astype set_index unstack sum set_index unstack sum
DataFrame
sub get join join replace where isin notnull agg join replace replace where isin notnull agg join replace columns apply size
values compare last sort_values iloc
replace ffill copy replace ffill
apply
append set_index merge
DataFrame join apply iteritems add astype
loc columns at T reset_index columns columns iloc loc index
product DataFrame product columns merge
apply iloc
columns columns
replace join first
astype set_index lookup loc lookup astype
values items items values items DataFrame T reset_index rename columns values items items values items DataFrame values items items values items DataFrame T
groupby filter eq drop_duplicates merge
append sum
bfill ffill astype replace groupby agg join dropna replace astype
values values count sum
merge drop_duplicates
set_index
index
iloc bool apply value_counts fillna astype mask mask apply value_counts fillna astype
columns join columns join
groupby apply to_dict
agg mean std unstack T columns columns astype agg join T assign sort_index
apply
DataFrame set_index DataFrame set_index DataFrame set_index DataFrame set_index DataFrame
drop columns columns values
iloc iloc mask iloc iloc iloc mask iloc mask
DataFrame size columns groupby apply
to_period duplicated
set_index unstack pivot_table index columns values mean
ffill lookup index loc lookup lookup lookup lookup at lookup ffill lookup iterrows lookup lookup lookup lookup at lookup
columns DataFrame columns columns items reset_index drop rename_axis reset_index DataFrame columns columns index items rename_axis reset_index
groupby size size size
DataFrame size size size
set_index iloc lt idxmax astype mean
iloc values
iloc groupby transform size groupby transform size sort_values
DataFrame columns iloc
astype
DataFrame columns apply
loc shift
all add all add
reset_index pivot index index columns values apply isnull dropna all rename_axis columns index reset_index set_index index unstack apply isnull droplevel dropna all rename_axis columns index
loc loc fillna loc loc fillna
columns astype get shape get index index index index index set_index rename columns drop rename columns drop drop drop drop astype
merge merge fillna
columns apply astype replace columns apply astype replace
items items append items update update update append DataFrame set_index DataFrame set_index
DataFrame where shift shift shift shift shift
index sum sum
copy DataFrame loc copy
DataFrame
mask loc mask loc isin mask value_counts index
DataFrame index melt append sort_values
values index values mean round
where
set_index
merge index rename columns
iteritems style apply
melt merge drop
DataFrame columns index index fillna fillna
merge
index astype
index get max index all sort_values max max all all all sort_values max max all all all drop max drop max
eq eq
where isna ffill
shift eq loc shift
append max DataFrame groupby apply
columns
merge
DataFrame replace dropna
DataFrame columns DataFrame columns add merge
mask loc where loc loc groupby ffill mask reset_index groupby index first count loc where loc loc where
merge query merge loc
asfreq fillna ffill fillna loc diff fillna ffill
apply lookup lookup values
columns reset_index join reset_index index set_index index
DataFrame pivot DataFrame set_index unstack
DataFrame columns replace columns values columns values replace
truncate iloc iloc groupby apply truncate explode reset_index
set_index fillna
replace drop sum sort_values
DataFrame
DataFrame DataFrame merge
columns
apply
pivot_table index columns values sum add_suffix reset_index
DataFrame
transpose get groupby sum groupby sum fillna ffill fillna bfill head set_index
applymap applymap notna astype idxmax
join fillna ffill
transpose
DataFrame index columns iloc iloc iloc first last
append to_excel index drop first iloc iloc columns drop all index drop dropna all
DataFrame columns values iteritems values DataFrame columns values iteritems values
groupby rolling count keys
merge query columns
DataFrame columns assign iterrows drop_duplicates merge
DataFrame groupby first sort_values loc groupby first apply reset_index merge sort_values
all empty append append
min max append sort_values reset_index drop append
values apply apply apply
axes axes groupby aggregate sum unstack plot axes
cov var
DataFrame resample fillna drop columns DataFrame DataFrame to_period index index fillna count
index DataFrame index index
DataFrame apply
first iloc add iloc
DataFrame apply explode agg apply DataFrame agg explode
rename rename sort_index resample ffill rename rename sort_index resample ffill iloc shape
info drop_duplicates min max loc drop_duplicates
product append
DataFrame get
apply join
copy index loc index loc index index loc index loc loc drop iloc apply
add assign index DataFrame
groupby transform mean rank iat expanding apply sub astype
groupby apply
DataFrame pivot assign pivot_table values index columns sum fillna pivot sum pivot div reindex
eq diff ne diff loc loc loc
DataFrame astype bool drop columns reset_index drop drop columns reset_index drop
index index get
join drop groupby sum
loc isin isin isin isin copy
get DataFrame get get get DataFrame DataFrame DataFrame values append
assign mul sum
DataFrame groupby size
assign columns
DataFrame columns isna sum
where
DataFrame apply
append append assign assign
abs loc values loc values min loc
query where where query where where values
groupby DataFrame index
DataFrame groupby count columns reset_index drop index
groupby var agg count add_suffix reset_index
itertuples index
assign apply
DataFrame columns
DataFrame columns index iterrows index index iterrows
DataFrame columns
where
between_time apply
query value_counts
astype astype
index index join sum index iloc index iloc join
columns where columns where apply columns columns where apply empty bool any all where applymap mask
isnull isnull values values values isnull values values
replace append replace drop insert append insert
DataFrame DataFrame at index copy reindex index index shift ffill rolling apply resample ffill mean reindex DataFrame DataFrame at index copy reindex index index shift ffill rolling apply index diff reindex DataFrame DataFrame at index copy reindex index index shift ffill index diff shift rolling sum reindex index
query apply
merge DataFrame columns count
mask ffill ffill groupby ffill ffill shift cumsum transform size
astype pivot_table index join reset_index
columns
replace
where
items lookup index
size product product product product size get product get
DataFrame DataFrame columns set_index
DataFrame groupby to_period agg count
columns nunique set_index set_index groupby append unstack columns columns reset_index set_index groupby unstack columns columns reset_index
apply sum apply sum
groupby agg reset_index join DataFrame values index add_prefix drop
sum size
index iterrows count DataFrame columns add tz_localize tz_convert join reset_index drop
DataFrame items columns sort_values replace
loc empty values append values values join apply
index resample mean resample sum resample mean
groupby groupby append
insert mask notna ffill dropna rename columns isin insert mask ffill rename columns
apply count replace sort_values get all append astype drop
groupby transform any astype
DataFrame groupby mean unstack plot DataFrame pivot_table index columns values mean plot
values fillna all explode any values value_counts value_counts value_counts
index items loc items loc items
DataFrame apply apply
isin isin isin
to_sql append
columns DataFrame
DataFrame
DataFrame where eq ffill
set_index columns columns stack dropna fillna ne reset_index groupby agg to_dict
count
values index index join
loc reset_index drop
to_csv
melt columns columns loc gt
select_dtypes columns select_dtypes columns apply apply dtypes
size rename columns rename columns size astype astype
merge ffill
index bool index values
values values values astype apply explode apply join
fillna ffill groupby apply join
assign query
loc loc loc loc shift loc loc shift loc isnull loc
merge merge groupby apply to_dict reset_index rename columns groupby apply to_dict reset_index rename columns groupby apply to_dict reset_index rename columns to_json
mode
mask isna loc mask loc mask loc mask loc mask loc mask loc mask loc loc mask loc loc mask
where isin groupby size groupby agg mean min max
DataFrame columns groupby apply nlargest reset_index drop
merge groupby count rename columns
groupby count unstack reset_index
join
size index size sort_values size size size size DataFrame index index groupby resample sum
apply
columns columns stack sum eq columns keys columns stack sum T eq
assign eq sum eq any
where where
rolling sum shift assign groupby apply
index isin bool index isin any index isin any
dropna iloc dropna iloc dropna iloc dropna iloc
DataFrame
reset_index abs abs abs idxmin loc loc set_index
rename rename
mask apply mask apply
groupby max
join mask mask mask
set_index unstack where ne shift stack reset_index set_index unstack where ne shift stack reset_index
bool bool
append all index append index index set_index append index reorder_levels
replace
assign ne astype groupby sum
loc index loc index
values DataFrame values columns values
query to_csv mode index
plot
filter eq cumsum eq
notnull notnull notnull notnull isnull isnull notnull notnull isnull notnull isnull notnull isnull notnull notnull isnull notnull isnull apply
DataFrame columns index DataFrame columns index DataFrame columns index DataFrame columns index DataFrame columns index
loc astype all
where at loc
index to_csv index
DataFrame
apply replace
iloc iloc groupby apply
max min
transpose columns T items at append loc transpose T where where columns first all columns first columns last where
DataFrame sort_values head
append append join join append groupby value_counts unstack fillna join groupby value_counts unstack fillna
values
where isnull where isnull
dot columns
DataFrame melt
DataFrame values append
DataFrame DataFrame
DataFrame append append filter append filter append append append
size DataFrame mean std apply apply join drop fillna mean std
apply
replace groupby ffill fillna iteritems iloc
DataFrame where isin values values values where isin values values values where isin values values values iloc where values to_dict append append join
DataFrame DataFrame columns columns min max groupby set_index unstack reindex columns stack reset_index rename columns reset_index reset_index
loc
index index
DataFrame
sum cumsum cumsum
set_index fillna
DataFrame columns DataFrame columns DataFrame columns head to_html head to_html head to_html
DataFrame replace replace
assign merge mul merge set_index set_index
items keys apply
diff lt values values round values append values DataFrame from_records columns count count values DataFrame values DataFrame
append DataFrame to_excel
groupby agg sum first last reset_index columns columns join rename columns sum first last sort_values
where isin
get mean groupby agg mean mean count all where get count groupby transform count get where all all loc take first all iloc
columns
loc loc idxmax
eq cumsum groupby ffill
DataFrame from_dict T
index groupby apply sample loc drop
isna isna
replace isin
set_index asfreq T reset_index
DataFrame columns loc loc isin isnull
groupby transform size mask loc mask groupby
duplicated bool
DataFrame append
DataFrame mask iloc apply index drop mask iloc applymap cumsum combine items mask iloc mask iloc apply iloc mask fillna apply sum iloc iloc mask fillna
groupby index loc iloc
where shift shift shift shift
drop columns rename columns
loc size
groupby join groupby append DataFrame columns index join index join index join append DataFrame columns
min replace max replace min replace max replace min max min min max min max max max min min any min apply replace max apply replace min apply replace max apply replace min min min max max max max min size max min min DataFrame DataFrame head min apply replace max apply replace min min min max max max max min size max max min min DataFrame DataFrame
var append var
values DataFrame
groupby sum index groupby count index groupby count index values values values
sum
lt assign cumsum loc groupby loc groupby cumsum
loc loc
DataFrame drop drop
groupby iterrows append groupby iterrows append
isin isin
groupby
DataFrame groupby apply reset_index groupby apply reset_index
groupby mean plot
DataFrame copy astype bool shift abs shift abs loc bool bool sum replace bfill fillna bool sum bool
diff diff diff reindex index diff diff assign count diff groupby diff cumsum diff where diff count diff
assign items assign
duplicated first iloc where drop index where
loc isnull sum DataFrame columns loc isnull sum mean std drop columns isnull sum mean std drop columns apply isnull sum mean std DataFrame columns loc isnull sum mean std drop columns isnull sum mean std drop columns apply isnull sum mean std
values index index values
columns columns columns DataFrame columns columns
merge rename columns merge rename columns drop columns rename columns
loc isin pipe
apply set_index
DataFrame columns explode DataFrame columns iloc values apply DataFrame columns columns
groupby apply sum explode reset_index
set_index groupby apply apply drop columns DataFrame set_index groupby apply index
sub sub sub size apply to_excel to_excel to_excel at at mode at
DataFrame iloc values values DataFrame index index columns columns
to_period
index index reindex bfill
DataFrame index DataFrame copy index index
loc columns
DataFrame stack dropna mean
set_index T to_dict get drop values
sum sort_values groupby cumsum drop columns
index
ne shift cumsum min max groupby agg min max rename columns droplevel reset_index
isin groupby
applymap
index index index index mean T index groupby index mean
dropna reset_index drop DataFrame keys values DataFrame iloc sum columns append
groupby size reset_index max groupby size reset_index merge rename columns dropna index iterrows reset_index drop drop columns copy loc append append reset_index drop
values assign replace assign cumsum sort_values drop apply join replace
notnull where groupby cumsum transform count le ge
DataFrame append reset_index drop
groupby index apply size iloc explode apply apply value_counts
reset_index reset_index isin
explode rename columns replace replace columns drop columns drop columns columns to_dict columns rename columns replace columns explode to_dict fillna ffill fillna bfill groupby first columns loc to_string index
isin isin astype groupby transform size
groupby index transform idxmin transform count gt loc
get
isin sum groupby agg reset_index columns sort_values head values pivot_table isin values index columns reset_index
groupby apply nlargest columns
mean explode astype mean
replace dropna
columns columns
groupby reset_index drop groupby reset_index drop groupby
DataFrame fillna ffill groupby apply join reset_index
min max columns columns min columns columns max
DataFrame groupby agg
loc
melt merge mask eq where mask insert columns where mask where mask
sample index sample index loc copy index size replace loc copy sample sample
apply
index replace replace index index index replace index index index index index fillna
apply loc loc
size groupby agg sum merge
sub sub
DataFrame value_counts index columns count reset_index values count
columns index
melt dropna reset_index pivot index index columns values reset_index drop index set_index stack reset_index set_index append unstack reset_index drop
apply
to_csv index groupby
DataFrame index keys reset_index index rename columns index index index
DataFrame columns DataFrame columns merge
assign apply
set_index unstack columns columns set_index unstack columns columns columns first columns columns index stack reset_index
var replace var replace merge var drop var join join get apply apply join
index sort_values
loc loc loc
sort_values
itertuples itertuples append columns columns DataFrame columns
sample replace DataFrame iloc reset_index drop index drop_duplicates reset_index drop columns
fillna set_index T append iloc iloc fillna iloc set_index columns columns T append iat iat append
iloc iloc resample agg sum
loc index index index
eq groupby transform any loc set_index eq groupby transform any loc
index index groupby
diff DataFrame cumsum ne cumsum cumsum iloc cumsum cumsum ne shift cumsum groupby diff transform groupby transform groupby
append copy loc append
plot melt astype groupby agg sum mean unstack columns columns reset_index plot melt
DataFrame columns DataFrame
loc to_period value_counts sort_index plot
groupby value_counts join rename groupby transform count groupby transform count
index index cummin mask index cummin cummin loc index index cummin
groupby transform nunique groupby transform nunique
merge
sort_values astype style axes loc
DataFrame index index DataFrame index index columns join DataFrame index index columns join DataFrame index index assign reindex columns columns add empty
axes
iloc cumsum iloc mul groupby max shift
product product join product explode reset_index drop all product get all product get all
columns index drop stack reset_index drop values transpose groupby index apply transpose copy columns columns loc
DataFrame sum DataFrame
reset_index drop
mean sum
ne shift cumsum first last columns groupby agg
columns columns
replace replace
index values columns
columns columns index index reindex index columns index index join columns columns join columns columns index index index columns index index index astype columns columns columns astype sort_index sort_index reindex index columns index index join columns columns join
groupby agg to_dict groupby agg items
DataFrame to_json at at columns reindex at head iloc
DataFrame set_index apply DataFrame explode explode reset_index sort_values reset_index drop set_index apply DataFrame explode reset_index sort_values reset_index drop
groupby apply sum nunique
max max notna groupby sum
astype
DataFrame index columns apply apply
DataFrame index
astype astype merge values DataFrame columns DataFrame values columns
isnull isnull bool bool isnull isnull bool notnull notnull all bool ne iloc ne iloc any bool
DataFrame min DataFrame columns iterrows index iterrows at
DataFrame to_csv mode mode mode
set_index stack reset_index columns sort_values melt columns
DataFrame columns sort_values head sort_values shift shift dropna rename columns
columns columns columns dropna set_index stack dropna rename_axis reset_index dropna assign sort_values reset_index drop set_index columns apply columns stack set_index append reset_index drop stack dropna rename_axis reset_index dropna assign sort_values reset_index drop
index
loc loc
groupby agg mean columns
get
join DataFrame join append
items append append append
apply
DataFrame apply max
sample sort_values duplicated get groupby diff fillna compare mask all mask groupby transform all get last duplicated last
DataFrame columns DataFrame columns
update set_index update set_index reset_index
DataFrame sum append append groupby transform groupby last groupby sum reset_index drop columns
values values values values values values values values values
transform
T T
stack rename_axis reset_index head
DataFrame columns join
items DataFrame columns keys groupby agg join groupby agg product
min max
DataFrame
groupby transform min
sum mean append
index iterrows
reset_index merge
DataFrame apply
loc mean DataFrame size
columns count
DataFrame columns
sample replace sample
DataFrame from_dict
astype astype values
copy
apply DataFrame apply
DataFrame items values keys keys
sort_values astype
isna loc shift
apply index columns
set_index unstack unstack groupby groupby agg size sum columns unstack reindex index min index max sort_index all index fillna rolling sum fillna mask replace bfill backfill all rename_axis stack reset_index sort_index reset_index
keys values keys values
stack stack reset_index append
groupby agg mean std groupby mean std transform mean transform std
iloc drop duplicated iloc groupby idxmax
index bool columns copy copy copy
DataFrame items columns
loc columns isnull any
info shape itertuples append append append columns values append columns values append columns values append append
get index append DataFrame append loc
loc
mask where mask where mask
clip DataFrame
copy set_index set_index where columns DataFrame DataFrame DataFrame
corr count
axes axes align axes align axes axes align axes align
apply apply join duplicated first where apply apply join duplicated first where drop fillna apply apply join duplicated first where drop
isnull
assign set_index asfreq resample agg max min dropna iloc dropna iloc
keys keys
DataFrame product product index values loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc loc
where isin
mean rolling apply mean std values mean std values mean std mean values mean std values mean std all values rolling apply values mean values mean std values mean std all values rolling apply values mean values mean std values mean std all values rolling apply values mean values mean std
DataFrame iloc values iloc values columns
loc isin
where isin
plot
sort_values groupby cumsum groupby sum groupby apply shift cumsum rename merge
value_counts unstack groupby count unstack value_counts unstack
groupby count
drop
bool
loc isin assign shift cumsum drop_duplicates
apply any
copy groupby join
replace drop columns rename columns replace drop columns rename columns to_csv index assign idxmax drop columns assign apply drop values assign drop assign drop columns assign replace drop
append join append append join
merge dropna all dropna all
value_counts index
where isin where
DataFrame index columns index values sample replace columns sample loc size replace
boxplot
merge drop columns rename columns
add_prefix
columns
max join
apply DataFrame join
reset_index drop round groupby transform nunique sort_values round
set_index T columns columns T T
join mask isin loc mask loc mask join
isin bool columns isin bool bool bool bool bool
copy copy to_records get to_records index
loc mask loc mask
DataFrame DataFrame columns index iterrows append append
shape assign shape values DataFrame columns join
applymap
items apply
itertuples DataFrame columns index iterrows itertuples
diff diff columns diff diff columns diff diff diff diff diff columns
apply apply
DataFrame set_index stack reset_index rename columns values isin values
groupby transform mean
max DataFrame stack
DataFrame
index iterrows index product product product DataFrame columns index iterrows index loc loc loc
query get
loc index index columns values DataFrame index columns columns loc
copy add add
dtypes iteritems
replace to_timestamp
values pipe
replace
query query isin isin
melt melt filter reindex columns columns ffill melt mask ffill get all shape melt drop
plot style
DataFrame values set_index stack rename_axis index reset_index
mean std mean std apply explode values sub index loc index copy add add add groupby add add index reset_index drop
last last max at max DataFrame merge apply to_string DataFrame merge apply to_string
columns columns replace columns columns replace DataFrame dtypes columns columns replace dtypes bool
DataFrame values values cumsum iloc values
isin where
index index iloc join iloc
DataFrame loc loc iterrows
replace replace
apply loc
DataFrame from_dict where
duplicated last bool
mask mask
groupby max reset_index
add first ffill
DataFrame
values values
groupby shift fillna fillna fillna where eq
groupby product agg size rename count reset_index product count
groupby agg first first sum
filter isin filter
shift fillna where where
loc iloc columns
groupby agg nunique size groupby agg nunique size groupby agg nunique size reset_index
sum
groupby sort_values drop
copy loc sum
value_counts reindex
DataFrame set_index to_dict isnull all drop columns
bool bool
columns index to_xarray to_xarray
copy astype values at columns all columns iterrows dropna loc loc
melt
query query
values
sum sum shape
groupby to_csv
merge
apply
set_index mask stack reset_index mask rename_axis stack reset_index drop
mean mean std std abs mean lt std mean
plot plot
T stack to_excel index
reindex min max groupby size unstack reindex min max
DataFrame
copy
sort_values
empty DataFrame DataFrame DataFrame where empty DataFrame DataFrame DataFrame where drop columns drop columns
mask mask mask
DataFrame where duplicated loc copy where groupby nlargest reset_index where index isin groupby apply reset_index sort_values compare all apply all shift merge loc explode where isnull drop columns drop_duplicates
sort_values groupby mask where diff abs cumsum astype astype astype get columns groupby groupby transform min transform max count transform sum
any
assign
DataFrame replace
bool index groupby index transform last_valid_index index
groupby count groupby count round count count
replace
melt set_index groupby resample ffill reset_index drop DataFrame iterrows
DataFrame
groupby apply join values notnull reset_index rename columns T groupby apply astype replace sum sum reset_index rename columns
round where loc
loc loc sum
query query to_csv index
astype astype
sample get product product product product loc product rename index index rename reset_index add product product product melt melt product add
DataFrame columns drop index
loc index loc
isna sum dropna
columns append groupby groupby mean groupby columns
sum reset_index sum reset_index iloc iloc astype
drop columns
filter columns div sum div
replace
groupby apply groupby apply reset_index groupby apply values index values
join
DataFrame
rename index loc loc loc
count DataFrame
select_dtypes fillna median
isin all all mask isin mask
shift
columns
set_index loc idxmin
index resample mean resample last assign resample mean index
last last max min last index index between_time reset_index groupby index agg set_index drop index sort_index
keys index iterrows at index
isnull isnull DataFrame isnull
groupby transform sum eq groupby transform sum eq groupby transform sum abs le first first first first first first first first first first first first first first first first first first first first first first first first first first first first first first first first first first first first first first
groupby head groupby sum
size transform size transform at size transform
append append append append
shift fillna DataFrame shift shift shift fillna shift fillna
set_index at
nunique iloc isin loc eq loc eq groupby apply nunique drop columns index reset_index drop reset_index drop
shift astype cumsum shift cumsum shift groupby where cumsum groupby where cumsum replace ffill replace ffill
DataFrame iloc iloc append
iloc fillna fillna fillna
merge pipe
sample DataFrame columns tz_convert apply apply apply eval
append columns
plot
DataFrame index itertuples keys index loc itertuples keys loc
apply
groupby astype pivot_table index columns values join reset_index drop
loc groupby fillna ffill astype sum sum groupby cumsum get sum groupby cumsum max groupby max reset_index first join merge rename columns isna isna copy sum loc groupby cumsum where loc columns get index reset_index drop loc copy
values
DataFrame size mean std mean std
nunique mask nunique ne mask mask mask bool
pad DataFrame join set_index drop stack reset_index drop sort_values rename columns
var sum explode explode groupby agg count sum rename round var reindex reset_index
where where hist where take values hist empty where
astype apply astype
assign shift cumsum
itertuples append DataFrame columns count
iloc replace
loc loc fillna
index DataFrame columns columns iloc loc
where where groupby transform sum
columns columns astype DataFrame mode keys astype
notnull loc loc
all DataFrame index index index index index fillna ffill sum groupby sum
index value_counts index get values values filter values isin
astype all shift shift to_csv
stack reset_index columns
iloc
merge isin query reset_index drop
count drop
apply astype apply astype apply
DataFrame
bool applymap
where isin where isin where isin
loc
merge drop merge drop rename columns
DataFrame index pivot index reset_index columns
values values squeeze min sub div groupby apply
index groupby index eq cumsum agg join index groupby index cumsum agg join items
iloc sum iloc sum shape groupby apply
dot columns
DataFrame columns keys
all all
columns reindex
apply
DataFrame product DataFrame merge
applymap
groupby apply apply apply groupby apply
groupby groupby merge drop
get eval eval DataFrame
DataFrame DataFrame
DataFrame index columns columns assign columns set_index reset_index get columns index columns
groupby mean unstack droplevel plot
loc values loc loc values
DataFrame
groupby apply join append append join apply groupby count rename count reset_index rename columns
assign mul groupby sum groupby apply sum reset_index groupby apply groupby apply groupby apply
reset_index drop reset_index drop
DataFrame
astype groupby agg count mean query count
columns where plot iloc reset_index drop to_dict index get min size get
gt
DataFrame
DataFrame
any first
to_html
values
index iterrows
ne cumsum add astype cumsum add astype
apply
copy loc
values values values values values values values values values append join
DataFrame stack iloc shape shape
index iterrows items index index
DataFrame index
groupby apply rename columns to_dict reset_index groupby apply rename columns to_dict reset_index to_dict rename columns to_dict append
DataFrame join get
assign
index merge
mask loc mask loc mask
index index iloc
first filter sum groupby sum
eq eq shift eq eq shift eq
iloc shift sum iloc apply groupby apply reset_index
DataFrame DataFrame fillna
groupby abs groupby groupby apply DataFrame columns drop_duplicates merge groupby abs iloc groupby apply groupby groupby apply
size sum isnull values size size min quantile mean median quantile max std iteritems DataFrame columns
iloc any astype index any astype
DataFrame columns set_index unstack stack unstack
fillna fillna
mul mul
DataFrame mask loc mask loc mask replace join iloc values DataFrame fillna assign
append append append
merge
copy
loc isin product isin
rename
sort_index reset_index drop loc index
append drop_duplicates reset_index drop
DataFrame
items items
index isin groupby apply
plot plot plot plot plot
diff sub fillna
set_index between_time reset_index
count
empty items empty
DataFrame
DataFrame index groupby mean groupby transform mean
assign explode drop columns apply
merge
rename DataFrame mean std mean std
DataFrame from_dict index
DataFrame index apply DataFrame index
count pivot_table index columns values count sum columns columns reset_index
set_index first join T
groupby transform mode mode
loc explode reset_index merge index
columns append values
values
DataFrame DataFrame apply
DataFrame columns index values groupby reindex assign align index value_counts sort_index reindex
index index columns
count count append
apply
groupby apply join
index
join shift cumsum index index set_index drop apply shift at shift iloc at index columns drop shift index columns reset_index DataFrame columns index join shift index cumsum set_index drop apply iloc at shift columns drop index drop shift columns reset_index
replace
DataFrame columns round isin
set_index drop index reset_index
groupby apply apply apply mean groupby apply values mean
DataFrame columns
set_index drop_duplicates DataFrame columns index index columns values explode insert
columns
assign astype astype eval astype astype
columns abs loc groupby idxmin drop
join reset_index drop
groupby agg groupby agg join
isin head bool
apply loc apply loc mean apply loc mean
DataFrame from_dict index columns apply eval
shift
merge fillna
apply
columns columns columns fillna columns
assign groupby merge assign groupby
set_index groupby unstack sort_index columns columns reset_index
all bool
apply add_prefix
DataFrame columns
DataFrame columns
assign groupby sum reset_index drop_duplicates assign assign groupby sum reset_index append drop_duplicates
index columns dropna
filter eval DataFrame columns filter
set_index columns columns stack shift
DataFrame index columns
plot
where where
eq where where
abs idxmin values lookup index columns first first first
DataFrame apply join DataFrame apply join DataFrame apply join
merge filter get mean count columns groupby transform mean groupby transform count drop columns drop drop_duplicates merge isnull groupby transform mean groupby transform count drop drop_duplicates merge isnull groupby transform mean groupby transform count drop drop_duplicates
shape iloc shape shape iloc shape
astype
DataFrame add shift add shift add at columns any columns sum values cumsum
values max values values count values count values DataFrame apply index at at DataFrame apply fillna
astype loc groupby idxmax
shift copy set_index sort_index diff abs
merge set_index unstack add_suffix reset_index
groupby filter count isna all groupby count transform count eq transform size gt groupby count agg count size count eq size gt isin index count
reset_index drop
loc loc loc
sum sum
values set_index to_dict index items get
round get dropna applymap apply columns columns mean apply groupby agg median index index plot index
DataFrame columns iloc columns
index
product loc product max
index iloc iloc append
join
index DataFrame columns values iloc loc filter columns reset_index mean mean apply astype append drop index append info
DataFrame DataFrame keys keys
empty get append append append to_excel index to_excel index
columns columns columns to_period columns
values DataFrame iterrows values DataFrame values set_index
DataFrame index where index
mask notna mask ne mask shift cumsum groupby transform size mask
mask ne groupby count
assign explode assign explode
loc index index
join iloc values
DataFrame T
set_index append unstack columns columns join
product
get loc update values loc isin
value_counts
DataFrame all set_index apply explode reset_index groupby append append DataFrame DataFrame set_index apply explode reset_index DataFrame set_index apply explode reset_index
melt dropna sort_values shape size shape shape size shape size melt dropna sort_values reset_index drop shape size shape shape size shape size
assign
cumsum shift where reindex values cumsum shift cumsum shift
merge fillna
DataFrame loc loc dropna
values info pop info values info info info set_index stack DataFrame DataFrame values index index stack unstack reset_index drop reset_index info info pop info info info info DataFrame values info info values info
groupby transform mean
set_index reindex reset_index
sort_values ne shift
iterrows groupby apply
mask fillna ffill fillna
index iloc drop index index index index index index
items append DataFrame head
set_index append loc unstack columns columns
iloc ne index iloc ne index ne index iloc copy
append
replace
where notnull where assign where notnull where where notnull where
where where merge rename columns
DataFrame append astype values to_csv index
DataFrame merge mask duplicated
index index index index
transform items drop_duplicates loc duplicated
append
reset_index ffill set_index
DataFrame index index columns columns loc style apply
DataFrame DataFrame join values
set_index apply stack reset_index drop reset_index insert apply
first copy copy drop assign drop append rename values append rename columns merge all join merge any sum first sum sort_values groupby ffill where fillna add sum values groupby cumsum astype
groupby DataFrame replace groupby mean stack rename transform sum groupby index size index index transform index index
apply join
groupby apply reset_index apply rename columns
replace isnull sum
eq first cumsum groupby apply duplicated eq ne
replace
fillna set_index
DataFrame DataFrame index iterrows sum sum append sum
columns nunique
merge
values values loc values values
rename columns loc drop index set_index groupby columns sum reset_index drop index copy
min iloc
add columns apply apply apply to_csv
sort_values size sort_values size size sort_values size reset_index drop
DataFrame columns applymap all columns columns columns columns
melt
loc to_csv index
groupby count
dropna
groupby nunique
apply apply mask stack rename_axis mask mask mask index mask merge merge
product product product product product product product product product product product product DataFrame where ffill groupby idxmax values loc index isin columns columns product pivot_table index columns product values
drop_duplicates
index rolling apply astype
groupby
loc
merge merge index
mask sub sub sub sub sub values apply mask all loc loc replace columns dtypes
apply get
loc loc keys values loc
astype astype astype astype isin
rename columns fillna apply DataFrame columns values rename columns astype astype DataFrame apply fillna astype replace
DataFrame columns groupby sum reset_index
T dot T
stack dropna rename_axis index columns reset_index
all DataFrame DataFrame replace replace
bool
replace corr loc
to_hdf
loc append
DataFrame columns index set_index index columns columns index DataFrame columns index set_index index DataFrame columns index DataFrame columns index
merge query reindex columns
DataFrame
melt query groupby last
DataFrame DataFrame
to_excel empty empty empty empty
loc
drop DataFrame from_dict drop pivot_table index columns values max columns reset_index drop
eq compare all all all mask eq all ne mask ne any mask mask
groupby groupby first groupby cummax groupby cummin
DataFrame reset_index drop
mean columns mean index groupby apply mean groupby apply resample agg mean sum head
DataFrame items head tail merge query head tail
DataFrame index columns assign
groupby count
set_index
apply all
loc merge
apply values DataFrame apply values
DataFrame sub DataFrame set_index astype sub sub set_index astype sample size values reindex sub index sub astype sub sub columns columns columns index div div columns div index
loc eq isnull
values count index
set_index T reset_index columns reset_index apply fillna
columns mask columns columns columns values columns mask columns mask assign reindex columns
mask last groupby transform max eq last groupby groupby mask agg first first min max min max max filter columns mean append sort_values reset_index drop
add columns index pivot_table index columns columns join columns values
plot
groupby sum
loc loc loc loc
diff mean notna index index index fillna cumsum diff mean notna index index index fillna cumsum T columns copy diff mean notna index index index fillna cumsum diff mean notna index index index fillna cumsum T
shape apply
sub join sub join
assign assign assign to_csv
groupby tail eq groupby shift eq drop index bool
index index rename
apply
DataFrame columns shift applymap
sample loc round isin values
melt pivot_table index columns groupby add values first add_prefix rename_axis index
DataFrame apply
get get DataFrame columns append
all columns filter columns sub append combine all columns values
columns apply replace mean std replace mean std
loc groupby idxmin fillna groupby where min append
apply
index index
add columns loc count iloc first iloc last iloc iloc count count count count groupby groupby apply reset_index set_index drop merge index merge fillna
groupby transform sum
round rename assign round rename assign merge
get notna loc loc
DataFrame columns DataFrame
DataFrame columns index iterrows index iloc index iloc index append append append append append append append DataFrame
groupby sum
apply
where groupby diff fillna
mean std mean std mean std mean std DataFrame mean std mean std replace mean std replace mean std fillna replace mean std fillna replace
groupby apply diff ne cumsum where eq
to_dict get where
pivot_table drop_duplicates index columns values count div sum sum loc sum
cumsum cumsum
values index min DataFrame index groupby aggregate groupby index agg groupby index agg all DataFrame index index DataFrame index index plot
applymap apply stack
loc groupby agg apply columns groupby agg sum
values
eq loc loc apply
query head
DataFrame columns DataFrame columns columns columns loc
merge
loc rolling sum dropna
copy update DataFrame
DataFrame iloc to_csv index iloc to_csv index mode iloc to_string index iloc to_string index
index index apply
DataFrame DataFrame merge merge sort_values index reset_index drop
DataFrame DataFrame columns iloc index style apply
pivot index columns values reset_index
to_sql replace
groupby iloc
DataFrame DataFrame bool bool
filter nunique
index index max index min
melt sort_values
count gt ge where radd index index sort_index join DataFrame drop_duplicates
DataFrame melt
groupby agg first last stack reset_index first last first last first last
astype bool T
apply apply fillna astype
fillna head
product product prod product prod shape to_csv prod index
all values loc iat last loc
groupby groupby
any values
append DataFrame
set_index to_dict min get min items
where DataFrame columns where DataFrame columns
where notna
mode
assign diff mean
pivot add add T
replace
apply
apply
set_axis loc drop sort_index rename_axis index columns set_axis loc drop sort_index rename_axis index columns
loc ne combine_first mask ne
mask sort_values groupby apply shift shift loc mask
append DataFrame astype
where isnull
join loc join loc
head
groupby
merge fillna
melt replace groupby sum count groupby cumsum min max groupby count set_index reindex ffill fillna append
groupby apply reset_index drop
columns columns columns columns where columns
columns columns astype to_csv index
astype mean astype mean
copy eq shift update astype radd astype mask isna values
apply iterrows loc
groupby columns groupby groupby apply
DataFrame columns DataFrame columns
isin isin
sort_values groupby rolling sum values
iloc where iloc ge shift iloc shift iloc where iloc ge iloc isna shift iloc iloc apply ge iloc isna shift iloc iloc apply where ge iloc isna
isnull sort_values isnull groupby sum astype sort_values
transform join index
loc loc
apply replace
index
eq groupby transform min
drop DataFrame columns first loc loc loc loc
append join
count value_counts astype count
merge columns
where fillna
isin
value_counts groupby value_counts groupby nlargest head
merge index set_index index drop
columns replace values values values values index sum values values
astype
to_json astype dtypes to_json to_json
sort_values transform iloc iloc groupby apply transform reset_index drop
astype astype apply join
rename groupby last groupby transform first
DataFrame sort_values
DataFrame T
index rename columns to_dict
itertuples join columns join columns itertuples
DataFrame columns DataFrame columns index iterrows items loc
loc
index index index
info info
groupby mean idxmax
index product index product product
isnull notnull notnull drop isnull notnull notnull fillna applymap notnull
DataFrame apply groupby transform sum groupby groupby apply reset_index
append DataFrame at at index index index append append DataFrame
index isin shape
rank groupby transform rank sort_values groupby add rank
groupby transform diff ne bool
columns last last columns append columns columns columns columns
abs abs
merge drop_duplicates
dropna
DataFrame astype
query index index query index index sum loc query index index sum items loc query index index sum query index index sum loc DataFrame query index index sum rename items items
DataFrame
DataFrame apply get dropna DataFrame values index index drop
set_index groupby agg mean sum pivot_table index columns values
var index index var
iterrows iloc iloc
add
groupby sum groupby sum pivot index columns values sum fillna sort_values pivot index columns values
groupby agg sum sum mean
index index
columns columns columns
index DataFrame index index index index index index index index
notna notna notna isna loc loc apply loc apply
DataFrame shape index columns DataFrame shape index columns corr apply
set_index groupby resample asfreq drop reset_index min max set_index reindex reset_index groupby transform max groupby transform max groupby transform max replace
DataFrame from_dict
sort_values groupby diff transform last sub transform first sort_values groupby diff transform iat iat
DataFrame DataFrame to_csv columns head
sample DataFrame loc loc where diff diff where diff where diff merge all join merge apply all replace where iterrows loc drop columns diff
index index loc index loc index loc index loc index index index loc index loc index loc index loc index
explode value_counts count explode value_counts sort_values tail
join groupby agg add_suffix join groupby agg add_suffix join groupby agg add_suffix
groupby aggregate join
append append
loc idxmax at idxmax
get count groupby count
merge where count
groupby groupby groupby to_csv index
values values values
all columns columns merge all columns reset_index drop all columns all columns columns reset_index drop
join value_counts join items join join DataFrame stack value_counts join items
columns columns columns isin any isin any
merge where
iloc iloc
T duplicated reset_index loc index columns drop drop
where where
DataFrame append DataFrame
abs index drop
to_period set_index drop groupby mean where
isna loc loc loc loc ffill fillna
count join append
DataFrame columns apply replace itertuples DataFrame from_dict index rename columns sort_values join loc loc index isin sum sort_values
fillna add rename set_index to_dict merge merge DataFrame T apply join
sum sum iloc
lt lt lt
iloc iloc shift all reset_index drop
astype assign duplicated
DataFrame values DataFrame
DataFrame columns
DataFrame columns DataFrame columns join items first items all keys all keys loc
fillna apply
mul apply
astype astype loc groupby idxmax drop
set_index stack reset_index drop reset_index ne drop set_index stack reset_index assign ne drop
loc apply
at size at DataFrame groupby apply join at le
pivot_table index columns values first reset_index
mask loc mask
pivot_table index columns values unstack rename reset_index
apply join
join join merge drop_duplicates
DataFrame DataFrame explode explode groupby apply reset_index groupby agg reset_index groupby apply keys reset_index
join pop apply join
items DataFrame
iloc info apply apply replace apply info explode merge
apply copy
apply
index values loc values apply
max append
index set_index index loc values loc values values where sum loc iloc reset_index drop loc iloc reset_index drop groupby sum loc values index reset_index loc values loc values where iloc reset_index drop loc iloc reset_index drop groupby sum values
pivot_table index columns
DataFrame merge
first index index loc iloc loc loc groupby apply
loc columns duplicated loc columns duplicated loc columns duplicated loc columns duplicated fillna
set_index set_index astype isnull isnull isnull isnull
set_index values set_index values columns update loc isnull fillna replace sum update loc isnull loc isnull
pivot_table index values columns columns columns
apply
pct_change mul columns
groupby agg sum first first
columns columns droplevel loc columns duplicated
DataFrame DataFrame apply astype merge loc
DataFrame size columns T apply nlargest idxmin T apply nlargest idxmin empty first first first first DataFrame size T apply nlargest idxmin columns values columns values columns values DataFrame size T apply nlargest idxmin columns values columns values columns values
apply iloc
assign groupby transform join drop_duplicates rename columns
columns eval
query shape
empty DataFrame
duplicated append
sort_values groupby cumsum sort_values groupby apply
plot
reindex index min index max set_index groupby apply drop reset_index
assign
merge loc merge loc
groupby sum
DataFrame append append
index
values DataFrame values values index head resample sum values head tail
eq groupby transform any where
sub replace index get values values append values values join values sub values values values join values replace squeeze values
values values reindex columns columns
mean max min DataFrame T columns index agg columns columns
DataFrame add add shape shape add shape columns add shape shape values
values merge fillna values
set_index loc eq index
values mean std
duplicated first fillna
to_csv index
loc sum loc eq any sum
loc
isin mean std query mean std isin mean std query mean std isin mean std query mean std
mask sum mask values
columns sum isna nunique append DataFrame columns values
set_index reindex ffill fillna iloc index index groupby apply reset_index
reset_index merge set_index join set_index
groupby transform size astype DataFrame index groupby transform size astype
ne shift cumsum groupby mask shift mod mask mask shift all update iloc index update where mask eq where replace fillna where mod cumsum mask
groupby transform
set_index groupby unstack reset_index
loc notna all
add columns to_csv
DataFrame loc isin groupby size count reset_index
sum groupby transform sum
loc isin groupby value_counts unstack
combine_first groupby apply expanding apply interpolate
ffill fillna
size size
info info max groupby transform max index drop index reset_index drop info loc info
set_index DataFrame index mean drop columns assign values reset_index groupby apply mean reset_index assign set_index DataFrame index mean drop columns assign values reset_index mean std groupby apply mean reset_index mean std
join replace
keys columns set_index DataFrame values index index stack reset_index
cumsum isna isna shift groupby transform min ffill fillna DataFrame size columns loc sample index cumsum isna isna shift groupby transform min ffill fillna
columns columns
index index asfreq assign query query ffill
DataFrame abs idxmin
iloc columns div iloc columns mul
join
merge set_index stack sum fillna
DataFrame reset_index index
DataFrame index index iloc index index values
shape shape
index DataFrame reset_index copy columns rename columns values index DataFrame index index index DataFrame drop columns
loc loc astype
DataFrame
head astype drop columns set_index
reindex rename join
get index index take first take iloc
eq eq DataFrame index index columns columns mask mask style apply to_excel index
mask merge merge merge merge loc mask merge merge loc merge merge merge merge
pivot_table values index columns fillna astype
iloc applymap update
DataFrame
columns iloc ne any iloc
apply loc loc
join apply apply astype
DataFrame fillna astype reindex max reindex
fillna ffill shift notnull astype notnull astype cumsum loc
swaplevel rename columns count rename_axis columns reset_index
to_excel
isin where groupby transform sum loc eq
get append DataFrame to_csv
size DataFrame melt dropna rename columns reset_index drop index groupby agg count reset_index dropna rename columns count columns join columns
pivot pivot_table values index loc index
empty empty merge
reset_index
DataFrame columns DataFrame columns
merge index values drop
query index empty DataFrame columns index index index DataFrame columns index append query index
max min
iterrows apply
var loc var loc isin info values
columns iloc iloc
where
groupby transform DataFrame first first first last last bool
DataFrame columns boxplot iteritems
columns columns columns any rename columns
columns count cumsum to_csv index
groupby last reset_index
iloc
DataFrame abs stack nlargest rename rename_axis reset_index
index iterrows columns apply
count DataFrame count mean DataFrame mean std DataFrame std min DataFrame min any quantile DataFrame quantile max DataFrame max
duplicated duplicated duplicated
DataFrame append append append
merge isna
columns astype
DataFrame columns copy
apply
DataFrame apply
groupby apply sort_values groupby agg
apply
apply
bool bool
apply replace astype info DataFrame columns columns dtypes
max cumsum
astype
DataFrame drop_duplicates
join
copy duplicated copy add groupby diff first diff sort_values groupby apply iloc filter where diff diff diff DataFrame values sort_values groupby transform apply diff groupby transform first filter where diff diff diff
get append DataFrame columns
loc loc
groupby apply quantile
reset_index drop values reindex
append max DataFrame first index columns values copy apply all max all apply columns DataFrame columns to_excel
all columns
mean DataFrame
melt groupby size reindex index index index
loc isna loc isna
value_counts min groupby head
DataFrame head DataFrame head
reset_index
astype astype apply mean std apply mean std
sample DataFrame
melt plot
index groupby transform mean
eq any eq any
apply
equals DataFrame DataFrame items get items get DataFrame values add append DataFrame equals compare keys values values
apply
any
any any any any
DataFrame index index columns columns iloc style apply
dropna
all count loc count sum count at count sum all assign count at all count at
DataFrame iloc index
sum mean mean
pop pop rename columns
DataFrame
apply
DataFrame drop index drop index
items get DataFrame explode reset_index drop append DataFrame
groupby transform sum where
stack dropna value_counts dropna
join join drop_duplicates join groupby agg join join rename columns droplevel join reset_index drop DataFrame DataFrame
replace apply
eq all eq sum eq sum
DataFrame columns values values loc
items DataFrame from_records columns value_counts loc
columns
set_index div set_index sub mul reset_index count mean min max
loc
DataFrame T T T T
groupby agg count sort_values iloc groupby count sort_values iloc groupby count
pop
shift
pct_change groupby apply
items
groupby apply
DataFrame columns index iterrows DataFrame index loc loc loc loc loc loc
values groupby transform sum
columns first columns sample columns sum add columns
loc eq eq loc ne ne loc
where sort_values eq idxmax
append
to_hdf where
get append div attrs append DataFrame to_csv mode
set_index set_index mask index isin index loc mask
iloc columns apply
mask gt gt assign where mask assign where mask groupby transform quantile mean fillna
unstack columns columns
to_dict rename columns assign groupby
sample DataFrame DataFrame index set_index set_index resample interpolate resample mean
pipe pipe pipe pipe pipe filter DataFrame from_records DataFrame from_records DataFrame from_records DataFrame from_records
drop_duplicates sort_values
copy loc transform loc
round applymap
loc
mask groupby transform loc mask loc mask astype sum loc mask loc mask groupby
apply
pivot_table index columns size pivot_table index index columns size
append
query query get to_csv query
DataFrame assign items
DataFrame index iterrows replace to_string
index
DataFrame
loc last last loc min last sum mean sum mean index columns sum mean
values index index max sum loc values
iloc melt sort_values drop sort_values
DataFrame round DataFrame round columns DataFrame round columns
all any iloc notnull all groupby cumsum apply join
apply assign
groupby filter index min groupby filter index min filter groupby filter index min
cummax shift fillna cummax shift fillna all
merge
drop_duplicates values drop_duplicates values drop_duplicates
DataFrame index columns
DataFrame pivot_table values index columns
apply min max lt gt append loc assign loc sort_values
DataFrame values items fillna DataFrame values transform items fillna
query DataFrame values query values query values values index loc
loc max shape
sort_values set_index groupby T rolling sum values sort_values groupby T transform rolling sum
columns iloc
columns values
loc loc loc
groupby transform diff dropna all
join first last empty append append
DataFrame index index groupby agg max index groupby agg mean index groupby agg min append apply round
DataFrame columns columns values columns rename columns columns columns columns columns columns columns
values set_index index index
first mask loc mask first mask loc mask
loc
shift filter
shape
join append append join DataFrame
query join items query join items query join items
keys append DataFrame columns
iloc groupby apply mean reset_index drop
DataFrame from_dict reset_index melt index dropna
groupby isnull iloc isnull iloc iloc iloc iloc index loc loc loc loc
groupby agg size min max
aggregate
DataFrame columns T rename_axis reset_index drop
DataFrame all join transform
DataFrame DataFrame from_dict columns columns
sum sum groupby agg sum plot groupby agg sum index
merge merge
explode loc groupby agg
loc
DataFrame index values index values index values loc sum
replace join
replace size isin
DataFrame columns
merge columns columns drop columns assign
T set_index DataFrame index
merge
set_index set_index
loc append loc
groupby filter
merge assign merge ne fillna mask eq columns bool
mean mode fillna apply pipe
apply
DataFrame columns index values
sample mean mean std groupby count apply unstack reset_index size append count DataFrame
shift columns columns iloc shift columns columns iloc
isin isin
loc plot
apply
get all filter to_csv index
pivot_table reset_index mean std groupby agg first mean unstack droplevel index agg reset_index index mean std
DataFrame columns index apply
reset_index reset_index get groupby groupby values diff ne eq min diff gt
any head
DataFrame groupby apply join reset_index
loc index values
DataFrame index DataFrame index ne compare DataFrame index ne ne ne where all values
reindex index groupby reset_index drop
astype set_index unstack columns columns join reset_index replace groupby first sort_values groupby diff transform mean mask duplicated last columns loc loc groupby ffill sort_index
count columns set_index columns
astype astype
last
mask
join join drop join DataFrame columns astype astype astype
groupby agg mean std swaplevel round astype mean std
join DataFrame rename columns replace append reset_index pivot_table index columns values
eq groupby shift where eq where groupby ffill
diff eq sum
DataFrame apply sort_values
attrs attrs attrs
assign duplicated bool assign duplicated bool
items __iter__
mask mask any loc mask at all any
columns loc append append DataFrame
query sum count sum
values values
DataFrame values astype values astype
groupby apply
groupby sum
gt all
DataFrame transform keys join keys items DataFrame transform items
columns select_dtypes columns columns columns value_counts count values value_counts duplicated all columns append drop
DataFrame
keys
gt groupby mean
columns columns
abs select_dtypes all
eq rename columns set_index loc eq rename columns set_index drop columns reset_index drop
last last iloc iloc stack
get append DataFrame columns
DataFrame drop columns columns
groupby agg min max rsub min max
pivot columns index values plot pivot columns index values plot plot plot plot plot plot
query isna
isna isnull mask isna any mask isnull any mask bool
apply DataFrame columns astype apply apply apply
loc
isin
all where gt all where shift gt shift gt all where where mask astype diff eq cumsum where reset_index drop groupby gt shift gt shift gt astype diff eq cumsum where reset_index drop groupby
select_dtypes columns apply replace
append
max columns to_excel
count transform count
groupby size to_dict T
values max values max
loc index assign groupby
set_index set_index mask ne loc mask index loc corr loc ne ne loc index loc index loc corr loc
columns shape
fillna assign ne shift cumsum take max any groupby apply max ne shift bool groupby apply reset_index
DataFrame columns DataFrame columns columns columns columns columns columns columns
apply
DataFrame columns isnull sum count count count
DataFrame columns duplicated
max values
groupby loc transform max
shift index loc index loc
merge
index reindex ffill reindex ffill
join join
at replace at append DataFrame
DataFrame index DataFrame index DataFrame index
groupby transform size sort_values
max max
apply
pop apply apply pop
merge columns columns mask isna drop
merge
index apply
pivot_table index columns values sum pivot_table index columns values sum columns columns join columns columns join reset_index
assign replace groupby sum where isin sum assign replace groupby sum where isin sum mean std assign reset_index melt index assign groupby sum groupby index sum mean std
index index index index
DataFrame replace replace DataFrame replace DataFrame DataFrame replace replace
sort_values reset_index
DataFrame index
index apply stack reset_index reset_index drop reset_index value_counts merge drop head groupby transform sum groupby transform sum iloc isin
loc
groupby apply DataFrame iloc reset_index drop set_index DataFrame index index groupby groupby apply DataFrame iloc reset_index drop mean std groupby mean std
merge
sort_values groupby rolling sum reset_index sort_values rename columns
assign dropna where eq
DataFrame mask duplicated first loc mask append loc mask
assign assign rename columns
sum sum pivot_table index values columns last
filter stack reset_index drop reset_index loc values loc values
count count items DataFrame from_records
loc index loc index
where
index isin index isin index isin index isin
items apply
diff set_index
sample replace sample replace iloc iloc columns
all sum index
columns apply DataFrame from_records columns dtypes apply dtypes columns columns columns columns
explode join DataFrame index index groupby agg dropna
values
stack rename_axis reset_index abs abs shift abs shift max rolling mean apply reset_index groupby apply sort_values
iloc apply value_counts
groupby size index
DataFrame append keys
groupby sum assign loc isin groupby sum assign loc isin
append
groupby first join
loc values
astype
replace median mean loc median
where ffill shift groupby apply reset_index drop fillna
columns rename columns dropna append dropna
DataFrame iterrows append append
size size style
DataFrame columns values values
pivot_table index values max min
values
values values sum sum
transform
drop_duplicates filter rename loc isin DataFrame
filter idxmax
drop index
groupby first reset_index cumsum merge columns fillna ffill drop head
merge groupby agg mean sum max min first last columns join columns join dropna add_prefix reset_index
columns index columns index columns index
DataFrame T iloc columns
groupby T sum groupby T agg sum
shift loc
mean groupby index apply
DataFrame
DataFrame DataFrame replace
set_index stack groupby set_index append unstack droplevel
DataFrame query DataFrame T append
set_index set_index columns
index var at var var var
apply all columns apply max min mean DataFrame columns
sum astype replace astype values astype replace sum values empty shape bool
merge
keys
DataFrame columns index loc
DataFrame columns apply mean std values DataFrame astype mean std
sample DataFrame size index
index add groupby apply apply drop drop head
replace replace
groupby sum sum groupby at groupby size groupby sum
DataFrame
DataFrame resample mean
apply set_index to_dict
DataFrame columns values values max values
groupby transform any
DataFrame iloc fillna
DataFrame values DataFrame reset_index drop reset_index drop
dtypes dtypes combine_first dtypes combine_first dtypes iteritems astype DataFrame DataFrame set_index to_dict apply keys
groupby agg sum
loc
loc index assign reset_index drop
index append astype bool size astype bool size
rename
columns columns droplevel drop ne
DataFrame index
append sum append sum DataFrame
DataFrame where first explode groupby agg explode dropna groupby first sample merge assign duplicated merge drop columns fillna
apply replace where
index index values values add get index values index append index values iloc index index index
columns melt replace max columns melt replace merge reset_index
DataFrame columns set_index set_index join fillna
loc index loc index
groupby diff where isnull cumsum
DataFrame reset_index DataFrame reset_index values isin append append iterrows values append T T
stack index
DataFrame size DataFrame rolling dropna T columns
groupby count sort_values groupby agg dropna
eq filter items sort_values
groupby mean items
DataFrame DataFrame set_index combine_first set_index reindex reset_index
hist
T min max min T
index index values shape shape
get dropna items loc
index reset_index set_index drop
columns DataFrame apply index
DataFrame
loc isin loc isin
apply sum values apply
index iterrows
set_index append stack groupby groupby apply join
sum loc any values DataFrame sum loc any values count head count cummax astype max sum values values DataFrame
DataFrame rename columns sort_values
DataFrame columns set_index reindex fillna DataFrame columns set_index reindex fillna T
values max add at
first assign where groupby transform min
DataFrame astype apply apply join
DataFrame DataFrame columns to_excel index DataFrame DataFrame columns to_excel index
product DataFrame product DataFrame product
groupby
index groupby index nunique index reset_index groupby nunique
astype
DataFrame columns sum sum append DataFrame columns sum
groupby apply tail mean
set_index apply sub set_index apply fillna reset_index
DataFrame index columns
index columns product reset_index
copy assign
unstack T columns join columns
index index index assign reindex
plot
DataFrame groupby min reset_index drop groupby idxmin index DataFrame groupby idxmin groupby
pivot_table index columns values
DataFrame sum groupby sum
columns columns sort_index rename_axis
DataFrame DataFrame rename columns
pivot_table index columns droplevel
loc groupby loc groupby DataFrame sum mean median groupby apply reset_index groupby apply reset_index
DataFrame to_sql index replace
get isnull apply isnull apply mask loc mask
loc index count assign groupby add astype radd drop count reset_index drop
T columns drop DataFrame dtypes astype round
mask values mask index mask loc mask max
pop apply apply pop stack reset_index set_axis keys values join reset_index drop
DataFrame values columns columns DataFrame DataFrame values columns columns
groupby
set_index combine_first set_index combine_first set_index
apply
DataFrame values columns
add groupby transform cumsum
drop_duplicates last sort_values
merge drop
DataFrame
add update replace
merge
sort_values
DataFrame index columns index index sort_index resample bfill index
assign mask
append
DataFrame columns DataFrame
get to_csv index
DataFrame columns DataFrame size columns sample replace value_counts sum
add to_csv index
transpose T T
groupby transform max sub eq drop_duplicates
DataFrame columns reset_index
set_index unstack pivot columns columns replace reset_index pivot_table index columns mean get median max columns columns reset_index replace
drop_duplicates merge
replace replace
assign astype sort_values index iloc empty index iloc empty
first drop drop drop shape drop drop at columns columns drop columns columns columns drop columns shape columns drop
index drop drop
set_index apply stack loc rename_axis reset_index set_index apply abs reset_index
std std size
sort_values sort_values
sort_index
combine_first astype
groupby transform eq all transform where transform
DataFrame sub join apply sub
combine_first combine_first drop values notnull drop
where where
groupby shift groupby shift groupby shift loc loc drop groupby shift fillna
merge sort_values reset_index drop
join join values
DataFrame DataFrame fillna DataFrame iloc iloc astype shape
isin astype
columns append append DataFrame columns columns
stack value_counts
fillna where isnull isnull isnull isnull apply
replace dtypes bool bool
values values append
DataFrame append
DataFrame head DataFrame set_index index DataFrame min max max groupby apply reset_index drop set_index
loc isin
fillna astype
DataFrame values mean index index columns columns
DataFrame DataFrame merge
DataFrame apply
abs append values
sub
isin isin apply apply values values DataFrame DataFrame isin mean std apply mean std mean std isin mean std apply mean std mean std
DataFrame
copy groupby shift groupby transform append where
join drop_duplicates replace index combine DataFrame columns iterrows replace replace combine product count append append DataFrame from_records columns to_pickle to_csv DataFrame from_records columns to_pickle to_csv rename rename join rename rename index insert rename rename join
merge combine_first
join resample mean fillna backfill DataFrame DataFrame columns columns shape shape set_index set_index between_time between_time between_time between_time join resample mean fillna backfill dropna head plot
DataFrame columns DataFrame append append DataFrame columns
columns
groupby agg sum sum size sum
drop
apply replace astype
groupby apply xs to_dict index
index
reindex set_axis to_dict values
any T
groupby columns columns agg sum groupby columns columns agg astype sum
columns index index
pivot index columns values fillna applymap columns columns apply
values DataFrame DataFrame values values DataFrame T append append append fillna
groupby sum get columns reset_index
DataFrame columns groupby sum where dropna
groupby agg sum mean reset_index merge
reset_index merge reset_index set_index sort_values
astype
iloc stack index reset_index groupby agg dropna index iloc apply
items
DataFrame DataFrame mask diff fillna eq groupby mask cumsum append append append
merge drop_duplicates
lt shift cumsum
index loc
first DataFrame T DataFrame T first where first merge notna first where first merge drop_duplicates first columns notna first first first first where first
loc values
to_dict to_dict
apply eval index apply index
melt merge set_index drop unstack columns columns
join
iloc count eq max sum count max sum count iloc max max count max max sum count
append append
DataFrame columns DataFrame columns DataFrame columns DataFrame columns eval eval eval abs DataFrame eval eval eval eval eval eval index eval eval eval eval eval eval eval eval DataFrame eval bool eval eval values keys eval eval eval eval eval eval eval pop pop iteritems get eval eval eval bool eval query mean std mean std
DataFrame loc
items to_csv keys keys keys keys keys keys eval keys
DataFrame
append append
loc iloc columns iloc where iloc
to_csv
sort_values sort_values where lt drop columns
agg
columns columns reindex max columns reindex max index interpolate interpolate get loc T
replace apply
DataFrame groupby groupby groupby groupby
columns groupby apply to_dict reset_index to_dict columns groupby apply to_dict reset_index to_json
melt melt melt
set_index loc value_counts index reset_index drop_duplicates
groupby apply loc max reset_index drop groupby transform max
applymap
astype sort_values astype sort_values
append
DataFrame values where isna values where isna
where index columns
groupby index agg max max max first
DataFrame abs
DataFrame
columns values query columns index query query replace keys rename columns columns query query query rename columns
filter groupby columns nunique ne diff dot columns
append append info info info info info info info info columns
min max min max insert where set_index reindex fillna columns fillna sort_values values
last last last last last last last keys append insert keys
index iloc equals iloc
apply dropna set_axis columns shape reindex columns
isin isin isin
DataFrame append
keys keys keys keys keys keys keys keys keys compare columns keys columns values keys keys keys columns keys keys columns keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys keys compare columns keys keys keys
to_excel first index T columns where columns index to_excel first index T columns where columns index
to_dict
at iterrows iteritems keys keys append columns
diff bool
explode
loc loc loc loc loc loc
DataFrame DataFrame apply values
set_index
replace astype
apply
append DataFrame pivot_table index columns values reset_index columns DataFrame pivot_table index columns values reset_index columns
DataFrame eq isna isna eq all DataFrame eq isna isna eq all
DataFrame columns index columns loc loc
merge
values index append values
DataFrame columns append
fillna merge
round
DataFrame size
groupby count unstack droplevel pivot_table index columns values count
apply
assign assign fillna bfill groupby apply fillna ffill reset_index drop
DataFrame
count groupby transform count drop_duplicates first count
ewm mean shift
corrwith DataFrame index columns DataFrame columns columns index columns
merge merge pop
astype mean std astype mean std
set_index eq dropna columns
set_index drop sort_index where shift shift
select_dtypes columns
append append append append append DataFrame index T
DataFrame columns
DataFrame columns DataFrame columns combine isna combine isna
apply fillna ffill apply fillna bfill
replace
DataFrame count set_index rename columns drop reset_index melt append count count head to_csv
loc values
DataFrame columns
melt min max DataFrame shift iloc
shift shift shift shift shift
merge
size iloc append DataFrame
groupby sum unstack columns columns join index merge
values
replace apply DataFrame set_index any DataFrame set_index any bool bool where where replace apply where DataFrame set_index any reindex columns
copy drop_duplicates last
stack reindex index reset_index drop
groupby columns apply apply join
DataFrame set_index groupby apply rolling sum
apply insert loc any values loc
set_index stack reset_index rename columns merge
notna astype
diff fillna diff
DataFrame index rename columns
value_counts dropna round sum plot iloc
append
drop T index index groupby index index sum sum index index DataFrame index sum values drop sum index index groupby index index sum index index DataFrame columns sum rename_axis
append iloc
apply any dropna index apply dropna
astype astype
bool
groupby
columns columns reset_index
astype columns nunique
apply loc isnull
apply
loc values
append get get DataFrame
where
columns filter isin
set_index reindex reset_index
fillna columns
DataFrame append append append DataFrame columns to_csv index
groupby filter
reset_index sort_values set_index
loc count replace loc count replace
diff cumsum cumsum sort_values groupby apply
set_index loc merge
replace replace replace replace replace apply apply
append
DataFrame columns columns loc isin sum isin sum
index iterrows get
join unstack add_prefix unstack add_prefix join DataFrame values add_prefix fillna DataFrame add_prefix fillna join DataFrame add_prefix fillna
astype DataFrame astype columns join add_prefix filter columns rename columns replace melt pipe assign assign sum filter sum sort_values
replace eval replace loc mask eval pipe pipe
where isnull where isnull
DataFrame apply join columns
mask index isin values loc mask loc mask index
groupby agg count sum groupby agg count sum
astype set_index
groupby nunique sort_values
columns mask isin duplicated
append DataFrame
join
lookup lookup lookup lookup
apply where index notnull
loc
mask groupby size gt count mask sum
get
pivot_table index columns values sum reset_index
groupby agg join iat iat
sample groupby join
DataFrame DataFrame join DataFrame pop
groupby size unstack reindex astype bool
DataFrame index columns DataFrame abs index index columns columns index index columns columns
pivot reindex index index columns columns fillna
DataFrame loc sum apply DataFrame sum explode DataFrame index index sum
set_index fillna
astype diff astype cumsum at groupby filter all min groupby filter all max
DataFrame size apply groupby size sum plot size
join size
unstack columns join columns values
DataFrame from_dict DataFrame from_dict
groupby apply join reset_index
apply append append
pivot columns values items first_valid_index dropna all
groupby agg sum min max
sum values join DataFrame add_prefix
any astype
columns DataFrame columns columns index index loc index loc index DataFrame index columns columns append
columns values values
DataFrame
DataFrame append append append DataFrame
index apply iloc index
pivot_table index columns values count
set_index stack reset_index drop rename columns
replace explode explode mean index
groupby apply reset_index mask all mask apply filter mask mask drop groupby apply reset_index drop
merge notna astype
get append DataFrame
interpolate index index index first set_index interpolate index values
eq mask where groupby ffill mask notna
DataFrame index columns DataFrame index columns DataFrame index columns
all columns dtypes astype copy values values any astype copy applymap bool applymap bool any any
set_index
fillna sum
take first
itertuples append DataFrame columns
groupby sum groupby sum reset_index
DataFrame size size size DataFrame
style columns values
iterrows
where isin
groupby rolling apply shift sum fillna values
isin groupby to_csv groupby
iloc max
groupby size
values insert DataFrame columns columns
var astype groupby cumsum var astype groupby cumsum var
loc
astype DataFrame values values values join pop reset_index drop rename reset_index drop
index drop rename set_index
nunique apply duplicated sum
columns DataFrame columns index columns columns style loc style applymap style to_excel
DataFrame columns all rename columns div sum values columns columns replace join
columns columns append
sort_values groupby mean rolling mean
filter values filter values values values join DataFrame index index add_prefix
loc
DataFrame drop_duplicates first loc loc loc loc loc loc
loc gt
get get
columns sum sum DataFrame apply sum sum all
head applymap
astype astype dtypes dtypes
groupby agg eq sum
merge
apply
loc cumsum groupby transform last mask gt eq mask bool
items DataFrame info info
all columns bfill iloc
set_index stack reset_index rename columns melt
DataFrame columns DataFrame append
index to_csv index resample sum DataFrame index index to_csv index
DataFrame columns merge
isin loc eq bool
reset_index set_index first index
DataFrame DataFrame columns reset_index drop
apply
groupby transform nunique groupby transform nunique transform count
groupby columns apply T fillna mean T
append
apply
values values items
replace replace replace
sum plot
append DataFrame
DataFrame iterrows to_dict groupby to_dict
columns sum iloc iloc reset_index drop index replace iloc index
columns fillna replace values apply count apply columns fillna replace values explode explode columns apply count apply columns fillna replace values reindex columns columns
loc loc loc
where
iloc merge merge query reset_index drop drop columns
items replace style
combine_first DataFrame index
groupby agg value_counts value_counts unstack
empty bool any all
DataFrame columns
index iterrows join
groupby ffill
truediv
diff eq notnull cumsum diff groupby apply where cumsum shift values
plot plot plot plot
values values min DataFrame shape min
to_dict to_dict
DataFrame DataFrame copy isin astype isin astype
groupby count first sort_values last values get last values values drop_duplicates nsmallest iloc index filter values join isin
apply join mask mask empty mask sample
groupby transform median
DataFrame astype
index values values values align add
set_index apply stack reset_index drop ge lt groupby index size append join
columns
groupby count groupby count
groupby agg sum eq sum reset_index columns
T eq eq eq loc loc
melt columns
where
DataFrame iat join join
all append append append append DataFrame groupby agg count sum rename columns count sum reset_index
DataFrame groupby apply to_dict reset_index rename columns groupby apply to_dict reset_index rename columns to_json
set_index drop loc set_index drop loc values
columns columns columns sort_index
reindex index max ffill
loc isnull loc isnull
items loc count
DataFrame join replace all replace add get
iterrows
get append get get append
DataFrame isnull astype groupby notnull astype cumsum sum values first isna cumsum cumsum where ffill fillna astype loc index count index first loc index loc index values values index update
DataFrame sum all first first add first all columns apply columns at all max columns columns
apply
values append index index index
groupby agg sum mode iat groupby agg sum value_counts index value_counts index sum groupby agg
loc
where diff
DataFrame add values iloc iloc add iloc add DataFrame DataFrame add index append
append iat groupby transform get iat get groupby transform
copy copy fillna apply
loc idxmax
axes groupby axes hist
set_index join replace apply value_counts fillna astype
DataFrame index index sort_values index set_index groupby rename append
merge eq astype where eq
loc shift loc loc
to_period to_period isin
DataFrame DataFrame astype assign assign rolling std rolling mean rolling mean rolling std where drop columns append loc sum values
DataFrame groupby agg count nunique query sum
apply
apply
groupby sum sort_values drop_duplicates last
DataFrame sample items append DataFrame sample items sample append head
DataFrame T DataFrame T columns columns index set_index set_index bfill ffill interpolate fillna fillna bfill fillna ffill
sample shape DataFrame index values index shape bool apply iloc
groupby index mask ne shift bool
to_string
sum sum index groupby apply
set_index rename_axis columns
merge merge assign
DataFrame index index min index max DataFrame
DataFrame columns append append
set_index iloc max empty values iloc replace max set_index iloc stack dropna groupby agg
loc loc loc where
index iterrows
groupby sum loc
loc
merge
mask index mask index loc mask ffill
merge min max where rename columns drop
DataFrame all empty columns dropna all dropna all merge loc combine_first loc values columns drop drop index
index loc columns
DataFrame
sum rename_axis reset_index index
apply
where where where where
replace astype
DataFrame columns
to_dict index items append
columns columns
all columns any any columns mask mask any columns any columns where mask mask any columns mask mask
merge fillna
apply apply explode
transpose
cumsum query groupby agg sum sum sum first
set_index groupby transform idxmin loc values
DataFrame columns index iterrows DataFrame append
xs loc loc
index set_index groupby agg first last min max columns
size size bool
apply isna sum DataFrame
shift eq eq sum shift eq eq sum shift eq eq bool
keys columns columns join notnull
iloc loc
apply
DataFrame replace groupby fillna ffill
items DataFrame columns
DataFrame columns loc
assign merge assign drop assign add all values add all values merge style join drop get product
DataFrame DataFrame groupby shape
join drop groupby sum
groupby apply to_dict
style apply to_excel style apply style apply style style
mask loc mask values loc mask iat loc mask
items DataFrame from_records columns value_counts loc
all values query get sub query sub min
assign shift sub
DataFrame values items columns pivot_table values index columns first
replace replace
notnull
apply explode
DataFrame index astype astype astype
columns iloc values iloc values iloc values iloc values iloc values iloc values columns iloc iloc any iloc iloc
shift groupby ne shift cumsum max last where any rolling max
join rename columns
get div align replace append DataFrame columns to_csv index
set_index
sort_values groupby diff groupby agg sum
DataFrame copy
merge reset_index rename columns index
pivot_table index columns values sum plot pivot_table index columns values sum plot
join groupby first add_prefix append
replace applymap applymap
DataFrame values values
groupby agg sum join
drop columns columns T
xs xs index index loc xs xs loc query first query index index items query items query items query query items query items query items query query sort_index loc query loc loc all query join join query query query loc loc loc loc index duplicated query keys loc keys loc loc loc index
DataFrame DataFrame values aggregate groupby sum reset_index
duplicated eq
get size groupby size size sample apply sample DataFrame sample columns index index size reindex index size reset_index
reset_index melt pivot_table index columns values reset_index columns index
groupby shift
astype groupby first sort_values drop columns
query groupby groupby
assign assign
set_index sort_index resample first asfreq isna index
isnull columns apply
eq where ffill
get max groupby max get max groupby max get add loc index append loc isin index isin
min sum groupby
loc shift
to_records
set_index eq all bool fillna set_index eq fillna all bool
DataFrame values values values DataFrame at at at
groupby
groupby get rename mean
replace
ne shift cumsum groupby agg first join ne shift bool
sort_values sort_values
groupby agg min max mean
DataFrame columns columns groupby DataFrame drop groupby DataFrame groupby update drop
DataFrame columns index columns value_counts
assign groupby agg mean std
apply
set_index to_dict groupby
pivot_table index values columns
iterrows append
DataFrame assign index merge sort_values set_index drop
loc loc apply where apply
DataFrame columns merge
set_index groupby unstack columns columns reset_index
loc isin values
explode pivot_table index columns values drop join DataFrame stack dropna droplevel rename pivot_table index columns values explode pivot_table index columns values reindex explode
DataFrame columns
explode explode set_index groupby resample ffill dropna reset_index groupby transform count groupby transform count
DataFrame
where eq cumsum where eq cumsum where isin cumsum any where ffill fillna
get append DataFrame columns head
DataFrame columns
DataFrame
info update append add
copy at max copy div get astype astype astype groupby count values empty
DataFrame any add DataFrame columns rolling apply
isin bool
min max round round append keys apply apply sum values
iloc iloc
drop columns
items index index reindex index DataFrame index index index sort_index
astype astype set_index set_index index astype mul astype astype mul astype set_index set_index index replace set_index set_index index replace
DataFrame columns columns corrwith abs idxmax
round round round apply round apply round
first items first
query reset_index drop mul assign index set_index
DataFrame columns columns
add_suffix assign
max
apply any
first all keys append keys DataFrame index columns all keys loc fillna columns index index query any sum
DataFrame index iterrows get at index
loc mask where
mask lt where
groupby agg isnull all dropna dropna empty groupby agg
DataFrame rename columns DataFrame rename columns
apply
sort_values sort_values join join
iteritems
where index columns index columns
replace get
mean
DataFrame append loc reset_index drop index
notna values values
reset_index columns count
shape values values
iloc shift keys
first last max min sum groupby index agg
astype values iloc columns
groupby transform mean loc
set_index loc reset_index set_index loc reset_index
DataFrame DataFrame DataFrame DataFrame
isnull columns apply join apply join isnull index
set_index index sub abs reset_index drop
values values mask loc mask loc mask
groupby apply
apply groupby agg mean
cumsum values drop_duplicates sort_values join groupby count groupby cumsum cumsum rename count sort_index
DataFrame append DataFrame set_index loc max loc min apply apply loc max min merge apply shape shape shape shape shape shape index max index min DataFrame append DataFrame sort_values sort_values to_records to_records size size shape shape shape shape shape shape
where fillna
transform sum items
where
groupby mean reset_index
DataFrame columns
DataFrame DataFrame set_index set_index keys plot set_index set_index columns set_index set_index keys columns columns droplevel
mean std groupby value_counts unstack mean std groupby size unstack mean std mean std groupby value_counts unstack mean std groupby size unstack mean std
DataFrame plot plot
to_excel to_excel
values merge rename columns drop columns
empty loc idxmax
join
DataFrame index index stack reset_index columns isin loc append
where any DataFrame from_dict size size size where any any astype
eq
dropna all dropna all
DataFrame index index ne shift cumsum shift iloc cumsum assign DataFrame groupby apply iloc iloc iloc columns
eq eq
groupby agg min max reset_index
rename_axis reset_index merge where
get
mean std mean std mean std apply mean std
shape drop
itertuples append query iloc info
to_dict values values values
merge apply apply groupby sum
abs
groupby size reset_index count groupby size reset_index count
eq loc mask diff lt ffill
append join apply
groupby items plot loc loc loc
DataFrame columns columns index index
where cumsum set_index unstack dropna all get groupby apply iloc
index
loc max loc min
loc
DataFrame items rename columns rename columns keys
where where join apply at
DataFrame pivot_table columns index
sort_values groupby keys shape values iat values append keys index values keys keys iloc keys shape keys keys keys loc copy
columns columns replace update update values
empty where append append append append append empty where all DataFrame columns append all columns
dtypes sort_index
DataFrame replace sum iloc
set_index groupby apply resample ffill
set_index index index drop columns
iloc iloc
eq loc loc merge
shift sum
set_index index columns columns stack reset_index index reset_index drop rename_axis
join keys
astype astype astype astype astype astype
index
apply loc loc
DataFrame dtypes DataFrame DataFrame values astype columns DataFrame dtypes columns columns var values apply DataFrame columns
value_counts index isin
loc isnull loc isnull index index loc isnull
dtypes rename iloc rename first agg min max mean T first max mean min
values groupby agg count mean reset_index
merge fillna ffill
DataFrame items
loc
groupby size
merge eq
columns pivot_table index columns values first reset_index
DataFrame DataFrame merge
DataFrame index
set_index unstack sort_index
add_prefix
values
rename columns rename columns
DataFrame DataFrame replace size set_index
unstack T columns columns
DataFrame to_csv index DataFrame index to_csv index
groupby transform nunique
index values
groupby rolling sum values
plot plot
applymap replace replace
pop apply
iterrows iterrows at iterrows iterrows at append drop_duplicates first
xs xs
iloc ne any iloc count first drop columns ne any columns drop columns
shift cumsum astype
DataFrame columns DataFrame DataFrame columns
notna mask shift groupby cumsum ffill fillna
last iterrows last first last iterrows last index first last at last loc first first last at last loc last
DataFrame cumsum cumsum where sum where diff at cumsum values values values diff size values first cumsum cumsum DataFrame cumsum diff cumsum cumsum groupby cumsum cumsum cumsum drop cumsum
groupby agg mean count
columns filter columns apply bfill
where isin where isin
DataFrame columns DataFrame columns stack index stack get size columns index loc sum columns index index isin sum loc values append plot columns values keys
DataFrame from_records values items values columns astype DataFrame loc append
columns rename index columns
append sort_values columns drop_duplicates last
any first T values values T
div DataFrame columns attrs append DataFrame append DataFrame all
resample mean reset_index ffill resample agg mean mean mean mean mean reset_index ffill
sort_values set_index reindex reset_index
DataFrame columns DataFrame append
DataFrame
index index
DataFrame to_excel
iterrows at plot get assign get all columns all values loc copy dropna loc all values loc copy dropna loc iterrows at plot round plot values keys loc size plot
DataFrame size columns shift DataFrame size columns shift shift shift iloc DataFrame size columns rolling mean iloc
cumsum groupby agg join first
DataFrame columns columns index index append sample
shape shape
ne all le le ge ge
loc
iterrows
drop_duplicates
groupby sum unstack plot
isnull isnull sample dropna sample sample DataFrame columns count sample loc loc shape index index count count sum size isnull sum loc isnull
merge where duplicated
DataFrame groupby transform any groupby transform any
columns sum
drop dropna columns
sort_values astype diff fillna replace ffill groupby count groupby max
groupby transform fillna median
DataFrame iloc columns iloc columns sort_values groupby apply
dropna shape shape shape
DataFrame replace replace replace
all
to_records index explode drop join DataFrame columns index index
all resample last ffill resample max ffill resample min ffill astype resample min ffill resample max ffill astype drop
T values
fillna dropna
DataFrame DataFrame DataFrame mean std DataFrame mean std DataFrame mean std DataFrame sum mean std DataFrame explode mean std
melt query sort_values drop
iloc
to_csv
reset_index join reset_index index shift index index iloc iloc set_index index iloc iloc set_index iloc
drop count merge drop count
index iterrows loc index itertuples append
DataFrame first columns apply join first
pivot index columns values plot groupby plot
fillna mean select_dtypes columns fillna mean
DataFrame set_index index all get sub DataFrame columns columns columns columns loc loc
mode groupby apply mode reset_index drop filter mode groupby transform count reset_index
join groupby mean
DataFrame from_dict iteritems all columns
cumsum
stack reset_index rename columns astype reset_index reindex rename columns reset_index
apply explode reset_index drop
replace notnull any index loc
mask bool mask bool
last loc last last ffill
sort_values sort_values
columns
groupby drop items iterrows append
columns join columns columns rename columns DataFrame columns columns add groupby first rename columns loc loc reset_index drop columns
mask notna isna loc mask where mask loc mask loc mask
columns DataFrame
rename index columns pipe
rolling sum shift
append values values
keys keys keys DataFrame keys DataFrame rename columns keys
apply
index values dropna index merge loc sort_values index columns melt index dropna drop columns loc set_index index append loc rename columns set_index index append columns set_index append unstack droplevel columns rename_axis columns reset_index index
count count sum groupby agg groupby agg to_dict items count count count
melt size size shape head tail sum mean size size items shape head tail sum mean size size size size items shape head tail sum mean size size
DataFrame append append
bool bool apply
DataFrame bool dtypes bool
DataFrame mul apply apply mul
pivot values count columns index reset_index drop
fillna ffill astype min fillna ffill astype max
values values apply
columns columns columns
columns columns append
DataFrame columns style max
isin index drop
replace replace replace fillna DataFrame drop replace replace replace fillna to_csv astype astype astype astype astype astype astype shape size shape shape shape shape shape shape shape
product prod prod prod prod prod prod prod prod eq assign groupby product mask transform any transform all mask product prod prod prod prod prod prod prod product prod prod prod prod prod prod prod prod prod prod eq assign groupby product mask transform any transform all mask product prod prod prod prod prod prod prod
loc groupby transform sum div groupby transform sum
bool drop_duplicates groupby size eq bool
where
reset_index drop astype columns DataFrame values columns aggregate at groupby agg mode mode quantile lookup iterrows max mode values values
sum
groupby sum mean sum sum mean sum sum mean sum
DataFrame index swapaxes
iterrows append to_dict
DataFrame empty items append
ne mask bool
bfill iloc fillna
add
DataFrame columns DataFrame columns append reset_index
reset_index rename columns index reset_index drop ffill astype reset_index drop
where eq replace replace replace
loc idxmin iloc loc idxmin iloc loc idxmin
isin
DataFrame DataFrame apply merge
groupby first head index last tail index values values loc first T loc last values transform size loc
index iterrows at index
at std at std at
apply
loc columns join
any all
product reindex index product min max index min
DataFrame columns apply
iloc groupby apply join
loc duplicated ne loc ne shift eq mean std loc duplicated ne mean std loc index isin drop_duplicates loc index mean std loc diff ne eq mean std loc duplicated add ne mean std
groupby value_counts unstack groupby count unstack
DataFrame
iterrows append size loc loc
index
values join
assign merge assign drop merge empty DataFrame values values DataFrame index columns index columns at div min T plot
plot size plot size
max min
astype sort_values drop_duplicates
pop pop
index size size copy loc loc reset_index drop loc groupby index sum join loc reset_index drop DataFrame columns
apply
DataFrame drop stack drop_duplicates T columns columns columns
merge melt melt pivot_table index columns values
sample DataFrame columns index iterrows
hist groupby rolling mean reset_index drop groupby agg sum count rolling sum assign hist sum count hist hist
assign set_index groupby mean resample mean groupby
columns columns append columns first index index append DataFrame columns columns keys to_csv index
size size DataFrame groupby groupby values first first groupby sum columns all columns
to_excel index to_excel index
mask astype apply apply all
DataFrame DataFrame keys groupby loc
DataFrame index drop columns to_csv
astype agg join add astype radd
to_csv to_excel index
DataFrame index columns copy
DataFrame index index columns columns loc loc style T apply
DataFrame DataFrame
fillna replace
groupby transform max query assign abs assign groupby transform min query loc
loc loc
where notna
isnull all bool
boxplot
groupby groupby groupby DataFrame
astype loc gt shift gt shift shift ge shift add astype
set_index rename_axis stack unstack reset_index rename_axis melt pivot_table index columns values mean reset_index rename_axis
groupby size reindex keys sort_index groupby size reindex keys
DataFrame var var var var join
DataFrame columns loc isnull values
columns columns fillna agg join
sub rename rename reset_index
DataFrame T dropna loc dropna
keys set_index T rename_axis index columns
merge query
iloc
apply idxmax values index apply
drop_duplicates last groupby apply reset_index drop
where join
diff
melt
DataFrame columns
DataFrame add DataFrame copy
groupby groupby apply
values all astype astype all bool
index loc index sum index
loc sample index loc sample index index isin index isin where index isin where index isin loc loc index size index size loc loc
all isin sum join join
merge
replace replace
merge melt drop_duplicates reset_index drop merge melt drop_duplicates reset_index drop
copy DataFrame size copy DataFrame to_string index join
set_index groupby columns sum T
astype bool
DataFrame columns merge items
index loc
items all
interpolate resample interpolate resample at values any resample asfreq
DataFrame index
join join shift dropna any sum
set_index append unstack swaplevel sort_index reset_index rename_axis columns columns
loc loc loc
merge drop
DataFrame groupby apply isin items
ge any idxmax
astype replace astype
DataFrame update update update update
loc mean std apply index mean std loc loc mean std eq values eq values mean std
any fillna first diff sort_values groupby diff sort_values
where isin
clip
DataFrame DataFrame DataFrame DataFrame
loc loc max
DataFrame explode groupby agg
DataFrame columns pipe set_axis columns columns rename_axis columns groupby columns apply pipe set_axis join columns columns
DataFrame size DataFrame size merge size head
DataFrame
shift cumsum
first columns rename rename columns
min max assign groupby cumsum merge DataFrame columns assign sort_values groupby ffill dropna drop assign sort_values reset_index drop
mask where loc
assign merge assign drop columns to_string
T values T values T shape T
replace
columns min max columns columns index pivot index columns values reindex reindex columns interpolate fillna ffill columns sort_index
DataFrame get get get get get columns
keys
join DataFrame reindex fillna astype
DataFrame columns index DataFrame columns index drop
unstack join set_axis
dropna
columns columns
reindex sum
columns iloc where iloc set_index add loc
reindex index astype assign cumsum merge loc assign groupby agg drop columns
min max isna loc loc apply
query
DataFrame rolling apply rank values rolling apply rank values head
groupby transform std columns
loc columns sort_index reset_index drop columns
assign drop groupby diff gt cumsum groupby size max
values
DataFrame
agg join replace
items to_dict DataFrame columns items to_dict
join
where
groupby
eq eq all bool index values
style style to_html
get filter ne shift cumsum groupby
append
isin items
DataFrame columns
set_index get
DataFrame
groupby
groupby nunique reset_index groupby nunique reset_index
DataFrame values take
to_hdf
values to_dict set_index groupby apply rename_axis reset_index values values
loc eq
groupby apply sample reset_index drop
set_index index isna loc
min min min min min min min
DataFrame rename rename columns append
groupby diff apply fillna set_index fillna fillna
set_index to_dict set_index index
merge assign filter bfill iloc drop
groupby agg size
apply add lookup
rename columns rename columns
abs columns
index index loc index loc index loc index loc index loc index loc index loc index loc index loc index
DataFrame
groupby apply agg T melt
set_index groupby sort_index set_index groupby sort_index reset_index drop mask duplicated fillna
groupby transform diff
astype astype
replace replace replace
set_index groupby resample agg sum loc
loc loc isin isin
T DataFrame T shape
DataFrame diff diff abs loc plot plot plot diff diff diff diff abs
first first first apply apply
DataFrame copy drop pop copy transform copy copy mean copy DataFrame values DataFrame transform DataFrame transform
iterrows append columns iloc columns iloc iterrows index columns loc
columns columns reindex columns
set_index stack unstack reset_index rename columns fillna
append append loc loc loc columns to_csv index mode
loc columns apply
apply
set_index to_dict fillna
loc append loc iloc values index
swaplevel sort_index
apply apply apply apply apply
values values index
where join empty where where join
values shape values shape shape mean std shape mean std shape shift dropna values mean std DataFrame shape mean std
DataFrame columns index index head tail
to_csv index items
join join
size product all keys product size size any add
set_index fillna set_index fillna merge groupby columns first
max groupby apply explode
loc iat loc iat loc iat loc iat loc iat loc iat loc iat loc iat loc iat loc iat loc iat loc iat loc iat loc iat loc iat loc iat loc iat loc iat
DataFrame groupby agg count sum columns groupby agg sum sum
query
values append DataFrame values DataFrame values
DataFrame index index append index append index eval DataFrame index
merge
groupby isna sum dropna
astype replace
DataFrame columns DataFrame columns iloc values index columns append
ne shift ne shift any bool
append DataFrame append DataFrame
rolling mean
groupby transform mean
count value_counts to_dict items
columns
min sum apply apply
apply
sort_values groupby agg min diff max min dropna
any apply any apply get values items values
columns apply columns columns compare apply
DataFrame size size assign groupby apply quantile apply drop
index index astype index index
to_csv index
groupby agg sum max
replace DataFrame replace
apply last
DataFrame columns drop columns columns DataFrame size columns columns columns DataFrame size columns columns columns
loc at idxmax
cumsum
apply mean std apply mean std mean std apply mean std
groupby apply loc apply
where loc sum loc groupby sum shape loc itertuples combine sum filter loc where loc values loc sum values
index index
isna filter values values loc loc loc loc values replace loc loc loc loc loc isnull loc isnull loc isnull
iloc values iloc values iloc values
mask groupby cumsum eq mask groupby transform groupby transform mask cumsum eq mask
DataFrame columns
value_counts head reset_index count merge rename columns index groupby count mean reset_index sort_values count reset_index drop
DataFrame index columns
reset_index merge set_index index sort_index index index ffill index index
replace replace DataFrame replace pad
sort_values where shift shift
set_index
loc index values
iloc index iloc index iloc iloc
columns shape shape
eq cumsum index merge drop columns
astype rank groupby apply rank
rolling mean rolling mean rolling mean
groupby apply sort_values
query query gt query DataFrame append max min values values values min values max values min values max
isnull isnull all all copy isnull any any copy
merge query reindex columns
columns fillna rename reindex rename columns reindex columns append rename columns reindex columns fillna pop
size notnull cumsum where isnull size groupby transform size le fillna ffill where
groupby sum
join replace
groupby nunique index
sum notna sum pivot_table index values nunique
drop_duplicates last duplicated merge size shape
astype apply
DataFrame DataFrame mask eq eq assign values columns loc mask loc mask values convert_dtypes ffill to_string
groupby apply to_dict index groupby apply join to_dict index
reindex columns columns
DataFrame DataFrame DataFrame index values
columns replace columns
groupby dropna count
index loc
transform
drop_duplicates sort_values drop_duplicates sort_values
eq all eq
DataFrame index groupby index apply max min
DataFrame columns values columns columns DataFrame index index columns
set_index groupby unstack sort_index columns columns
DataFrame index DataFrame index reset_index columns
melt fillna drop melt assign fillna astype min
sum groupby sum reset_index
where assign add ffill values where ffill
select_dtypes columns fillna fillna apply fillna fillna
iloc
merge fillna merge fillna astype
unstack isin shift fillna
groupby diff where groupby transform size loc groupby idxmax
replace replace
value_counts plot sum sum
first at groupby agg sum count columns columns
iloc append iloc iloc
eq columns all groupby cumsum
groupby agg min max apply min max explode reset_index set_index reindex loc size isna index index rename columns groupby agg min max apply min max groupby agg min max apply min max explode groupby agg min max apply min max explode reset_index
groupby mad transform mad transform abs mean mad
loc index rolling apply
notna apply
DataFrame style max mode transform
groupby transform diff
ne mask groupby transform mean mask
groupby shift cumsum agg first first last query drop columns reset_index drop
set_index unstack to_string groupby astype set_index unstack to_string
DataFrame columns
keys values DataFrame from_dict index stack astype index droplevel groupby
join apply join apply sub
transform sum all values sum sum values transform sum sum values
dtypes apply iat iat iat iat iat applymap
DataFrame index value_counts replace groupby count groupby count
DataFrame value_counts plot DataFrame value_counts to_dict keys values values values value_counts to_dict keys values values values
values index sub sub index index apply
mask mask mask ffill where mask dropna set_index astype unstack reindex columns rename_axis rename_axis
melt count groupby transform count count drop count
eq shift eq any groupby filter groupby shift eq eq isin loc copy
ne bool isin loc ne bool DataFrame size astype size astype ne groupby transform sum astype bool mean std eq groupby transform all mean std isin loc ne mean std assign mask eq groupby mask transform all mean std groupby transform astype mean std groupby filter any mean std
set_index groupby apply reindex index min reset_index
merge loc
groupby keys apply values to_dict
DataFrame columns columns drop columns
append append append append append
shape compare values values values
DataFrame index columns set_index iterrows DataFrame index columns set_index iterrows
groupby agg count sum to_period to_period
groupby transform nunique set_index sum unstack reindex unstack swaplevel sort_index
apply
assign assign assign
to_csv
groupby groupby agg sum sum agg sum sum sort_index agg sum sum assign agg sum sum assign sort_index add append groupby agg sum sum assign sort_index
join join join
style index columns index columns index columns index columns index columns index columns index columns index columns index columns
sort_values groupby head set_index
max min min max min DataFrame columns transform DataFrame columns transform min max
merge
DataFrame DataFrame
apply mask loc loc loc
groupby value_counts to_dict
merge loc loc
min rolling apply min min values shift values shift values
sum
empty append DataFrame columns where
DataFrame astype
index append DataFrame columns
index groupby pivot index index columns values
values values mean values std values values mean values std T dot corrwith keys columns
loc any index mean std loc isin any index mean std
replace astype groupby mean groupby first drop columns groupby index mean fillna astype replace reset_index merge loc replace astype drop columns groupby index mean fillna astype replace
all values values apply fillna
merge
columns columns columns columns stack reset_index rename columns set_index
append
apply apply
loc loc
apply
count groupby count transform sum
mask lt loc mask
set_index to_dict items set_index to_dict items
apply columns to_dict
reset_index index drop loc groupby index groupby agg append join
transpose transpose
DataFrame
DataFrame columns
groupby apply assign sum
pivot_table index columns values sum loc reset_index
merge append
set_index index index iterrows loc index loc index loc loc append loc index DataFrame columns
ne groupby transform all isin loc eq
groupby diff fillna loc loc
all columns groupby filter groupby itertuples itertuples get le ge any append loc idxmax get first values append shape append shape join DataFrame columns columns
index iterrows T set_index T plot
shape shape shape shape
apply copy copy filter first apply groupby agg count sum min apply copy apply groupby agg count sum first reset_index copy index apply
set_index notnull mean mul applymap groupby agg notnull mean mul groupby agg notnull mean mul DataFrame
DataFrame shape columns columns index index
reset_index index groupby count unstack
filter get get index loc
set_index plot plot index
loc loc loc loc
apply max apply mean
columns rename columns rename columns join join drop drop columns
columns loc keys loc
groupby bfill diff abs cumsum cumsum ffill
apply all
DataFrame columns empty dropna all values fillna ffill first drop first rename columns iloc first drop index to_dict groupby apply to_dict
replace replace
columns join columns values join columns values sum sum sum sum
groupby loc
set_index apply loc drop nsmallest index values
resample interpolate interpolate
DataFrame columns DataFrame columns insert index
mask mask pivot index columns values mask rename columns set_index join reset_index reindex columns mask all where mask bool
rename pipe pipe pipe
copy loc DataFrame columns iloc iloc
isna astype eq astype
index apply get values
index index index assign loc loc shift mask mask mask shift
from_dict DataFrame from_dict
DataFrame DataFrame
DataFrame
columns columns
DataFrame pop append append
columns columns columns columns loc iloc replace to_csv index
DataFrame DataFrame columns to_period columns
pivot index columns values sum pivot_table index columns values sum
DataFrame values items values append values all sub sub append sub append sub apply explode reset_index drop index values iloc
mean update
explode get set_index groupby agg assign
columns DataFrame columns columns
DataFrame
groupby apply groupby sum idxmax unstack mode rename columns
DataFrame DataFrame shape append DataFrame DataFrame columns DataFrame shape append DataFrame
values drop index drop index drop loc index drop where ne values query eval DataFrame size ne
apply values shift shift take dot product dot apply
merge assign pop fillna astype
merge sort_values
DataFrame
merge
loc loc copy replace astype apply astype replace replace astype sort_index loc loc copy sub sub sub sub apply sort_index
join notna groupby sum
size sub columns columns columns replace columns columns loc
merge merge apply
index index append index index shape iloc index fillna groupby
columns drop_duplicates
cumsum
apply dtypes
DataFrame columns DataFrame size columns iloc info DataFrame columns columns values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values values dtypes
DataFrame DataFrame merge merge drop columns
DataFrame reset_index
DataFrame DataFrame columns append
replace ffill bfill where astype dtypes
columns loc astype to_excel
columns columns stack rename_axis rename_axis stack join reset_index
groupby count rsub size drop isna groupby sum set_index isna sum groupby count rsub size mean std mean std mean std
drop DataFrame columns columns index iterrows append reset_index drop
DataFrame groupby agg count apply sort_values apply at reset_index drop index append sum
melt reset_index drop groupby mean reset_index drop isin pivot_table index columns values
copy select_dtypes select_dtypes
DataFrame index columns DataFrame index rename
append columns DataFrame style apply to_excel
head style
any eq any
columns groupby agg mean sum sum sum reset_index
reindex index index DataFrame index index
where pivot columns values
groupby merge
groupby transform count
replace
stack duplicated any bool bool
eq sum idxmax
groupby median
sum sum count sum
product columns columns
update update to_dict update pop to_dict iloc T
set_index unstack
DataFrame T columns melt groupby pivot index columns values rename_axis index columns
DataFrame DataFrame set_index reindex values max min DataFrame DataFrame set_index reindex min max plot values
groupby mean join join groupby mean
first at loc iat loc set_index loc
iloc iloc iloc
groupby first
columns columns columns columns columns append columns drop
DataFrame join rename columns min max min astype max astype min max
cumsum cumsum
merge
groupby loc index apply sum astype get groupby loc index dot astype get
reindex values
DataFrame values sum loc count iloc
eq all eq eq all bool
set_index groupby sum
DataFrame apply
reset_index drop
merge fillna
squeeze squeeze
iterrows index loc columns index index iterrows values values values append
loc loc
loc get loc items DataFrame pop keys index add_prefix DataFrame pop keys index add_prefix join join reset_index drop append
isna sum sum
mask isna size
apply apply join apply join
merge groupby transform nunique gt any eq ne isin groupby
set_index set_index append
add
loc values loc values
size size size replace replace append to_sql replace index update DataFrame
columns dtypes
loc loc apply dropna
iloc eq sum
sum sum sum
replace head
ffill index index index
DataFrame shift apply
DataFrame columns
replace replace index index index index replace replace
groupby reset_index set_index pivot columns values count
DataFrame shape DataFrame product iloc columns apply reset_index drop
reindex columns columns
DataFrame columns append DataFrame columns
DataFrame columns drop_duplicates count groupby at count count groupby
diff rank rank ge rank ge diff sum rank
values items all values join values loc
DataFrame empty empty DataFrame merge
loc apply
values DataFrame DataFrame
DataFrame DataFrame astype sum isin astype sum sample sample
merge merge reindex columns columns groupby melt merge groupby filter size columns pivot_table index columns values join reset_index drop rename_axis columns
loc
values items DataFrame fillna T reset_index drop values DataFrame fillna T loc
replace ffill
DataFrame applymap values join groupby max DataFrame pop applymap values join groupby max
DataFrame
DataFrame replace combine_first
replace where value_counts gt
where where loc loc
values
columns assign items
set_index append merge set_index append
sort_values mask duplicated columns mask mask ffill
lt
columns drop drop_duplicates sort_values index
DataFrame astype astype
DataFrame columns max where dtypes isnull all all values columns groupby loc index loc index astype
join columns astype values values
DataFrame quantile append append append values head
melt groupby size rename apply value_counts unstack rename drop join
first columns columns merge dropna
first reindex columns
pivot columns values
DataFrame loc join
DataFrame columns
rename columns
dtypes
DataFrame size replace copy copy loc loc
apply where where
DataFrame isin
axes plot DataFrame
groupby apply shift ffill loc
join
columns append DataFrame
DataFrame index DataFrame index
melt groupby value_counts reset_index merge
replace replace
DataFrame isin DataFrame values isin
apply value_counts apply
groupby apply sum sum mean mean sum mean groupby apply sum sum mean mean sum mean
index interpolate plot index
copy loc loc sum append loc sum rename
sum DataFrame where first abs first
xs groupby
DataFrame cumsum values replace
fillna fillna DataFrame join
empty iloc
count gt sum
groupby shift cumsum agg first sum mean
loc loc
set_index items loc itertuples loc
loc mask
merge
join set_index rename columns
index
DataFrame columns drop index drop index drop index
values loc loc backfill values loc loc shift bfill
notnull where eq
DataFrame columns iloc iloc append
merge reset_index
set_index reindex
DataFrame items index keys columns DataFrame from_dict items index
DataFrame columns index reset_index iterrows append DataFrame columns
lookup index radd
replace iloc at
DataFrame head
head head
filter ne stack reset_index at mask fillna
head DataFrame empty head head DataFrame
isna apply all isna apply all diff cumsum fillna groupby sum groupby agg min max
DataFrame dropna DataFrame dropna
reset_index reset_index groupby mean
DataFrame
index first DataFrame index columns index aggregate columns agg columns groupby columns sum agg index columns columns to_string index first DataFrame index columns index columns dtypes dtypes items dtypes dtypes items columns sum first sum first dtypes items groupby agg to_string
index mean index loc mean mean round mean
astype bool
max max idxmax DataFrame columns max max idxmax max
iloc iloc
groupby sum
apply
loc iloc iloc groupby agg head loc iloc iloc
values ffill first columns keys rename rename columns groupby size apply to_dict rename reset_index rename columns groupby apply to_dict rename reset_index rename columns to_dict size size size size size size
eq bool eq iloc bool
groupby pivot_table index columns values first columns join columns sort_index reset_index drop columns
DataFrame DataFrame itertuples
columns T
groupby transform
append
index iterrows get append set_index
mask mask shift loc mask loc mask mask shift fillna astype bool reset_index drop
iloc
iterrows
merge set_index
DataFrame columns DataFrame columns merge
DataFrame index
DataFrame join
where where
merge
loc abs groupby idxmin reset_index drop
astype values
first drop_duplicates set_index last drop_duplicates last set_index last
apply apply groupby drop columns to_json apply apply groupby drop columns dropna all drop columns all to_json
astype astype astype
reset_index groupby apply isin index reset_index reindex isin index reset_index groupby apply
sum
columns DataFrame
transform copy
fillna astype apply join
get groupby size filter reset_index
apply explode DataFrame merge at all groupby apply
DataFrame DataFrame index iterrows get loc index values loc index values append shape loc index values append shape add
join set_axis
groupby apply loc duplicated squeeze loc duplicated squeeze
DataFrame from_dict keys keys index iteritems append append DataFrame from_dict index keys
DataFrame from_dict to_dict items
ffill isna max assign ffill dropna
to_json
DataFrame apply
apply
set_index set_index combine_first reset_index
apply apply apply
DataFrame set_index index
DataFrame columns DataFrame columns DataFrame columns index reset_index drop index
keys loc index
groupby append reset_index drop
size
values values values rank rank groupby rank min to_dict all replace where rank where rank rank rank
columns
replace set_index
groupby apply ffill bfill where
DataFrame astype DataFrame drop_duplicates first sort_index
fillna groupby columns apply max
groupby filter values
DataFrame columns size size size
loc fillna astype
apply value_counts
groupby agg join
values values values
DataFrame values items values columns keys values DataFrame keys keys values values explode values reset_index drop
fillna replace
DataFrame values merge explode mask mask eq isna filter agg rename mask groupby agg rename columns DataFrame values merge explode DataFrame groupby transform first sort_values drop mask mask eq isna filter agg rename mask groupby agg rename columns
isin astype isin astype isin memory_usage DataFrame size columns size isin astype mean std isin astype mean std notnull astype mean std values mean std
DataFrame iloc
copy copy loc index DataFrame index empty get reset_index drop index
DataFrame
columns dtypes value_counts isin index values
set_index to_dict fillna merge fillna drop columns
drop_duplicates
DataFrame mask isin isin mask mask
get columns
index set_index index index index rename columns T apply loc mean T
join style style join style style style style get append DataFrame columns
DataFrame index columns append DataFrame index columns
iterrows iterrows
all apply join dropna astype
isin isin groupby prod transform sum loc
DataFrame
resample last copy index groupby last groupby diff diff get where diff index add diff ne add loc add diff shift add diff add
apply
fillna fillna
rank groupby rank rank eq astype
first join reset_index take groupby mean reset_index columns add index reindex columns index columns
first first max min last reset_index groupby agg set_index
DataFrame columns index iterrows index index iterrows index index iterrows iloc index
values bool itertuples columns iloc columns columns columns columns items
DataFrame drop drop
join groupby apply unstack
apply cov
assign merge assign drop
groupby fillna ffill fillna astype groupby notnull cumsum
mask notna any
melt query drop columns
all items all DataFrame add columns isin isin isin keys isin
any
merge
groupby mean assign join mean DataFrame join mean
DataFrame set_index apply loc
columns get index values columns unstack values columns unstack values unstack columns unstack values join join drop join
eq cumsum groupby agg last last rename columns columns last last
get to_csv index
groupby sum groupby sum
sample groupby transform size transform first sample sample where filter filter filter filter
replace
merge fillna loc columns drop columns
insert count groupby transform count get drop_duplicates
mask isnull loc mask loc mask
index
DataFrame index columns groupby apply sum head columns shift stack keys unstack unstack iterrows
DataFrame combine DataFrame DataFrame append
duplicated duplicated
first
values
groupby apply set_index reindex min max ffill
index rename_axis count columns
columns drop columns columns
values query items
isna any any
join filter drop filter drop
DataFrame size add_prefix melt mean std apply value_counts fillna astype mean std stack groupby value_counts unstack mean std
melt columns explode dropna
dtypes
last shift groupby shift last astype
DataFrame columns first join DataFrame columns
apply all values columns columns apply
fillna loc apply
value_counts
diff lt diff shift lt astype diff lt shift astype
add columns shift shift shift empty values mean columns loc mean columns values loc values ffill ffill columns rename columns loc notnull columns
at at
groupby sum groupby sum
loc loc values
loc where loc iloc where loc loc plot plot
duplicated groupby agg nunique sum rename columns reset_index
DataFrame apply sub
fillna replace values fillna astype
loc isin isnull isnull loc isin isnull isnull loc isin isnull isnull loc isin isnull loc isnull loc isnull
first copy append shape columns add var var add columns empty info
columns columns assign values columns iterrows assign loc assign columns columns groupby loc loc groupby apply fillna ffill fillna where shift last last loc drop columns drop
add items
astype astype
value_counts astype reindex nunique join index join index astype groupby apply unstack reset_index rename columns groupby size rename reset_index astype groupby agg join assign count reset_index reindex columns
replace groupby apply fillna ffill fillna bfill drop_duplicates
groupby agg join values
DataFrame columns append reset_index drop iloc fillna rename columns
groupby cumsum shift all loc index
loc isnull isnull
DataFrame values
loc
apply replace
DataFrame DataFrame merge drop columns
iterrows values iloc values
DataFrame columns nlargest index
drop_duplicates set_index loc isin DataFrame T columns columns
reindex assign index loc sum reindex assign index loc sum loc value_counts all reindex assign index loc sum loc value_counts all loc
all groupby to_csv index first to_csv index values
sort_values groupby agg diff mean groupby max min count
duplicated index index index index index
loc isin
append
astype apply max
loc index count reset_index drop index astype loc
explode loc groupby agg reindex index
groupby product shift bfill product apply product
pct_change cumprod
groupby apply values
plot groupby value_counts mul reset_index stack plot
mask
last update keys
apply where
groupby to_csv index groupby columns columns to_csv index
index index columns columns loc fillna loc fillna
join
DataFrame columns dropna
DataFrame values groupby head
pivot_table values index sum loc pivot_table values index sum loc pipe
apply bool bool
items items DataFrame from_records index transpose
rename_axis index reset_index cumsum merge eq eq assign drop_duplicates sum eq eq assign drop_duplicates
sample
assign
where where fillna bfill
DataFrame size columns set_index reset_index any index values index values any values values mean std mean std mean std mean std
query query query join
cumsum mask bfill groupby filter groupby agg first last rename columns first last reset_index shape values astype groupby apply reset_index drop sort_values
columns isin shape
columns index astype columns index
DataFrame join merge where values values values columns
rolling sum index shape loc index where index index loc index loc index loc index copy copy where groupby iloc cumsum rolling sum rolling sum drop index align DataFrame reset_index drop drop all where sum fillna copy copy mean std copy mean std DataFrame size copy mean std copy mean std index index where index index index index index rolling sum values values values copy mean std
join prod sum
groupby apply assign shift reset_index drop sum sum groupby cumsum sort_values sum
replace
loc iterrows
mask count notna loc mask fillna
loc loc loc
index index index index index index
explode index explode DataFrame values explode index join drop explode DataFrame index index add_prefix explode join drop explode explode explode explode join drop explode DataFrame index index add_prefix explode join drop explode
groupby filter any any
isin DataFrame bool
values iloc applymap values empty iloc applymap
apply
set_index
sort_values shift rename columns diff replace drop
DataFrame head astype mean std mean std mean std to_period mean std to_period to_timestamp mean std mean std mean std
set_index index isin set_index index where
values to_excel index
DataFrame loc to_dict
append
loc loc isin loc loc isin loc
add groupby count unstack groupby count unstack columns columns
loc loc loc copy loc loc loc bool
columns append apply rename append
copy
merge corr merge fillna
replace append DataFrame columns
eq DataFrame where index index columns columns style apply
shift fillna shift astype bool loc shift astype bool assign cumsum all shift fillna loc loc fillna ffill astype bool groupby agg first first last
DataFrame get at join
groupby count reset_index boxplot
DataFrame reset_index drop reset_index drop reset_index drop
DataFrame drop
shape shape iloc
lookup index lookup index stack get index stack get index
any apply index values index values values any apply index values index values values any loc nunique
DataFrame astype groupby add astype
loc
values values values
get groupby mean reset_index
set_index astype copy rolling apply append value_counts reset_index DataFrame rename columns fillna astype drop DataFrame sort_values reset_index set_index astype copy rolling apply append value_counts reset_index DataFrame rename columns fillna astype drop set_index index DataFrame sort_values set_index astype copy groupby rolling apply append value_counts reset_index DataFrame rename columns fillna astype drop sort_values DataFrame sort_values rolling set_index astype copy rolling apply append value_counts reset_index DataFrame rename columns fillna astype drop loc append loc append loc loc rolling
groupby agg first sum
T columns iloc drop iloc index columns columns DataFrame values T columns index
values loc loc loc isin groupby apply
DataFrame keys values values
iloc iloc iloc iloc iloc iloc
DataFrame
last append
DataFrame columns append append append
count DataFrame loc count iloc
DataFrame columns itertuples index
applymap
index index index where index empty append iloc
set_index update set_index rename columns reset_index
groupby apply set_index idxmax
set_index where melt dropna merge add_suffix merge add_suffix index columns reindex index columns rename_axis index columns convert_dtypes set_index where stack reset_index rename columns
groupby apply
DataFrame join columns values
assign sort_values groupby shift
iloc iloc keys keys sort_index reset_index drop
apply apply all DataFrame columns apply all apply join
DataFrame DataFrame DataFrame
join append append append DataFrame T columns head
copy merge columns append isna sum copy update copy
merge
groupby add to_string index
DataFrame columns
apply head apply head groupby sum reset_index copy DataFrame loc apply apply values columns head
sort_values groupby diff astype
apply
set_index set_index
sample sample sample sample append
dot columns
mask filter ne sum ge mask
max
join index index apply replace all replace replace apply sub
DataFrame apply mean std size mean std
lt all isin notna where
cumsum groupby agg
DataFrame columns drop
iloc isin iloc
loc loc
index T reset_index pivot index columns count index to_csv count count count
loc update loc
astype iloc iloc iloc
groupby size unstack
astype apply DataFrame drop
DataFrame
index cumsum index cumsum groupby columns iloc drop iloc drop index append append
all iloc any items astype groupby apply tail T T T T T groupby apply groupby apply tail T T T T T
sum size mask mask groupby agg sum size mask mask groupby agg reset_index join
DataFrame columns DataFrame rename columns reindex columns columns
loc apply DataFrame index columns stack reset_index
diff bfill ne cumsum groupby agg min max mean rename_axis
iterrows
loc loc loc
columns loc apply
loc dropna loc dropna
keys keys DataFrame
append join
sort_values set_index stack set_index groupby append reset_index drop unstack fillna duplicated bool
groupby value_counts unstack
eq shift diff mask ffill
sort_values drop_duplicates first merge sort_values drop_duplicates last sort_index
DataFrame columns append mean std DataFrame columns loc mean std DataFrame columns loc mean std
drop_duplicates set_index sort_index
join
columns DataFrame index index columns columns index index index iteritems columns where where assign
agg to_dict
DataFrame
replace groupby last
groupby
index append index drop
append
fillna
DataFrame first first append DataFrame from_records columns
set_index groupby sum join
loc mask sort_values
append
applymap get style apply
astype apply
sort_index sort_index reset_index drop reset_index
at at at at
DataFrame columns at all groupby filter eq eq shift ne any reset_index drop
where groupby sum ge reset_index groupby sum ge groupby any reset_index groupby transform sum ge groupby any reset_index groupby sum ge reindex reset_index
first index index columns apply stack apply unstack index columns index columns columns DataFrame shape shape shape T columns shape shape
head head merge drop shift head shift diff head shift diff
fillna pad fillna sum DataFrame pipe
head
astype index rolling sum astype bool
isnull loc groupby apply fillna median
add to_csv index mode
replace mean std replace first mean std astype where notnull where mean std dtypes replace dtypes
value_counts index isin
shift iloc
DataFrame sub
merge
index values shape shape loc loc loc loc loc loc
values
sample DataFrame index sum max filter values loc index index rolling groupby index rolling mean dropna droplevel sort_index
apply
groupby value_counts loc
applymap stack replace empty replace reset_index get apply groupby index apply to_dict
DataFrame DataFrame merge notnull copy
diff abs diff sort_values diff duplicated diff
mul values
DataFrame select_dtypes DataFrame select_dtypes corrwith set_index index sum
copy values
groupby index index min index max agg mean std mean
loc loc loc loc radd loc loc
notna groupby transform any
explode
itertuples values
explode
apply all index
DataFrame join apply join join
index notna index
columns iloc drop apply transform drop apply transform
fillna mean std
update explode apply explode set_index append unstack
DataFrame
replace groupby ne shift cumsum sum eq sum
update loc where loc loc loc loc loc loc loc loc loc loc
apply
astype replace astype
groupby min min max max loc min iloc loc min iloc any max min max min max apply reset_index columns columns
values columns
set_index drop
max values replace append values append join last DataFrame index columns empty values replace join
query index values
DataFrame DataFrame columns DataFrame columns DataFrame columns
apply
max DataFrame merge fillna drop rename columns
DataFrame values columns columns DataFrame values columns columns
loc loc loc loc loc loc loc loc loc loc loc
where notna shift values loc loc shift update loc astype drop columns drop
query drop set_index query index index append append filter
DataFrame columns
groupby size reset_index groupby max
DataFrame index columns
merge columns columns columns isin columns max max columns assign drop columns columns get sum columns columns DataFrame DataFrame
merge query drop
astype astype
DataFrame values values values apply apply
DataFrame where isin
groupby apply sum sum
set_index columns index set_index columns index isin all
groupby transform nunique set_index sum reindex
values assign sort_values transform groupby transform first
DataFrame iloc iloc columns drop_duplicates reset_index drop
loc iloc loc iloc
DataFrame DataFrame merge notnull fillna to_string index merge assign notnull
isnull
index index index DataFrame index index index index index index DataFrame index index index index info info info info info index index
apply mask apply
reindex index reindex index
apply join apply join replace
groupby agg mean count reset_index
groupby
set_index set_index
DataFrame add index DataFrame index columns
DataFrame DataFrame sum values values values values
values values
apply to_csv
pivot columns values apply isna dropna rename_axis
DataFrame DataFrame groupby diff abs dropna drop
to_sql append index
rename columns
first where where
DataFrame where count where count
merge loc drop_duplicates
columns DataFrame loc loc ffill diff mask notnull notnull cumsum ffill loc loc cumsum ffill loc ffill diff mask notnull loc mask loc mask cumsum ffill notnull diff fillna lt cumsum notnull diff fillna lt cumsum
apply columns copy loc
index loc
groupby size apply set_index to_dict index to_dict size size size size
sort_values drop_duplicates first
empty append append join keys keys empty DataFrame DataFrame append replace to_csv
rename ne shift cumsum groupby apply join reset_index reindex columns columns
DataFrame DataFrame DataFrame to_csv mode append DataFrame DataFrame DataFrame to_csv rename columns to_csv
items DataFrame
transpose DataFrame T rename columns rename columns loc columns astype astype T set_index rename astype index index
duplicated DataFrame duplicated duplicated loc isin duplicated loc isin
DataFrame columns loc apply
DataFrame
shape sum
loc loc
count groupby transform size
filter columns replace replace
groupby mean reset_index
DataFrame columns
groupby transform var astype
get values items all max values items update add update append DataFrame reset_index drop
set_index
append mode to_excel mode mode to_excel index
DataFrame index apply DataFrame index apply apply
any groupby size reset_index any groupby sum reset_index
sort_values
merge eval min max min max
stack reset_index drop index loc copy stack reset_index drop index loc copy reset_index drop stack reset_index drop index DataFrame columns loc stack reset_index drop index loc copy reset_index drop values stack reset_index drop index loc copy reset_index drop values
isin
index index index index loc loc loc
set_index stack rename reset_index
astype astype
sample
count pad notna apply pad
join DataFrame set_index resample count DataFrame set_index resample count
append append DataFrame index DataFrame columns set_index count all columns loc sum mean loc drop mean get columns applymap index reset_index
where shift where shift index
last last squeeze get
columns drop join
reset_index drop reset_index drop merge set_index apply duplicated where
pivot_table index columns values count reindex values index fillna
shape
groupby count sort_values index groupby count rename columns sort_values index
apply replace columns astype astype merge drop
append assign
isnull sum DataFrame columns isnull sum
apply
set_index groupby reindex index min index max ffill reset_index reindex columns groupby min max set_index reindex ffill reset_index
merge
columns apply where add_suffix
DataFrame
replace astype iloc mask replace astype loc mask
columns DataFrame columns
merge astype copy drop_duplicates first index iloc reset_index drop
get all min max DataFrame cumsum astype pad mode pad shift columns index columns sum groupby sum replace stack stack unstack astype reset_index rename_axis columns rename columns
DataFrame set_index set_index groupby agg min max
columns DataFrame values columns index index
iloc columns iloc columns loc index loc index
rename columns iloc columns DataFrame rename columns iloc columns
merge
values join values
loc astype astype astype astype join join to_csv sample index
DataFrame columns loc loc loc
apply
DataFrame DataFrame DataFrame DataFrame first loc DataFrame groupby apply groupby agg groupby agg groupby agg groupby agg
DataFrame columns replace replace replace replace
DataFrame columns DataFrame columns merge merge
sum sum sum groupby sum sum groupby
DataFrame apply apply explode apply explode explode
shift
mask eq merge mask bfill diff ne cumsum
get get append DataFrame
groupby mean plot size
sample pivot_table index columns values sum
groupby to_excel
index where columns apply sum
groupby groupby sum
loc columns loc loc ne ne bool
values shape index columns mask max max stack index mean std stack idxmax mean std stack index values mean std where values values max index values columns values mean std values shape index columns mean std DataFrame min sort_values
index
sort_values duplicated mask sort_values drop_duplicates first reset_index drop
eval set_index
apply replace replace
head
append DataFrame pop index DataFrame pop index items to_excel
DataFrame T columns DataFrame
count mean std apply mask describe T
index index index index index
values shape shape values shape shape shape shape DataFrame columns columns
loc loc apply
columns sum
set_index groupby count ge all loc
items items to_excel index
drop_duplicates last groupby apply set_index asfreq drop reset_index
get all isna all get all index all columns drop columns index
drop all drop index drop all drop index
style apply
groupby
DataFrame DataFrame append shape
mask replace get replace pipe pipe replace pipe pipe
apply apply apply
DataFrame items T to_excel index
drop merge set_index index combine_first
groupby filter any
set_index set_index dropna
loc rolling apply gt all shift
DataFrame items apply explode DataFrame index index columns
melt groupby value_counts unstack sort_index rename_axis columns index melt pivot_table columns index size rename_axis index columns sort_index
DataFrame from_dict set_index set_index reset_index
append append append append append append DataFrame index index iloc index index append
set_index index reindex reset_index
DataFrame merge
join eval
pop
loc index index loc index index
DataFrame size DataFrame loc where get values first values loc values values values DataFrame shape shape
join
DataFrame drop DataFrame drop apply
DataFrame
loc astype
drop explode groupby agg count sum reset_index
DataFrame values index columns columns columns DataFrame values index columns columns columns DataFrame iloc values index columns columns columns groupby apply
value_counts value_counts idxmax max
append DataFrame columns
apply
dropna sort_values groupby diff sub dropna sort_values groupby shift sort_values
index index
where notna sum
index values
loc keys loc
groupby apply shift index index droplevel groupby apply shift index index droplevel
columns columns columns count count count count
DataFrame columns index transform apply transform
drop columns shape shape
columns
assign where fillna groupby apply fillna cumsum
copy copy iloc iloc append
items to_excel index
astype cumsum loc groupby mean
to_sql replace
applymap
sub groupby transform min
groupby agg first groupby value_counts index index index index index loc index to_string index loc index to_string
DataFrame set_index apply explode reset_index duplicated loc DataFrame mask iloc apply apply explode loc index duplicated mask
merge drop drop
loc loc apply shift isna sum
groupby groupby mask ne groupby rename groupby rename mask mask join groupby first fillna astype
groupby transform transform
columns groupby agg sum reset_index
bool
set_index columns index groupby columns columns loc groupby loc columns update loc update all
sub to_excel
loc values abs values values
mean groupby mean groupby transform mean groupby mean
eq any
loc index loc loc apply loc loc isin index drop
first DataFrame columns size size first
merge merge filter
DataFrame groupby sum reset_index
replace dropna eq mean mul T applymap dropna eq mean mul T applymap replace dropna eq mean DataFrame replace dropna eq mean mul T applymap
DataFrame keys groupby count keys plot
DataFrame value_counts head rename_axis reset_index
rank
mean keys mean keys
astype items
sort_values sort_values
at
groupby index
DataFrame index columns DataFrame index columns DataFrame add index columns
isna
loc
groupby astype pivot columns index droplevel reset_index
get append replace append replace append replace append DataFrame columns DataFrame
empty
iterrows append
product values values product index index
columns columns dtypes columns columns dtypes columns append
where
set_index stack rename index rename loc sum loc assign reset_index
to_dict assign apply
value_counts iteritems
apply
astype replace astype replace
join columns join columns join columns
index index index assign index DataFrame values index index columns
DataFrame T DataFrame T mode mode replace
replace replace replace replace replace replace replace apply add_prefix
replace
append replace size size DataFrame
index index index set_index index index index first index index
set_index set_index reset_index where eq
drop columns insert loc
get get replace replace apply where get get
iloc columns columns
DataFrame DataFrame
mask merge eq mask set_index index isin set_index index
pct_change
apply isnull sum
isna eq
DataFrame apply DataFrame apply
sum DataFrame values
loc all loc columns
DataFrame DataFrame iloc iloc merge interpolate
DataFrame groupby count reset_index count
bool ne bool
all columns set_index columns columns columns columns stack filter at replace eq any replace stack reset_index groupby set_index append unstack reset_index drop reset_index
sample T filter
stack stack reset_index drop columns drop_duplicates groupby pivot index columns values rename_axis index columns DataFrame from_dict apply drop_duplicates to_dict index DataFrame apply DataFrame values DataFrame columns stack stack compare empty DataFrame columns stack stack fillna fillna all
nunique value_counts index iat index iat append DataFrame index
where join where where join where
get isnull all index index drop first all dropna all
DataFrame DataFrame
DataFrame
DataFrame apply apply
duplicated loc groupby transform min
index reindex index append join set_index add_suffix columns append join set_index reindex index add_suffix columns
index DataFrame
values DataFrame from_dict index T
index index
apply round
replace groupby ffill
DataFrame index
explode groupby add astype pivot columns values rename_axis columns
DataFrame size columns loc loc drop columns loc drop loc reset_index drop loc drop columns loc drop loc reset_index drop loc drop columns loc drop loc reset_index drop loc drop columns loc drop axes plot axes plot axes plot axes plot axes columns values
DataFrame DataFrame mask isin mask isin where isin mask add mask merge
values sum sum
cumsum sum
groupby agg mean
where all values size
eq isin pivot astype sub reset_index assign
ne shift cumsum assign pivot assign diff iloc reset_index rename_axis
DataFrame columns merge index set_index DataFrame columns merge index set_index columns columns DataFrame values itertuples where keys DataFrame index index values keys loc values loc values loc values loc values append DataFrame DataFrame
apply apply
mask mask loc mask sum iloc values values iloc columns
get append append append DataFrame columns to_csv index
join
merge
pivot_table index columns values columns DataFrame to_records pivot get
append to_csv
DataFrame columns DataFrame columns
groupby transform diff sub where diff diff diff add
ne shift ne shift any
apply groupby shape
append to_csv index
DataFrame DataFrame
groupby agg join sum
keys values items values keys append keys join keys
merge
append
eq set_index stack align set_index stack groupby all
at drop copy
to_excel
apply apply
items add add append append first items values keys DataFrame set_index
DataFrame eq sum lt all
merge rename columns drop sort_values reset_index drop sort_values reset_index drop isin dropna isin dropna isin dropna merge rename columns drop
replace
values apply join keys
DataFrame first shift where replace fillna ffill where where diff diff diff mean
DataFrame
index
DataFrame columns to_pickle loc
copy append
merge merge fillna
index replace
query to_excel index
columns index reset_index drop to_csv index
last first value_counts first
info DataFrame dtypes to_csv index
DataFrame columns columns loc isna DataFrame rename columns loc columns DataFrame
iloc dropna all
groupby apply ffill bfill groupby ffill combine_first bfill
append join replace append append pad max pad pad DataFrame transpose get transpose empty DataFrame append join replace DataFrame from_dict join join
DataFrame mask gt any any where mask
DataFrame filter
groupby sum rename_axis reset_index
groupby agg mean count append DataFrame append
isnull isnull notnull fillna assign
groupby pct_change
rolling apply index
apply get values
DataFrame from_dict index stack reset_index DataFrame from_records values drop columns pivot index columns values fillna
filter empty empty
min
index replace shape
isna all cumsum dropna groupby
DataFrame DataFrame index
set_index melt dropna pop columns melt sort_values merge columns merge rename columns drop_duplicates set_index reindex columns compare dropna dropna
iterrows append append append
astype
pivot pivot_table values size index columns sum pivot pivot cumsum
reset_index melt index pivot index index values columns columns loc notna
iterrows append
DataFrame to_csv index DataFrame to_csv index
lookup index
explode query groupby agg first apply
drop_duplicates index
to_period to_period apply
at join
values sum
values values sum sum
iat iat where values where
groupby agg sum count rolling sum shift assign sum count
all columns columns replace values
astype where eq values add fillna sum
style groupby count unstack plot
index isin reset_index groupby index first loc loc
DataFrame apply filter
set_index
groupby product apply reindex product groupby product ffill bfill droplevel
append append mean std
filter dropna sort_values index iloc index
apply
iloc iloc tail values iloc iloc head values iloc iloc tail values iloc iloc head values apply apply
assign where cumsum melt dropna drop columns
astype astype join DataFrame columns
style applymap
DataFrame get duplicated duplicated duplicated drop_duplicates replace values duplicated loc where isin
DataFrame apply drop columns explode
set_index columns unstack T
groupby transform count quantile quantile quantile
merge merge append merge all DataFrame merge append append merge
iloc loc columns iloc
columns rename columns
values max notna all idxmin idxmax apply loc idxmin idxmax max values max notna all idxmin idxmax mean std mean std
index index DataFrame index append append index drop head
itertuples
groupby agg mean std swaplevel round astype mean std append
style style
DataFrame
DataFrame rank groupby transform count rank groupby DataFrame groupby copy last drop columns loc isin drop_duplicates last merge where index iterrows sum append round append
first duplicated last copy first loc loc values drop loc
pivot index columns droplevel rename_axis columns reset_index
groupby min merge sort_values
keys
DataFrame set_index loc
groupby reset_index iterrows columns columns columns append append DataFrame columns
DataFrame DataFrame combine_first dtypes dtypes combine_first dtypes dtypes iteritems astype
loc shift replace drop_duplicates drop
fillna
join append append DataFrame columns rename columns DataFrame columns append iloc columns rename columns DataFrame columns append rename columns DataFrame columns append dropna replace replace astype loc groupby idxmin to_excel index to_excel index join
index index
loc eq loc eq sub
to_dict items loc
replace
isin isin
iat
DataFrame columns DataFrame columns merge to_excel
groupby agg to_records
DataFrame columns loc loc loc iloc loc iloc
set_index to_dict filter columns replace
shift replace astype where where shift mask copy
merge merge merge
columns append append index iterrows columns append
ne shift any cumsum astype
plot plot
add drop_duplicates groupby agg sum size first columns columns join reset_index rename columns div astype mul astype where where isin join DataFrame drop_duplicates set_index max reindex add columns astype append join join reindex columns
DataFrame index columns all count first append append empty add
round isin isin
loc to_dict
set_index update set_index reset_index set_index set_index assign set_index update set_index assign set_index loc isin
groupby notna cumsum compare groupby notna cumsum transform size ffill mask groupby ffill where notna
dtypes astype describe count mean std min max
join size replace
columns
groupby transform value_counts groupby transform rank max sub sub groupby agg count mean median
items
apply columns
style max
DataFrame size DataFrame size where
append columns add columns iloc iloc
join
apply count count
items select_dtypes assign applymap
DataFrame index iterrows append fillna
replace attrs
groupby size reset_index values groupby
append append last DataFrame DataFrame
join
columns join join columns
lt lt DataFrame set_index lt lt
DataFrame count apply count
DataFrame index DataFrame size columns index join set_axis columns astype stack rename fillna
groupby agg mean
groupby last to_csv
shape append DataFrame DataFrame
swaplevel reindex index rename_axis index
mask
iterrows mean std mean std
get
DataFrame
DataFrame insert insert melt columns astype
value_counts to_dict DataFrame count count value_counts to_dict value_counts to_dict get
mask notna isin values mask isin mask sum mask bool
DataFrame isin isin all bool isin all any
cumsum values astype loc diff loc loc groupby sum reset_index drop groupby apply
apply
any
groupby apply loc values values loc index index columns
sample to_csv
groupby plot
items
merge drop columns query reset_index drop iloc assign iloc append reset_index drop rename
append items items merge to_csv
loc index loc index set_index append loc
DataFrame values values product DataFrame product columns
DataFrame columns groupby mean
xs max xs min sort_index index where max min index index
loc items
drop columns
plot get
iloc
apply max reset_index apply max reset_index sort_index
append append append append append append append
apply
